# Proposed — Sabrina-Local-AI

> Things scheduled runs want to do but need human approval before execution. Triaged in the evening review window. Approved items graduate to QUEUE.md by the next Planner run.

## How approval works

- Check the box `[x]` to approve. The next Planner run will move it to QUEUE.md with appropriate priority.
- Strike through (or delete) to reject.
- Annotate with `[needs-discussion]` if you want to talk it through before deciding.

## Format

```
- [ ] [proposed-by: role / YYYY-MM-DD] Title
  - Why: ...
  - What it would do: ...
  - Risk / blast radius: ...
  - Suggested priority: P0 | P1 | P2
```

---

- [ ] [proposed-by: pre-automation-prep / 2026-04-28] Commit the ToolSpec MCP migration
  - Why: CLAUDE.md notes the migration "shipped uncommitted." Until it's in git, every Worker reads STATE describing code that may not exist on the current branch. Closes the state-vs-reality gap.
  - What it would do: stage just the ToolSpec MCP migration files (`to_anthropic_dict()` / `to_mcp_dict()` and tests), commit separately from any other in-flight work, leave the ClaudeBrain wire-up for the existing P1 queue item.
  - Risk / blast radius: low. Pure additive; no callers of `tools=` exist yet (that's the next item). Pre-commit `compileall` already in place.
  - Suggested priority: P1

- [ ] [proposed-by: pre-automation-prep / 2026-04-28] Commit the ONNX embedder swap
  - Why: STATE.md says ONNX is the default `[memory.semantic.embedder]` backend "shipped uncommitted." sentence-transformers retained as legacy fallback per CLAUDE.md. Same state-vs-reality gap as above.
  - What it would do: stage just the embedder swap (config default + ONNX backend module + the new regression tests), commit separately. Leave the rest of the working tree alone.
  - Risk / blast radius: medium. The semantic-memory layer is critical-path; swap is supposedly already shipped, but a fresh commit means the pre-commit hook re-runs `compileall` and any reviewer can see the diff cleanly. If memory tests pass on Windows, ship.
  - Suggested priority: P1

- [ ] [proposed-by: pre-automation-prep / 2026-04-28] Add ToolSpec.to_anthropic_dict / to_mcp_dict round-trip tests
  - Why: CLAUDE.md notes the ToolSpec "exposes both `to_anthropic_dict()` and `to_mcp_dict()` for forward-compat" — but there's no documented round-trip test. The existing P1 wire-up item depends on Anthropic dict shape being correct. Closing this loop unblocks that item.
  - What it would do: small new test file under `sabrina-2/tests/` exercising both serializers and asserting round-trip equivalence. ~30-60 minutes, well-scoped, no production-code change.
  - Risk / blast radius: very low. Test-only, no runtime path.
  - Suggested priority: P2

- [x] [proposed-by: pre-automation-prep / 2026-04-28] Split the queued ClaudeBrain wire-up into two Worker-sized items (approved 2026-05-02 via workflow-efficiency research)
  - Why: the current QUEUE P1 (verbatim: "Wire ClaudeBrain.chat to pass tools= and handle ToolUseBlock events (~150 LOC), enabling write_clipboard ToolSpec end-to-end. Definition of done: write_clipboard fires from a real voice turn; tests pass; ToolSpec.to_anthropic_dict round-trips. Notes: ToolSpec MCP migration already shipped uncommitted; brain side is the missing wire-up.") is realistically 2-3 hours including tests and Windows e2e validation. Per planner.md, queue items should fit in one Worker run (~1 hour). Today's first Worker is likely to bail with `[in-progress]` on this one.
  - What it would do: replace the single P1 with two: (a) "ClaudeBrain.chat accepts `tools=` kwarg and emits ToolUseBlock-shaped events" + unit tests; (b) "wire `write_clipboard` handler end-to-end + Windows voice-loop validation per validate-automation.md."
  - Risk / blast radius: zero — this is a queue restructure, not a code change. Improves Worker throughput on day one. If QUEUE has shifted by the time the human approves this, treat the verbatim quote above as the canonical version of the item being split.
  - Suggested priority: P1 (both halves)

- [ ] [proposed-by: night-auditor / 2026-04-29] Take stock of the entire pass-2 working tree before more Workers iterate on top of it
  - Why: `git status` shows 27 modified files + 27 untracked files. The two existing PROPOSED items ("commit ToolSpec migration", "commit ONNX embedder swap") were written on the assumption that the uncommitted surface is just those two changes — but the actual uncommitted surface is much bigger. Modified production code includes `sabrina-2/src/sabrina/brain/claude.py` (+134 lines), `memory/store.py` (+175), `cli.py` (+162), `voice_loop.py` (+33), `chat.py` (+10), plus `tests/test_smoke.py` (+578), `pyproject.toml`, `sabrina.toml`, `config.py`, and 9+ rebuild/* docs. Until this is partitioned into commit-shaped units, every Worker run reads STATE.md describing code that may not exist on the current branch and may build on top of working-tree state that the next `git stash` or branch switch would erase. This is the canonical state-vs-reality gap and dwarfs the two specific commits already proposed.
  - What it would do: a one-time triage session (human-led, not Worker-automated) to walk the working tree, decide what is shippable / what is exploratory / what is dead, and produce a sequenced commit plan. Not a code change in itself.
  - Risk / blast radius: zero by itself; high-value because every Worker action between now and that triage is at risk of being silently invalidated.
  - Suggested priority: P1

- [ ] [proposed-by: night-auditor / 2026-04-29] Decide policy on the 22 untracked research drafts under `rebuild/drafts/research/`
  - Why: `rebuild/drafts/research/2026-04-26-*.md` (22 files: portrayal-study, threat-model, ollama-parity, multi-surface-identity, memory-architecture-evolution, etc.) are untracked. Same goes for `rebuild/CLEANUP_FINDINGS_2026-04-26-night.md`, `rebuild/CLEANUP_FINDINGS_2026-04-26.md`, `rebuild/LEGACY_REPLACEMENT_GATE.md`, `rebuild/RESEARCH_INDEX_2026-04-26.md`. Either they're load-bearing (commit) or they're scratch (gitignore the drafts directory). Right now they exist only on Eric's disk and a `git clean -fd` would silently delete a day's worth of intellectual capital.
  - What it would do: human decides — `git add` the keep-set, add `rebuild/drafts/research/` to `.gitignore` for the discard-set, or split.
  - Risk / blast radius: low. Just reduces the cost of an accidental `git clean`.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-04-29] Decide fate of `stage-pass2-commits.ps1` / `stage-pass2-commits-v2.ps1` (root-level, untracked)
  - Why: two PowerShell scripts at the repo root, untracked, named like mid-flight staging tooling for the pass-2 commit work that produced the current uncommitted surface. Either they're done and should be deleted, or they're the canonical recipe for the working-tree triage above and should be committed under `scripts/` (or wherever) so they don't disappear.
  - What it would do: human decides — delete or commit. Trivial.
  - Risk / blast radius: trivial.
  - Suggested priority: P2

- [ ] [proposed-by: researcher / 2026-04-29] Mark PROPOSED #3 (ToolSpec round-trip tests) as in-tree-done, not queue-ready
  - Why: research/2026-04-29-claudebrain-tool-wire-up-surface.md confirms the four round-trip tests already exist at `sabrina-2/tests/test_smoke.py:1869-1965` (test_toolspec_to_anthropic_dict_uses_input_schema_key, test_toolspec_to_mcp_dict_uses_camelcase_input_schema_key, test_toolspec_round_trip_both_serializations_share_payload, test_toolspec_mcp_shape_has_required_fields_per_spec). PROPOSED #3 was authored before the night-auditor's working-tree census surfaced. This is the first concrete instance of the state-vs-reality gap PROPOSED #5 calls out.
  - What it would do: when triaging PROPOSED #3, the human checks the box only as an instruction to commit the existing tests as part of the working-tree triage (PROPOSED #5), not to write new tests. The next Planner should not graduate PROPOSED #3 to QUEUE as a write-tests item.
  - Risk / blast radius: zero. Documentation correction only.
  - Suggested priority: P2 (informational; modifies how PROPOSED #3 is interpreted, not what gets shipped).

- [x] [proposed-by: researcher / 2026-04-29] Adopt the (a)/(b) split for QUEUE P1 with the precise diff surface from research/2026-04-29 (approved 2026-05-02 via workflow-efficiency research)
  - Why: PROPOSED #4 already calls for splitting the QUEUE P1 into two Worker-sized items but didn't pin the boundary. The research doc lands the precise surface: (a) protocol.py + claude.py + ollama.py + unit tests; (b) events.py + voice_loop.py + sabrina.toml flip + Windows e2e validation. The recommended Anthropic-SDK strategy is to KEEP the existing `stream.text_stream` loop (preserving first-sentence-streaming UX) and dispatch tool_use via `final.content` + `final.stop_reason == "tool_use"` after the loop completes. Recursion cap = 5. Tool-result follow-up uses `tool_use_id` + `content` (string for write_clipboard's dict→json) + `is_error` flag. Ollama raises `NotImplementedError` with the exact message specified.
  - What it would do: replace the single P1 in QUEUE with two queue items per the research recommendation. The research doc is the canonical reference for the diff; queue items just point at it.
  - Risk / blast radius: zero. Queue restructure + reference, not a code change.
  - Suggested priority: P1 (both halves).

- [ ] [proposed-by: researcher / 2026-04-29] Document the ToolSpec-location divergence between tool-use-plan.md and the shipped code
  - Why: `rebuild/drafts/tool-use-plan.md:200-209` puts `ToolSpec` in `brain/protocol.py`. The shipped code (`sabrina-2/src/sabrina/tools/__init__.py:36-71`) puts it in the tools package, with the rationale "handlers and their registrations live next to the spec class." Both arguments are reasonable; the divergence is currently undocumented and a future maintainer will hit it. This isn't a code change — it's a plan-doc / DECISIONS.md update.
  - What it would do: either (1) edit tool-use-plan.md to record the `tools/__init__.py` decision and why, or (2) add a small DECISIONS.md entry (decision number TBD) capturing the layering rationale per the operator-voice / decision-doc convention. (1) is lighter; (2) is more durable.
  - Risk / blast radius: zero. Documentation only. Touches `rebuild/drafts/` (allowed for plans) and possibly `rebuild/decisions/` (off-limits — would need explicit approval; that's why this is a proposal, not a worker task).
  - Suggested priority: P2.

- [ ] [proposed-by: night-auditor / 2026-04-30] Extend PROPOSED #5 working-tree census: `.pre-commit-config.yaml` is also dirty
  - Why: the previous night-auditor's working-tree enumeration (2026-04-29 02:37) listed the modified production code and the rebuild docs but missed `.pre-commit-config.yaml`. Tonight's audit confirms it is modified — the local hook `compileall-sabrina2` (entry: `python -m compileall -q sabrina-2/src`) lives only in Eric's working tree. CLAUDE.md describes this hook as load-bearing ("Pre-commit hook runs `python -m compileall -q sabrina-2/src` to catch SyntaxError-on-commit"), but a `git stash` or branch switch without commit would silently delete it. The hook content is in-scope per CLAUDE.md ("Do not modify or extend the pre-commit hook outside its existing scope" — this is the existing scope), so this isn't a violation; it's a census correction. The triage in PROPOSED #5 should fold this file into its commit-shaped-units plan.
  - What it would do: when the human-led triage in PROPOSED #5 happens, treat `.pre-commit-config.yaml` as one of the files to partition. Likely commits with whatever earlier "scaffold compileall hook" change introduced it (~2026-04-26 timing, per `git log` last-touched).
  - Risk / blast radius: zero by itself. The risk is that without committing the hook, a fresh clone or a stash mishap leaves the project with no SyntaxError safety net on commits.
  - Suggested priority: P2 (subordinate to PROPOSED #5).

- [ ] [proposed-by: night-auditor / 2026-04-29] Ruff cleanup pass once the working-tree triage settles
  - Why: `ruff check src tests` reports 82 errors across `sabrina-2/`. Breakdown: 16 unsorted-imports (I001), 13 function-uses-loop-variable (B023), 11 datetime-timezone-utc (UP017), 9 suppressible-exception (SIM105), 8 unused-import (F401), 8 quoted-annotation (UP037), 5 function-call-in-default-argument (B008), 3 deprecated-import (UP035), 2 zip-without-explicit-strict (B905), 2 yoda-conditions (SIM300), and one each of B904/E402/E501/SIM117/UP041. 50/82 are `--fix`-eligible. Separately, `ruff format --check` says 20 files would be reformatted (incl. `cli.py`, `voice_loop.py`, `memory/store.py`, `chat.py`, `tests/test_smoke.py`). Almost all of this is in code that's already touched in the uncommitted working tree — auto-fixing now would inflate a triage diff that's already huge. Run after triage, not before.
  - What it would do: `ruff check --fix` for the 50 fixable items; manual review of the 13 B023 (loop-variable-in-closure) findings since those have actual bug potential; `ruff format` for the 20 drift files.
  - Risk / blast radius: low — auto-fixes are mechanical. B023 is the one to read carefully (closures over loop variables can mask real bugs). Pre-commit `compileall` will catch any syntax regressions.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-05-01] Census correction: PROPOSED #6 says "22 untracked research drafts"; actual is 19 untracked + 2 already-tracked
  - Why: PROPOSED #6 (and the 2026-04-29 02:37 night-auditor JOURNAL entry that produced it) records "22 files" of untracked drafts under `rebuild/drafts/research/`. Tonight's audit shows the directory holds 21 files total: 19 untracked (all `2026-04-26-*.md`) and 2 already-tracked (`2026-04-25-sources.md`, `2026-04-25-stack-alternatives-survey.md`). The "22" appears to have been an off-by-3 count or pre-deletion of 3 drafts that no commits or other audits captured. Either way, the human triage in PROPOSED #6 should not search for files that aren't there. The 19 actual files: avatar-cue-track-implementation, calendar-drive-integration, celebration-and-care, failure-communication, identity-stability-under-stress, memory-architecture-evolution, multi-surface-identity, ollama-parity, personality-eval-framework, portrayal-sources, portrayal-study, round2-sources, sources-research-batch3, sources, stillness-ambient-mode, threat-model, toolspec-first-three, voice-to-voice-direct, wake-word-training-pipeline.
  - What it would do: when triaging PROPOSED #6, work from the 19-file ground truth above. The 2 tracked 2026-04-25 drafts are already in git and should be excluded from the gitignore-vs-commit decision; PROPOSED #6's policy applies only to the 19 untracked.
  - Risk / blast radius: zero. Documentation correction only.
  - Suggested priority: P2 (subordinate to PROPOSED #6).

- [ ] [proposed-by: night-auditor / 2026-05-01] Working-tree census extension: `write_test.txt` is `D`eleted-not-staged
  - Why: tonight's `git status --short` shows ` D write_test.txt` — a tracked file (added in commit `8dfb1ac` "Fix barge-in thin-spots (009a)"; content is the literal string "Line 1\nLine 2\n…\nLine 10") deleted in the working tree but the deletion is not staged. This is new since both 2026-04-29 02:37 and 2026-04-30 02:37 night-auditor baselines (neither of which mentions this file). The file appears to be scratch test data committed by mistake during the 009a barge-in work, so the working-tree deletion is probably correct — but as long as it sits in working-tree-only state, a `git stash`, branch switch, or hard reset will quietly bring it back. Subset of PROPOSED #5 (working-tree triage).
  - What it would do: when the human-led triage in PROPOSED #5 happens, treat the `write_test.txt` deletion as one of the items to partition: stage and commit the deletion (most likely), or restore the file if it turns out to be load-bearing for some test path that wasn't obvious. AST/compileall don't fail without it; nothing in `tests/test_smoke.py` references the path, so "stage the deletion" is the expected outcome.
  - Risk / blast radius: trivial — the file is 10 lines of "Line N" scratch text; no production code references it.
  - Suggested priority: P2 (subordinate to PROPOSED #5).

- [x] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Codify a "Linux-validated, Windows-pending" partial-DoD tier in CLAUDE.md (approved + applied 2026-05-04 via unblock run; CLAUDE.md "Partial-DoD tiers" section is the implementation)
  - Why: 6-day idle pattern (worker-8am bailed 2026-04-29, idle 8am–12pm × 6 days = 30 wasted slots) traces to a single root cause: the project DoD is binary. CLAUDE.md says "All `pytest` tests pass on Windows" and "Voice loop validated end-to-end (record sample → STT → brain → TTS) before claiming any voice-loop change ships." The Cowork sandbox is Linux/Python 3.10 + no pywin32. There is no path for any Linux Worker to ship anything that touches voice-loop runtime. Worker.md step 7 already names this as a half-exception ("Windows-only e2e gate exception"), but the DoD it points back to is still binary, so workers route to NEEDS-INPUT instead of partial-ship. The (a)-half of the QUEUE P1 — protocol/claude/ollama + 6 unit tests, no voice_loop changes — is structurally Linux-shippable today; only the convention is missing.
  - What it would do: amend CLAUDE.md "Definition of done (project-wide)" with an explicit second tier: "Linux-validated subset" — for changes that don't modify voice-loop runtime, audio I/O, clipboard, mss/pynput/pyperclip paths, or pywin32-only modules. Workers may ship the diff to an `automation/...` branch + draft PR with `e2e: pending Windows runner` in the title and a "Windows DoD checklist" body comment. Item moves to a new `[linux-shipped]` state in QUEUE/DONE; Eric's next Windows session promotes to `[done]` after running the Windows checklist. Touches: CLAUDE.md (~15 lines), worker.md step 7 (relax language from "exception" to "tier"), QUEUE.md format note.
  - Risk / blast radius: low. The `[linux-shipped]` marker is informational; nothing claims production-ready until Eric promotes. Existing PROPOSED #4/#9 already imply this split; this just makes it a project rule. Doesn't touch off-limits dirs (`rebuild/decisions/`); doesn't push to main.
  - Suggested priority: P0

- [x] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Grant Planner standing authority to split mixed-surface QUEUE items without `[x]` (approved + applied 2026-05-04 via unblock run; planner.md step 3 standing-authority subsection is the implementation)
  - Why: PROPOSED #4 + #9 (the (a)/(b) split with the research-doc-pinned boundary) have sat unchecked for 6 days. They are queue-restructure items, not code changes, and the research doc already pins the boundary precisely. Requiring `[x]` on a queue restructure that is mechanically derivable from the diff surface is a cost without a payoff. Standing pattern, not one-off: any future queue item with both Linux-side (brain/data/test/handler) and Windows-side (voice_loop/audio/clipboard/runtime) surfaces will hit the same wall.
  - What it would do: amend planner.md step 3 — when a queue item has surface in both buckets, planner SHOULD split into (a)-Linux-half and (b)-Windows-half during morning curate. Each half gets its own DoD tier: (a) Linux-validated, (b) Windows-validated. Planner cites the boundary source (research doc, plan-doc, or its own assessment) in the queue notes. The human still owns approving NEW items via PROPOSED → QUEUE; the new authority is restricted to splitting an already-approved item. PROPOSED #4 + #9 graduate as the first concrete instance.
  - Risk / blast radius: low. Planner cannot promote new work; only restructure already-queued work. Workers still gate on tier-appropriate runtimes.
  - Suggested priority: P0

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Tag every QUEUE item with `linux-runnable` or `windows-required`
  - Why: workers currently inspect each queue item top-down, decide based on the item body whether the DoD is reachable from their runtime, and either work or idle-exit. The decision is mechanical and recurring. Today's planner already knows whether an item touches voice_loop / audio / pywin32; surfacing that as a tag turns the worker's gate from "read item, parse DoD, infer runtime" to "match my runtime to tag." Composes with the partial-DoD tier (P0 #1) and split-authority (P0 #2) — all three pieces fit together.
  - What it would do: planner adds `[linux-runnable]` or `[windows-required]` (or `[linux-runnable, windows-validation-pending]` for items whose Linux half is workable but full DoD needs a Windows pass) to each QUEUE entry. Workers gate on tag — Linux-sandbox worker picks `[linux-runnable]` items, idle-exits if none. Adds ~one line to worker.md step 2 and ~one line to planner.md step 3.
  - Risk / blast radius: trivial. Documentation contract; no runtime change.
  - Suggested priority: P1

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Convert idle Worker slots into a bounded Linux-sidework ladder
  - Why: 5 worker slots × 6 days = 30 idle "no actionable item, idle exit" entries. The new `engineering:code-review`, `engineering:debug`, `engineering:testing-strategy` skill wiring (already in worker.md steps 4, 6, 8) sits unused because the bailout pattern routes around them. There is bounded, safe Linux-runnable sidework that does not touch the dirty tree, does not require human approval, and produces durable artifacts: (a) `code-review` against the most recent commit on `main` (read-only, journals findings to PROPOSED if any P0/P1 surface); (b) `testing-strategy` scoping pass against any `[in-progress]` queue item to identify a Linux-runnable test subset; (c) `ruff --fix` pass on already-committed files only (skips the dirty tree); (d) census-sync of PROPOSED #5/#6/#11/#13/#14 with current `git status` (corrects state-vs-reality drift).
  - What it would do: amend worker.md step 3 — when no actionable item exists, before idle-exit, pick ONE sidework slot from the ladder above (priority order, first match wins). Each is bounded: read-only or doc-only, no production code edits, no commits without a real queue item. Failure mode is a JOURNAL line and exit, same as today. Adds ~20 lines to worker.md.
  - Risk / blast radius: low. All four sidework branches operate outside the dirty tree (PROPOSED #5 still owns triage). Code-review is read-only by design. Ruff --fix on clean files only adds a worker-tagged commit to a sidework branch; no main/master touch.
  - Suggested priority: P1

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Auto-promote doc-only census-correction PROPOSED items without `[x]`
  - Why: PROPOSED #8 (reinterpret PROPOSED #3 as in-tree-done), #11 (`.pre-commit-config.yaml` census), #13 (PROPOSED #6 22→19 correction), #14 (`write_test.txt` deletion fold-in) are census corrections to other PROPOSED items, authored by night-auditor or researcher, touching only state files. They have sat 4–6 days awaiting `[x]`. They cannot meaningfully be rejected (they're factual corrections), and they don't change worker scope. Requiring human approval for fact-correction is review overhead with no decision payoff.
  - What it would do: amend planner.md step 3 — the planner MAY auto-merge a census-correction PROPOSED item into the parent it corrects (rewriting the parent's body in place) and remove the correction item, IFF: (a) authored by night-auditor / researcher / planner; (b) touches no code or runtime configs; (c) explicitly scoped as "subordinate to PROPOSED #N". Logged in the planner's JOURNAL bullet. The four current items above graduate-and-merge on the next planner run after this proposal lands.
  - Risk / blast radius: trivial. Census corrections are facts, not decisions; they age in place today purely because the planner has no authority to act.
  - Suggested priority: P1

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Move planner/researcher dedup-marker entries out of NEEDS-INPUT into STATUS-MARKERS.md
  - Why: NEEDS-INPUT count is 5 — 3 are real decision asks (worker-8am DoD fork, pytest-gate, GitHub-MCP), 1 is a contingent research scope ask (bounded-question 04-30, blocked on the same DoD-fork answer), and 1 is the planner day-2 marker (04-30) which is a legibility note that the role doc explicitly says "its only job is to keep the dedup rule honest." The marker inflates the count Eric sees in every digest from "3 real decisions + 1 contingent" to "5 open." That headline number drives evening-review fatigue.
  - What it would do: create `STATUS-MARKERS.md` (sibling to NEEDS-INPUT.md) that holds ONLY the role-doc dedup-marker entries. Update planner.md step 5 and researcher.md step 2 to write the marker to `STATUS-MARKERS.md` instead. The dedup rule still works (same scan target); the digest reads STATUS-MARKERS.md but does not count it toward the "Waiting on you" tally. NEEDS-INPUT becomes 3 + 1 contingent = 4, then 3 once the DoD fork is answered (which auto-resolves the contingent research entry).
  - Risk / blast radius: trivial. Pure file-move; no role-logic change beyond the write target.
  - Suggested priority: P1

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Have night-auditor classify NEEDS-INPUT items as decision / research-scope / structural
  - Why: today Eric sees 5 indistinguishable NEEDS-INPUT entries. Three are 1-line decisions (DoD fork pick (a)/(b)/(c)/(d); pytest-gate (a)/(b)/(c); GitHub MCP install or remove); one is a research-scope ask; one is a structural marker. The decisions are seconds-of-effort each; the structural one is a no-op. Without classification, the digest's "5 open" number reads as a bigger-than-it-is review burden.
  - What it would do: amend night-auditor.md step 5 (not its core scope; it's already reviewing NEEDS-INPUT every night) — for each open entry, add a one-character class tag in front of its line: `[D]` decision, `[R]` research-scope, `[S]` structural-marker. Digest's "Waiting on you" section groups by class with effort estimates. Composes with the STATUS-MARKERS move (P1 #6 above).
  - Risk / blast radius: trivial. Audit-side classification, no auto-action.
  - Suggested priority: P2

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Pre-stage the (a)-half ClaudeBrain wire-up as an explicit `[linux-runnable]` QUEUE item
  - Why: the moment the partial-DoD tier (P0 #1) and split-authority (P0 #2) ship, the cleanest first-test of the new convention is the QUEUE P1's (a)-half, whose diff surface is already grounded in `research/2026-04-29-claudebrain-tool-wire-up-surface.md` (protocol.py + claude.py + ollama.py + 6 unit tests, no voice_loop, no toml flag, no Windows e2e). This is the single highest-leverage Linux-runnable unit in the entire 14-day backlog.
  - What it would do: planner stages a new QUEUE entry: `[linux-runnable] [P1] (a) ClaudeBrain wire-up — protocol/claude/ollama + unit tests` with DoD scoped to "compileall + 6 unit tests pass under static-import; ToolSpec.to_anthropic_dict round-trip tests already exist at test_smoke.py:1869-1965 and ship as part of the commit; voice_loop / events.py / sabrina.toml flag deferred to (b)." Worker picks it up next slot, code-reviews own diff before commit (via the existing engineering:code-review wiring), pushes to `automation/worker-2026-MM-DD-Nam`, opens draft PR with `e2e: pending Windows runner`. The (b)-half stays as a separate `[windows-required]` item for Eric's next Windows session.
  - Risk / blast radius: low. The (a)-half is the cleanest Linux-runnable surface in the backlog. The diff is bounded (~80 LOC + 6 unit tests). The dirty `brain/claude.py +134` lines are personality blocks per JOURNAL — the wire-up delta should be commit-able alongside or alongside-only-the-wire-up; if it conflicts, the worker bails per existing role doc and writes to NEEDS-INPUT.
  - Suggested priority: P2 (depends on P0 #1 + #2 landing first)

- [ ] [proposed-by: workflow-efficiency-analysis / 2026-05-04] Reduce daily Worker schedule from 5 hourly slots to 2 (8am, 11am) until QUEUE consistently produces Linux-runnable items
  - Why: 5 idle "no actionable item, idle exit" entries per day × 6 days = 30 wasted slot-runs. Each slot is a Cowork session boot, file reads, and a JOURNAL append for no productive output. Even with the sidework ladder (P1 #5), 5 slots/day exceeds plausible Linux-runnable throughput on most days. Two slots — one for the morning queue grab, one mid-morning for follow-up or sidework — match the actual Linux-runnable shape of this project.
  - What it would do: edit `schedule.json` to remove the 9am, 10am, 12pm worker tasks and re-register only 8am + 11am. Eric re-runs the Cowork scheduled-tasks MCP registration once. Restore to 5 if/when DONE.md starts logging multiple ships per day. Note: the user's brief says "Don't change roles/schedule/state" for this analysis pass — so this proposal stages the schedule change but does not execute it; Eric checks `[x]` and re-registers when ready.
  - Risk / blast radius: trivial. Reducing scheduled tasks reduces noise; Eric can re-add at any time. Doesn't touch roles or state.
  - Suggested priority: P2

- [informational] [proposed-by: unblock-run / 2026-05-04] Partial-DoD tier and planner split-authority went live 2026-05-04
  - Why: this entry exists so the next night-auditor and the next planner see the rationale without having to reconstruct it from JOURNAL. PROPOSED #19 (codify Partial-DoD tier in CLAUDE.md) and PROPOSED #20 (planner split-authority for mixed-surface items) were both approved out-of-band on 2026-05-04 via the workflow-efficiency research that produced PROPOSED #19–#28. The unblock run applied the structural fixes directly: CLAUDE.md now defines Full DoD vs Partial DoD ("Linux-validated, Windows-pending"); planner.md step 3 now grants standing authority to split mixed-surface queue items without `[x]` when the boundary is research-doc-pinned. PROPOSED #4 + #9 were marked `[x]` in the same run as the first concrete instance, and the (a)-half wire-up was promoted to QUEUE as a `[linux-runnable]` P1.
  - What it would do: nothing further on its own — informational only. Acts as a status marker for tonight's night-auditor (which will see the new tier and the new authority and should NOT re-flag PROPOSED #19/#20 as "still unchecked" — both are now `[x]` with the implementation pointer in their headers). Tomorrow's planner can treat this entry as the canonical rationale-pointer; the next planner that runs after #19/#20 are observed-as-applied may delete them along with this informational entry.
  - Risk / blast radius: zero. Documentation of a change already applied.
  - Suggested priority: informational (no graduation; the human MAY delete this entry once the next night-auditor and planner runs have absorbed it).

- [ ] [proposed-by: night-auditor / 2026-05-05] JOURNAL.md append-only invariant breached overnight by worker-9am heredoc recovery — duplicate / triplicate entries
  - Why: tonight's audit shows JOURNAL.md has multiple duplicate entries from the 2026-05-04 09:00 worker-9am run's heredoc-recovery after Edit-tool truncation hit (per the worker's own JOURNAL bullet: "Edit-tool truncation hit repeatedly mid-run [...] JOURNAL.md (this file too — needed restoration mid-bookkeeping)"). Concrete duplicates by line number: `## [2026-05-04 07:00] planner` at lines 261 + 301; `## [2026-05-04 08:00] worker-8am` at 271 + 306; `## [2026-05-04 15:00] [unblock] partial-dod-and-split-authority` at 274 + 309; `## [2026-05-04 09:00] worker-9am` at 289 + 315 + 325 (triplicated). NEEDS-INPUT.md also has a duplicate worker-9am 2026-05-04 09:00 entry (lines 68 and 78). The role-doc rule "Append-only log. [...] Never edit or delete prior entries" was indirectly violated by the recovery (the recovery wasn't malicious, but the result is a corrupted audit trail that future planners and digests will read out-of-order). The JOURNAL is also out-of-chronological-order: 14:00 workflow-efficiency-analysis is at line 257 (above 07:00 planner at 261); 15:00 unblock at 274 (above 09:00 worker at 289); 17:00 formalization-pass at 346 (between 10:00 worker at 343 and 11:00 worker at 353).
  - What it would do: human-led pass — read JOURNAL.md, identify the canonical version of each duplicated entry (most likely the most-complete copy), delete the redundant copies, and re-sort the 2026-05-04 entries into chronological order. NEEDS-INPUT.md should also be deduped (drop the second copy of the worker-9am stale-lock entry). Composes naturally with PROPOSED #5 working-tree triage since both files are dirty in the working tree anyway.
  - Risk / blast radius: low. Pure state-file cleanup. Does not touch code. The entries to remove are byte-identical or near-identical; no semantic information is lost. Future digests will be more readable and the dedup-rule scans (planner.md step 5; researcher.md step 2) won't have to reckon with multi-copy text.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-05-05] 2026-05-04 16:45 digest JOURNAL entry truncated mid-word at line 370 ("- Wr")
  - Why: tonight's audit found the 16:45 digest entry exists in JOURNAL.md (line 370+) but its body cuts off mid-second-bullet at the literal string `- Wr` (presumably the start of "- Wrote `digests/2026-05-04.md`..." per the 5-day pattern). This is a third file-write truncation in the same project on the same calendar day (2026-05-04 worker-9am hit it on JOURNAL.md, NEEDS-INPUT.md, QUEUE.md, ollama.py, protocol.py, claude.py, test_smoke.py; 18:00 sabrina-backlog-decomposition hit it on QUEUE.md and STATE.md per its own self-report; tonight's night-auditor hit it on PROPOSED.md mid-write — recovered via heredoc). The chronology is also broken: 18:00 entry is at line 360 (between 12:00 worker at 356 and 16:45 digest at 370), confirming the harness routinely appends in completion-order rather than chronological-order.
  - What it would do: human-led pass — read `digests/2026-05-04.md` (which is the canonical full digest output, untruncated) and reconstruct the missing tail of the JOURNAL entry from it. Likely 3-4 missing bullets (the "Wrote digests/...", "Did not modify..." standard digest body + a "Next handoff" closer). Subordinate to the JOURNAL hygiene proposal above; could fold into the same human-led JOURNAL-cleanup pass.
  - Risk / blast radius: trivial. Documentation only; the digests/2026-05-04.md file holds the full record. No state semantics lost — just JOURNAL legibility.
  - Suggested priority: P2

- [ ] [proposed-by: night-auditor / 2026-05-05] STATE.md "Last updated" header is stale — body references 14:00 / 17:00 / 18:00 events under a 07:00 timestamp
  - Why: STATE.md line 7 says `2026-05-04 07:00 (planner)`. But the body refers in past tense to the out-of-band 14:00 workflow-efficiency-analysis run (line 11 + 17), the 18:00 sabrina-backlog-decomposition (line 27), and implicitly the 17:00 formalization-pass via the ROADMAP.md-decomposed reference. STATE.md was clearly edited after the 07:00 planner run — most likely by the 18:00 sabrina-backlog-decomposition run (which DID journal at line 360 and which self-reports in its own bullet that it Edit-truncated STATE.md and recovered via heredoc append). The "last updated" marker is now misleading: a reader who trusts the header will think they're reading day-6 STATE before any 2026-05-04 unblock activity.
  - What it would do: tomorrow's planner (or a one-line manual fix) refreshes the STATE.md "Last updated" line — either to `2026-05-04 18:00 (sabrina-backlog-decomposition)` matching the most recent edit, or to today's 07:00 once tomorrow's planner runs and rewrites STATE wholesale. The latter is cheaper (planner step naturally rewrites STATE every morning) and fully resolves the staleness on its own.
  - Risk / blast radius: trivial. Single-line state-file fix. Auto-resolves on the next planner run regardless.
  - Suggested priority: P2

- [informational] [proposed-by: night-auditor / 2026-05-06] HEAD advanced to `acd6725` overnight — STATE.md and QUEUE.md are pre-commit-snapshot-stale
  - Why: tonight's audit shows HEAD jumped from `f690336` (4-28) to `acd6725` "Add rebuild research, digests & sabrina-2 updates" (Eric, 2026-05-05 08:43:46 -0600). This breaks the 7-day commit drought and is the first commit since Sabrina automation went live on 2026-04-28. The commit absorbed the (a)-half wire-up (`brain/claude.py +279`, `ollama.py +15`, `protocol.py +50`, `voice_loop.py +33`), worker-8am 2026-05-05's P5.2 diff (`sabrina-2/data/shortcuts.yaml`, `tests/test_shortcuts_yaml.py`, `.gitignore` negation), the personality test suite (`tests/personality/golden_set.yaml`, `judge_prompt.md`, `test_regex_smokes.py`), the pre-commit hook (`.pre-commit-config.yaml`), `ROADMAP.md`, all rebuild research drafts including the 2026-05-05 P5.5 + P2.7 specs and the 2026-05-05 wake-word research, and the day's role + schedule changes (spec-writer + graduation-sweeper). 78 files changed, +16,072 / -1,191. STATE.md line 28 ("No code commits since 2026-04-28 automation scaffold") is now false; QUEUE.md still shows the (a)-half wire-up + P5.2 as `[in-progress]` lock-blocked (false — both are in `main`). Eric's commit landed via a different working-tree / git client than the sandbox sees: the local `.git/index` is still the same 24020-byte file from 2026-04-29 14:14 (still corrupted with `fatal: unknown index entry format 0x74000000` / `0x21000000` on `git status`), and the 0-byte `.git/index.lock` from 2026-04-29 14:15:52 UTC is unchanged — so the lock+index recovery is still pending and the 2026-05-04 worker-9am + 2026-05-05 worker-{8am,9am,10am,11am} NEEDS-INPUT entries remain load-bearing for the four still-uncommitted Worker diffs (P5.4 / P5.5 / P5.1 / worker-12pm queue admin).
  - What it would do: nothing further on its own — informational only. Acts as a pointer for tomorrow's 07:00 planner so it doesn't read the stale STATE.md narrative ("first non-block-state morning since 2026-04-29 ... `[in-progress]` blocked on stale `.git/index.lock`") and miss that the (a)-half + P5.2 are in fact landed. Suggested planner work: (1) wholesale-rewrite STATE.md to reflect HEAD = acd6725, (2) move QUEUE entries for the (a)-half wire-up + P5.2 (and P2.1, already `[done-pending-move]` per worker-12pm 2026-05-05 12:00) to DONE.md, (3) leave P5.4 / P5.5 / P5.1 as `[in-progress]` since the lock+index recovery is still pending and the diffs aren't in `main`, (4) note that the ROADMAP.md + 010 personality spec changes that landed in the commit may invalidate parts of `rebuild/drafts/` references already cited in PROPOSED items.
  - Risk / blast radius: zero. Documentation of a state change already applied in `main`. The audit trail this entry creates is the only thing standing between tomorrow's planner reading a 6-day-stale STATE narrative and noticing the commit landed.
  - Suggested priority: informational (no graduation; the human MAY delete this entry once tomorrow's planner has rewritten STATE.md and reconciled QUEUE).

- [ ] [proposed-by: night-auditor / 2026-05-06] Ruff drift went up overnight — 5 new test files added today are unformatted; `ruff check` errors 81 -> 87 and `ruff format --check` would-reformat 20 -> 25 files
  - Why: tonight's audit shows `python3 -m ruff check sabrina-2/src sabrina-2/tests` -> 87 errors (was 81 baseline since 2026-04-29; +6 today, distributed as I001 `unsorted-imports` 15 -> 19, plus +2 elsewhere likely F401 from new test files). `python3 -m ruff format --check sabrina-2/src sabrina-2/tests` -> 25 files would reformat (was 20 baseline; +5 today matching the five new test files: `tests/test_fixtures_load.py`, `tests/test_shortcuts_yaml.py`, `tests/test_audio_utils.py`, `tests/conftest.py`, `tests/test_utils/{paths,mocks,test_mocks}.py` — the last three count as 1 net new vs the 5-file headline because two of them are not in HEAD@acd6725 yet, only in working tree). All sabrina-2 .py files AST-parse clean (54/54) and `compileall` exits 0 — this is purely cosmetic ruff drift, no Broken-tier issues. Auto-fixable: `ruff check --fix` claims to handle 55 of the 87, and `ruff format` resolves the format-check 25.
  - What it would do: a single Worker shift (S effort, ~10 min) runs `cd sabrina-2&& python3 -m ruff check --fix src tests && python3 -m ruff format src tests`, AST-verifies + tests, commits. Could ship under Partial-DoD on Linux (zero production-code semantics changed, only formatting + import sorting). Most of the touched files won't even need a Windows e2e since they're test files.
  - Risk / blast radius: low. `ruff check --fix` is conservative; `ruff format` is idempotent. The 32 errors flagged as "not fixable" by `--fix` (B023 / SIM105 / B008 / B905 / B904 / E402 / E501 / SIM117) are pre-existing and out of scope; this proposal only cleans up the +6 newly-introduced ones plus the long-standing format drift.
  - Suggested priority: P2 (Improvable, blast radius is the test tree only)

- [ ] [proposed-by: night-auditor / 2026-05-06] State files have NUL-byte tail-padding — `QUEUE.md` (1971 NULs) and `STATE.md` (1175 NULs) past the last newline
  - Why: tonight's audit measured `python3 -c "data.count(b'\x00')"` on every state file: `QUEUE.md` carries 1971 NUL bytes after the final newline (3.7% of total bytes), `STATE.md` carries 1175 (15.6%). All NULs are pure tail-pad — zero NULs appear before the last newline, so textual content is intact. This pattern was first documented by worker-8am 2026-05-05 08:00 (1492 NULs on QUEUE.md, 7035 on NEEDS-INPUT.md, both stripped that run); NEEDS-INPUT.md is now clean (0 NULs), but QUEUE.md re-grew its tail-pad and STATE.md acquired a new one — most likely from a subsequent Worker's Edit-tool write that came in after worker-8am's strip. Failure mode: most readers tolerate tail NULs, but `wc -c` lies, byte-count comparisons against `git show HEAD:QUEUE.md` give false-deltas, and any tool that strict-decodes UTF-8 mid-buffer will trip.
  - What it would do: a 3-line Python pass — `for fn in ('QUEUE.md','STATE.md'): data = open(fn,'rb').read().rstrip(b'\x00'); data = data + b'\n' if not data.endswith(b'\n') else data; open(fn,'wb').write(data)` then `os.fsync`. Reversible (the tail bytes are pure padding; nothing semantic is lost). Could fold into a future Worker shift that's already touching state files, OR into the human-led JOURNAL hygiene pass proposed in #29 (yesterday's night-auditor proposal). Could also be a one-line addition to the standard role-doc post-write check: `data.count(b'\x00') == 0`.
  - Risk / blast radius: trivial. Pure tail-pad strip; no content touched. The Edit-tool's NUL-padding is a sandbox / write-cache artifact, not user content.
  - Suggested priority: P3 (Improvable, mostly a hygiene + future-proofing call)

- [ ] [proposed-by: researcher / 2026-05-06] Sharpen P2.5 QUEUE description — most of the implementation already shipped in `acd6725`; remaining scope is Windows e2e validation + small XML-template polish
  - Why: `research/2026-05-06-autostart-approach.md` (this run) ratifies Task Scheduler as the autostart pick across all four DoD axes. While doing so, it surfaces that the implementation already in `main` at `acd6725` is materially complete: `sabrina-2/src/sabrina/supervisor.py` ships `render_task_scheduler_xml` + `install_task_scheduler_task` + `uninstall_task_scheduler_task` (UTF-16-LE BOM XML + schtasks shell-out), and `sabrina-2/src/sabrina/cli.py:1130-1241` ships `sabrina run` (supervisor entry) + `sabrina autostart {enable,disable,status}` (Task Scheduler + NSSM-service routes). The `[supervisor] mode = "task_scheduler"` default is already in `sabrina.toml`. P2.5's current QUEUE description ("install + uninstall scripts per P2.4's pick; verify `sabrina voice` runs on a fresh Windows login") implies a build-from-scratch shape that doesn't match reality.
  - What it would do: a future planner / spec-writer rewrites the P2.5 entry's Definition-of-done + Notes to: (1) validate `sabrina autostart enable` on Eric's box (creates the `SabrinaAI` task; reboot; `Get-Process python` shows supervisor + voice; `logs/supervisor.log` shows the at-logon launch); (2) validate `sabrina autostart disable` removes it; (3) optionally tighten the XML template per the five follow-ups in the research doc (RestartOnFailure block; Priority audit; UserId substitution check on non-domain-joined Win11; Hidden cosmetic; RunOnlyIfIdle test); (4) decision doc filed. The two `install_autostart.ps1`/`uninstall_autostart.ps1` script artifacts the original DoD names are unnecessary — `sabrina autostart enable/disable` is already the install/uninstall surface; PowerShell wrappers would be one-liners around the existing CLI verbs and add no value.
  - Risk / blast radius: zero — pure QUEUE-description sharpening. The remaining e2e + polish work is unchanged in substance; only the framing changes from "implement" to "validate + tighten." Could fold into the next planner morning rewrite of QUEUE without `[x]`-approval if planner.md split-authority is interpreted to cover description sharpening on already-approved items; otherwise a normal `[x]` graduation.
  - Suggested priority: P2 (low-risk QUEUE legibility fix; no urgency since P2.5 is `[windows-required]` and Eric will hit the description on his next Windows session anyway).

- (promoted to QUEUE 2026-05-15 07:00 by planner; was `[x]` auto-graduated 2026-05-14 by graduation-sweeper) PROPOSED #37 — Sharpen CLAUDE.md "verify post-edit" rule (researcher 2026-05-09). Now lives as the P2 [linux-runnable] [partial-dod-eligible] queue item "CLAUDE.md — sharpen 'verify post-edit' rule to name Read tool as authoritative" in QUEUE.md § Answer-batch follow-ups (2026-05-15).

- (promoted to QUEUE 2026-05-15 07:00 by planner; was `[x]` auto-graduated 2026-05-14 by graduation-sweeper) PROPOSED #38 — Harden roles/worker.md state-file append discipline (researcher 2026-05-09). Now lives as the P2 [linux-runnable] [partial-dod-eligible] queue item "roles/worker.md — deprecate Python `read() + os.replace()` for state-file appends; codify Edit-tool-with-anchor as the safe path" in QUEUE.md § Answer-batch follow-ups (2026-05-15).

- [ ] [proposed-by: night-auditor / 2026-05-12] Investigate the 2026-05-10/2026-05-11 automation-fire gap — 18 expected scheduled runs landed zero JOURNAL entries
  - Why: tonight's audit shows JOURNAL.md's last canonical chain entry is 2026-05-09 16:45 digest. The only entry since is a single human-initiated 2026-05-11 14:00 `[decisions] open-decisions-snapshot-2026-05-05` one-off. Missing per the standing schedule: 2026-05-10 night-auditor / researcher / planner / graduation-sweeper / 5×worker / digest = 9 expected runs, plus 2026-05-11 the same 9 expected runs minus the human one-off = ~9 more. Total ~18 scheduled runs over two days landed zero JOURNAL entries. STATE.md mtime 2026-05-09 (no planner ran); DONE.md mtime 2026-05-06 (no worker landed a commit, internally consistent); QUEUE.md mtime 2026-05-08 (no planner curated, consistent). Cowork sessions ARE reaching the repo (the 2026-05-11 14:00 ad-hoc run proves the file-write channel works), so the failure is most likely (b) tasks fired but bailed before the JOURNAL-append step, or (d) Eric paused the sabrina-* schedules during the OPEN-DECISIONS review window. Without grounding which one, the next two-day cycle risks repeating the gap silently — the JOURNAL is the only signal automation runs leave, so a silent stop is invisible until a human audits the file mtimes.
  - What it would do: a 5-minute Eric check in the Cowork scheduled-tasks UI for: (1) are the nine `sabrina-*` tasks still registered? (2) when did each last fire (or attempt to)? (3) are there error/timeout records for the 2026-05-10/11 slots? If (1) shows missing tasks → re-register from `schedule.json`. If (2) shows recent successful fires with no JOURNAL trace → automation is bailing somewhere between launch and the role-doc step 7 (JOURNAL append) — likely the FUSE-lock-induced bail pattern from worker-8am 2026-05-07 spreading from Workers to all roles. If (3) shows errors → file with the next planner. If Eric intentionally paused the schedules for review → mark `[answered] paused, will resume YYYY-MM-DD` and this entry retires.
  - Risk / blast radius: zero by itself (read-only diagnostic). High-value because the audit cycle is the only feedback loop telling Eric whether automation is alive; a 2-day silent gap is the longest dropout since automation went live 2026-04-28 and represents a 100% miss rate on those two days. Composes with the existing worker-8am 2026-05-07 FUSE lock-bail NEEDS-INPUT entry — if the gap is FUSE-lock spreading to non-Worker roles, clearing `.git/index.lock` may also unblock the JOURNAL channel for them.
  - Suggested priority: P1 (Risky — automation observability blind spot; second-order impact on every other queued item since they all depend on the role chain firing)

- [ ] [proposed-by: researcher / 2026-05-12] Fold tool-loop-cap operator-voice surface into the (b)-half ClaudeBrain wire-up — `_MAX_TOOL_RECURSION` fires silently today; bail line + bus event + structlog warning close the surface
  - Why: `research/2026-05-12-tool-loop-cap-surface.md` (this run) grounds the 04-30 03:30 NEEDS-INPUT menu's option 4 (recursion-cap surface). `brain/claude.py:42` defines `_MAX_TOOL_RECURSION = 5` and lines 250-258 yield `Done(stop_reason="tool_recursion_cap")` when the cap fires. `voice_loop.py:542-543` (the only `Brain.chat` consumer) reads `Done.input_tokens` and `Done.output_tokens` only — `Done.stop_reason` is consumed by zero production code paths today. When the cap fires, the user hears whatever partial text the model's 5th round emitted (often nothing, per the Shape-B self-loop case the existing `test_claude_recursion_cap_yields_done_with_cap_reason` test constructs) and then silence — exactly the "if something's broken, say so" failure decision 010 names. The cap-surface is one of three Done.stop_reasons (alongside `cancelled` and the model's own `max_tokens`/`end_turn`/`stop_sequence`) and the ONLY one today without a spoken/observability surface. The cap-bail surface is the load-bearing operator-facing behavior the first time `[tools] enabled = true` runs against a real workload.
  - What it would do: fold three additions into the (b)-half ClaudeBrain wire-up diff (the `[windows-required]` P1 sitting in QUEUE since 2026-04-29 `research/2026-04-29-claudebrain-tool-wire-up-surface.md` §5/§6): (1) add `ToolLoopCapped(rounds: int, last_tool: str | None)` Pydantic event to `events.py` (~5 lines); (2) extend the existing `elif isinstance(ev, Done):` branch in `voice_loop.py` with an `if ev.stop_reason == "tool_recursion_cap":` sub-branch that speak-queues the literal `"I'm stuck on this. Stopping here."`, appends the same to `reply_parts` so it lands in history, `await bus.publish(ToolLoopCapped(...))`, and `log.warning("tool.recursion_cap", rounds=_MAX_TOOL_RECURSION, last_tool=last_tool)` — voice_loop tracks `last_tool` in chat-stream local scope, set on every `ToolUseStart` (~8 lines); (3) extend `test_claude_recursion_cap_yields_done_with_cap_reason` with a sibling voice-loop test using the existing `FakeBus` + `FakeSpeaker` fixtures from `tests/test_utils/mocks.py` asserting bail line in speak queue + history + bus event + log warning (~30 lines). Total ~45 LOC across 3 files. The bail line `"I'm stuck on this. Stopping here."` is vetted against decision 010 Block 1 ("information before apology") and Block 2 (opener-list, closer-list, one-apology-max, "refuse as character" framing) — six words; no "I'd be happy to", no "Let me know if", no apology, no exposure of the cap value or "tool"/"recursion" implementation vocabulary. Override knob is a module constant `_TOOL_LOOP_CAP_BAIL_LINE` — one-line re-edit if dogfood shows the wording reads wrong. The change is additive to the (b)-half diff already planned in `research/2026-04-29` §5/§6; same Worker slot when Eric pulls the (b)-half on Windows.
  - Risk / blast radius: low. Add-only on `events.py` + `voice_loop.py` + tests; no change to the landed (a)-half code in `brain/claude.py`. The new branch fires only when `stop_reason == "tool_recursion_cap"`, which can only happen when `tools=` is passed AND the model invokes them 5+ times — gated on `[tools] enabled = true` flipping. Today's voice loop (tools disabled, default `[tools] enabled = false`) is unaffected. Composability verified against P4.B1 KillSwitch (already landed in (a)-half `brain/claude.py:67-69, 156-173`): kill-switch path emits `cancelled` not `tool_recursion_cap`, so the bail surface doesn't interfere with `BargeInDetected`. Composability verified against P4.C2 router: cap fires inside `ClaudeBrain.chat` before the router's swap-point, so the next turn re-enters routing fresh.
  - Suggested priority: P2 (Improvable — the (b)-half wire-up is itself P1 `[windows-required]`, but the cap-surface fold-in is a one-slot addition to the same diff, not a separate ship; treat as part of the (b)-half pickup checklist rather than its own queue item).

- [ ] [proposed-by: night-auditor / 2026-05-14] Spec-writer 2026-05-13 06:50's two NEEDS-INPUT appends did not durably land — phantom append recurrence
  - Why: spec-writer 2026-05-13 06:50 JOURNAL entry (lines 1012-1019) reports appending two NEEDS-INPUT entries for P4.A3 ("expression-slot binding" + "placeholder rig commit policy", both stamped `[from: spec-writer / 2026-05-13 06:50]` + both spec-recommend (a)) and explicitly asserts "NEEDS-INPUT count moves from 27 → 29." Tonight's audit grep'd NEEDS-INPUT.md for `P4\.A3|expression-slot|placeholder rig|spec-writer / 2026-05-13` — **zero matches**. Canonical direct-grep `grep -c '^- \[ \]' NEEDS-INPUT.md` = 27 (unchanged from 2026-05-13 02:37 audit), not 29 as the spec-writer entry asserts. The anchor the spec-writer 5/13 entry used ("Worker can ship the (a)-half against (a) without waiting; if you pick (b) or (c) before pull, Worker reroutes (conftest fixture and CI invocation change)." — closing tail of the spec-writer 5/12 P4.A2 headless-test entry) is still unique in the file (count = 1) and still the last entry's terminal line, so the Edit didn't append after it. The spec file itself (`rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md`) IS in the working tree (mtime 2026-05-14 00:38 UTC, 29509 bytes, in tonight's 43-untracked census) — so the Write tool call for the spec landed durably; only the Edit call against NEEDS-INPUT diverged. STATE.md "spec-writer (a)-half NEEDS-INPUT picks now sit at 23 (up from 21)" narrative line (planner 2026-05-13 07:00 overwrite, paragraph 5) and graduation-sweeper / digest / worker-8am / worker-10am downstream entries all inherited the wrong 29 count without re-grepping. This is exactly the FUSE-write-divergence pattern documented in `research/2026-05-09-fuse-divergence-pattern.md` recurring after ~5 days of clean Edit-tool-with-anchor practice, and confirms the H2 finding that Edit-tool reported-success ≠ canonical-NTFS durable-write under the FUSE-lock regime.
  - What it would do: (i) Eric verifies on his Windows-side view that NEEDS-INPUT.md genuinely lacks the two P4.A3 entries (the sandbox view and the canonical NTFS view should now agree since no further writes have raced — but the H2 research doc shows divergence can persist; Eric's read is the canonical tiebreaker). (ii) If absent, the next spec-writer slot (2026-05-14 06:50) re-applies the two appends with a fresh anchor, then re-verifies via canonical-direct-grep before journaling. (iii) STATE.md narrative count auto-corrects on next planner overwrite once true count is re-established. (iv) OPEN-DECISIONS.md S11(a) bulk-ratify scope reverts from "21 spec-writer (a)-half picks" + 5/13's claimed +2 = 23 back to 21 until the appends actually land; if Eric `[x]`'s S11(a) before then, only 21 entries close, not 23.
  - Risk / blast radius: medium. Downstream chain entries (5/13 graduation-sweeper, digest, worker-8am, worker-10am, this run's pre-audit STATE read) all inherited the phantom 29 / 23 counts; the canonical numbers are 27 / 21. Nothing structural breaks — no code edits depend on these counts — but the narrative drift is exactly the legibility-erosion pattern PROPOSED #37/#38 are meant to prevent. More importantly, it proves the FUSE-divergence surface is still live after Edit-tool-with-anchor adoption: the anchor was unique, the role-doc discipline was followed exactly, and the write still didn't land. Suggests either (a) a separate root cause from the worker-9am 2026-05-07 09:00 incident (which involved Python `read() + os.replace()`), (b) Edit-tool-with-anchor has its own divergence window under sandbox-FUSE-lock contention, or (c) something rolled back the change after the Edit returned success. Composability with PROPOSED #37 (CLAUDE.md verify-post-edit via Read) + #38 (worker.md deprecate Python rewrite): both hardenings would fire for this case — a verify-post-Edit-via-canonical-direct-grep step on the spec-writer side would have caught this immediately. Worth tightening the verify rule from "Read tool" to "canonical-direct-grep via bash" since the Read tool itself may sit on the sandbox-side view.
  - Suggested priority: P1 (Risky — silent narrative-drift across multiple downstream entries; first FUSE-divergence recurrence after 5 days of clean discipline; surfaces a verify-post-Edit gap in PROPOSED #37's "Read tool" recommendation that grep-via-bash would close).

- [ ] [proposed-by: night-auditor / 2026-05-14] Undeclared `.dashboard-enrichment.json` at repo root — unaccounted-for ~31 KB structured-data artifact appeared overnight
  - Why: tonight's working-tree census shows 58 entries (15 modified + 43 untracked), +3 vs. night-auditor 2026-05-13 02:37's 55-entry baseline. Two of the +3 untracked map cleanly to today's chain artifacts (`digests/2026-05-13.md` from 5/13 16:45 digest; `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md` from 5/13 06:50 spec-writer). The **third** is `.dashboard-enrichment.json` at the repo root (31141 bytes, mtime 2026-05-14 05:54:40 UTC = 2026-05-13 22:54 PT, owner sandbox uid 1417). Schema-versioned JSON (`schema_version: 1`, `generated_at: 2026-05-13T00:00:00Z`, `items: {...}`) with 23 keys shaped like `"sabrina::night-auditor — GitHub MCP not connected in this Cowork session — remote-PR audit step (role…": { question: "...", options: [...] }` — clearly a NEEDS-INPUT-scraping decoration produced by some external tool for UI rendering, not by any role doc. JOURNAL.md / PROPOSED.md / NEEDS-INPUT.md / QUEUE.md / STATE.md / CLAUDE.md / roles/ all return zero `dashboard` mentions except CLAUDE.md line 102 ("No new dashboards/files without explicit ask") — which is a rule, not a license. No role doc claims authorship; no scheduled-task entry was running at 2026-05-13 22:54 PT per JOURNAL chain reconstruction (graduation-sweeper 07:30 + digest 16:45 are the latest 5/13 entries, both wall-clock-earlier than 22:54). Producer is most likely a cowork-system instrumentation pass not orchestrated through the role chain.
  - What it would do: (i) Eric identifies the producer (Cowork-side dashboard scraper? a hand-run command? a plugin?) — a 30-second check of "what runs on this machine besides the 9 sabrina-* scheduled tasks" would ground it. (ii) Once producer is known, choose: (a) `.gitignore` the path so it stops appearing in `git status` and never accidentally stages; (b) commit it deliberately if Eric wants it tracked; (c) ask the producer to write to `outputs/` or a session-local path outside the project root. (iii) If producer can't be identified, the path is a footgun for accidental `git add .` staging — recommend (a) `.gitignore` as the conservative default. Composes with PROPOSED #5 working-tree triage: this is the first NEW untracked-at-repo-root entry to appear since 2026-04-28 automation went live; the rest of the 43 untracked is steady-state (Worker chain artifacts + research docs + spec drafts + Eric's OPEN-DECISIONS.md), all categorized in #5's body.
  - Risk / blast radius: low. The file is read-only-from-our-side (no role touches it), at repo root, named with a leading dot (so visible to `ls -la` but skipped by some default globs). Worst-case: Eric runs `git add .` from a Windows shell to stage the 10 in-tree Linux-runnable diffs after clearing `.git/index.lock`, and this 31 KB JSON rides along into the commit without intent. The `[Worker self-review staging notes]` in QUEUE entries name explicit file globs (`sabrina-2/src/sabrina/...`), so following the per-entry recipes avoids the footgun, but a careless `git add .` would not. Best-case: producer is Cowork-system and a single-line `.gitignore` edit closes it.
  - Suggested priority: P2 (Improvable — first new repo-root artifact since automation went live, worth one entry so Eric clocks the producer; not blocking anything; not data-loss-shaped).

- [ ] [proposed-by: researcher / 2026-05-14] Reframe PROPOSED #41 — the "phantom append" finding is bash-side FUSE-cache lag, not Edit-tool write divergence
  - Why: `research/2026-05-14-phantom-append-is-bash-fuse-lag.md` grounds the bounded question PROPOSED #41 raised. The spec-writer 2026-05-13 06:50 P4.A3 NEEDS-INPUT appends DID durably land on canonical NTFS — both entries are visible at lines 238 and 247 via Read tool, the Grep tool returns 29 for `^- \[ \]` (matching the spec-writer's claimed 27 → 29), and the spec-writer's anchor is no longer terminal in the canonical view. Bash via `mcp__workspace__bash` reports `wc -l` = 236 / `grep -c '^- \[ \]'` = 27 / `tail -5` ending at the P4.A2 entry / `stat` mtime 2026-05-13 02:47:00 UTC (i.e. bash sees a snapshot ~11h before the spec-writer wall-clock, persisted across the entire 5/13 chain + tonight's 02:37 audit, ~21+ hours of cross-session cross-chain-handoff lag). This is the H2 read-side lag from `research/2026-05-09-fuse-divergence-pattern.md` recurring with a stronger envelope, not a new write-side failure mode. PROPOSED #41's reasoning chain ("the anchor used is still single-occurrence in the file (count = 1) and still the terminal-line of the last NEEDS-INPUT entry — so the Edit didn't append after it") implicitly assumes bash-grep == canonical NTFS; under H2 that assumption fails because bash sees the pre-write snapshot where the anchor IS still terminal. The night-auditor 02:37 explicitly trusted Read tool over bash for JOURNAL.md the same run (H2 echo on `wc -l` 1010 vs. Read line 1063) but used bash-grep as the primary verification path for NEEDS-INPUT.md without applying the same fallback.
  - What it would do: (i) Mark PROPOSED #41 with a body postscript "see `research/2026-05-14-phantom-append-is-bash-fuse-lag.md` — phantom is bash-side, appends are durably present at NEEDS-INPUT.md:238 + :247; do NOT loosen PROPOSED #37's Read-tool-authoritative recommendation"; alternatively `[x]` close #41 with the same postscript citing this PROPOSED entry as the reframe. (ii) Add a one-line note to `roles/night-auditor.md` step 4 ("Findings") — "when a state-file content discrepancy is detected via bash-grep, cross-verify via Read or Grep tool before filing as PROPOSED-Risky; bash-grep can lag tool-channel writes by 21+ hours per `research/2026-05-09-fuse-divergence-pattern.md` H2 + `research/2026-05-14-phantom-append-is-bash-fuse-lag.md`." (iii) Auto-correct STATE.md narrative on next planner overwrite: the canonical counts are 29 NEEDS-INPUT total / 23 spec-writer (a)-half picks consolidated under OPEN-DECISIONS S11(a), NOT the 27 / 21 the night-auditor 02:37 wrote.
  - Risk / blast radius: low-but-load-bearing for narrative integrity. Nothing structural breaks — code edits don't depend on these counts. But the night-auditor 02:37 finding will propagate through today's chain (researcher → planner → graduation-sweeper → workers → digest) unless reframed; downstream entries will inherit "27 / 21" counts that are 2 short of canonical. More importantly, PROPOSED #41's recommended sharpening ("tighten verify rule from Read tool to canonical-direct-grep via bash") is exactly the wrong direction — implementing it would have re-introduced the same lag-blind-spot that caused the false alarm in the first place. Composability: directly extends `research/2026-05-09-fuse-divergence-pattern.md` (new cross-session lag observation); reinforces PROPOSED #37 (verify via Read tool); reinforces PROPOSED #38 (Edit-tool-with-anchor remains the safe path — the spec-writer used it exactly as #38 prescribes, and the write succeeded). Both #37 and #38 hit true day-5 graduation eligibility today per graduation-sweeper 2026-05-13 07:30's calibration; this finding strengthens both rather than complicating either.
  - Suggested priority: P1 (Improvable, doc-only — corrects a Risky-P1 narrative miscall + extends an existing research doc + adds one line to one role doc; net-clarifying, not net-action; should land before tomorrow's night-auditor to prevent further chain inheritance of the wrong counts).

- [ ] [proposed-by: researcher / 2026-05-15] P4.C2 router warn_threshold field-removal rework — align in-tree (a)-half with Eric's B answer to spec Q1
  - Why: P4.C2 (a)-half (worker-11am 2026-05-08, in lock-blocked working tree) shipped the spec-recommended (c) "null-fallback chain" shape: `BrainRouterConfig.warn_threshold_usd: float | None = None` + a resolver in `make_router_from_settings` that reads the router-side field and falls back to `[budget].warn_usd_monthly` when None. Eric's 2026-05-15 dashboard answer batch picked **B** (`**[answered: B 2026-05-15 via dashboard]**` at NEEDS-INPUT.md:140): "Read `[budget].warn_usd_monthly` directly; no `warn_threshold_usd` on `[brain.router]`. Single source of truth." Aligning the in-tree (a)-half before Eric clears `.git/index.lock` lets the lock-clear sweep land a Eric-letter-correct commit rather than committing the (c)-shape and then immediately follow-up-rewrite. Grounded in `research/2026-05-15-dashboard-answer-batch-rework-surface.md`.
  - What it would do: (a) `sabrina-2/src/sabrina/config.py:73-83` — drop `warn_threshold_usd: float | None = None` from `BrainRouterConfig` + drop the docstring bullet at lines 74-76 (~6 LOC). (b) `sabrina-2/src/sabrina/brain/router.py:17-21, 317-322, 359-361` — flip docstring framing ("Q1 (a)" → "Q1 (b)"; "defaults to None and falls back" → "reads `[budget].warn_usd_monthly` directly") + replace the conditional resolver with `threshold = settings.budget.warn_usd_monthly` (-2 LOC code + docstring re-wording). (c) `sabrina-2/sabrina.toml:36-54` — drop the `warn_threshold_usd` mention from the `[brain.router]` block comment + drop the commented-out override line at 52 (-7 lines). (d) `sabrina-2/tests/test_router.py:365-398` — delete `test_make_router_from_settings_threshold_from_router_cfg` (~17 LOC; the override knob no longer exists); simplify `test_make_router_from_settings_threshold_falls_back_to_budget` to drop the warn_threshold_usd-None setup (~5 LOC trimmed) and flip docstring "Q1 (a)" → "Q1 (b)". Direct-construction tests at lines 160-241 (`Router(... warn_threshold_usd=10.0)`) stay unchanged — the `Router` class constructor keeps the parameter for testability. Worker shift, partial-DoD-eligible; lands on same `automation/worker-2026-05-07-8am` branch as the rest of the lock-blocked pile; commits in the same lock-clear sweep.
  - Risk / blast radius: low. ~25 LOC code + ~15 LOC docstring + 1 test deleted + 1 test simplified, all in `sabrina-2/`. Linux Partial-DoD gates: `python -m compileall sabrina-2/src` clean post-edit; `pytest sabrina-2/tests/test_router.py` passes the simplified test + the 4 direct-construction tests (`test_select_cost_aware_*`). No production-runtime behavior change for fresh installs (today's `[brain.router] policy = "claude_default"` doesn't traverse the threshold resolver; only `cost_aware` did, and the resolver result is identical — `settings.budget.warn_usd_monthly` either way — for installs that never set the router-side override). No off-limits files touched. Composes cleanly with the 11 other lock-blocked diffs (zero file overlap with P4.B1 / P4.C1 / P4.A1 / P5.* / P2.7 / P2.2). Spec doc at `rebuild/drafts/research/2026-05-08-p4c2-router-routing-policy-spec.md` becomes slightly stale (still recommends (c)) — draft research docs going stale post-decision is normal, no edit needed.
  - Suggested priority: P1 (Improvable, ~40 LOC + tests — should land before the lock-clear sweep commits the (c)-shape; tomorrow's planner can queue as a fresh `[from: researcher / 2026-05-15]` follow-up to the P4.C2 (a)-half QUEUE entry, or fold into the lock-clear sweep planner notes).

- [ ] [proposed-by: researcher / 2026-05-15] P4.B1 kill-switch hotkey docstring update — flip spec Q1 (a) → (b) framing to match Eric's B answer
  - Why: P4.B1 (a)-half (worker-9am 2026-05-08, in lock-blocked working tree) already plumbed the kill-switch hotkey field through both `AutomationConfig.kill_switch_hotkey` (config.py:293) and the `KillSwitch.__init__(hotkey=...)` constructor (kill_switch.py:132), and the toml block at sabrina.toml:248 declares `kill_switch_hotkey = "<ctrl>+<alt>+k"` — so the (a)-half ships forward-compatible with either Eric's A or B answer. Eric's 2026-05-15 dashboard answer batch picked **B** (`**[answered: B 2026-05-15 via dashboard]**` at NEEDS-INPUT.md:113): "Same default as A but the escape hatch is ~10 lines and matches the existing TOML knob pattern used by `[budget]` and `[brain.router]`." The (a)-half code shape matches B exactly. Only the docstring framing still says "spec Q1 (a)" / "hard-coded for v1." Grounded in `research/2026-05-15-dashboard-answer-batch-rework-surface.md`.
  - What it would do: `sabrina-2/src/sabrina/automation/kill_switch.py:18` — flip "Hotkey binding is hard-coded `<ctrl>+<alt>+k` for v1 (spec Q1 (a))" to "Hotkey binding defaults to `<ctrl>+<alt>+k` and can be overridden via `[automation] kill_switch_hotkey` (spec Q1 (b))." Lines 39-42 — flip the "Default hotkey (spec Q1 (a)). The spec recommends keeping this hard-coded for v1..." comment to "Default hotkey (spec Q1 (b)). v1 default; `AutomationConfig.kill_switch_hotkey` overrides via toml..." ~6-10 LOC of docstring/comment edits. No code-shape change. No test changes (existing tests assert behavior, not spec letter). The (b)-half wiring (voice_loop reads `settings.automation.kill_switch_hotkey` and passes to constructor) was always (b)-half scope, unaffected.
  - Risk / blast radius: trivial. Docstring-only edits in one file. Linux Partial-DoD gates: `compileall` + AST + ruff all green by construction (docstring edits don't change ruff metrics). Could be batched with PROPOSED #44 (P4.C2 rework) above to amortize a Worker slot's overhead — both edits land in the same lock-blocked pile on the same `automation/worker-2026-05-07-8am` branch.
  - Suggested priority: P2 (Improvable, doc-only — net-clarifying for future readers, no behavior change; if the lock-clear sweep happens before this lands, Eric can land it via a `--amend` on the P4.B1 (a)-half commit or skip it entirely without consequence).

- [ ] [proposed-by: night-auditor / 2026-05-16] Dashboard answer-resolution JSONL key-prefix mismatch — `roles/night-auditor.md` step 6 specifies `<project>` = `sabrina-local-ai` but the dashboard producer emits keys with `sabrina::` prefix
  - Why: tonight's audit ran step 6 end-to-end for the first time (5 `[resolved:]` markers from today's chain at NEEDS-INPUT.md lines 25 + 34 + 46 + 56 + 66 — worker-8am GitHub MCP (B) + worker-9am researcher menu (A) + worker-10am P2.7 cost-table (A) + worker-11am P4.C1 per-rule knobs (A) + worker-12pm P4.C1 apology-dedup (A)). All 5 verified via the diff-side disjunct of step 6's gate (`git log --since="24 hours ago"` returns 0 commits — lock at ~208h still gates — but the answered option's implied work IS in the diff for all 5, either as the in-tree (a)-half code that ratifies Eric's A answer or as worker-8am's 3-file role-doc edits). JSONL row appended to `logs/sabrina/2026-05-16.jsonl` (5 rows). The discrepancy: role-doc step 6 names `<project>` = `sabrina-local-ai`, but `.dashboard-enrichment.json` (the producer's question-metadata output, untracked at repo root per PROPOSED #42) keys every question with a `sabrina::<from-tag-prefix> — <truncated-title>` prefix. If the producer's `filter_open()` reads the JSONL using its own `sabrina::` key format to match against open NEEDS-INPUT items, a `sabrina-local-ai::` row will not drop anything — the resolved markers fail to retire from the dashboard. Tonight's JSONL chose to match the producer's `sabrina::` prefix (verifiable via byte-for-byte key reuse from `.dashboard-enrichment.json`), deviating from the role-doc spec for the goal step 6 names ("the producer's `filter_open()` reads this file to drop the item from the next dashboard scan"). Either the role doc or the producer is wrong; either way this needs reconciliation before the next night-auditor run files JSONL.
  - What it would do: pick one, edit the loser. (a) Flip `roles/night-auditor.md` step 6 `<project>` from `sabrina-local-ai` to `sabrina` to match the producer's actual emitted key format. ~3 LOC role-doc edit; partial-DoD eligible; lands on `automation/worker-2026-05-07-8am`. (b) Edit the dashboard producer (location unknown — `.dashboard-enrichment.json` is the output, not the producer source) to emit `sabrina-local-ai::` prefixes. Heavier; needs the producer found first. Tonight's JSONL is forward-compatible with (b) only if a one-shot rewrite of the existing 5 rows lands at the same time.
  - Risk / blast radius: zero by itself (proposal, not a change). If unreconciled, the dashboard producer's filter_open() either silently drops nothing (the bad case) or actually does match on title-only without the project prefix (the lucky case). Tonight's JSONL is the first end-to-end test of the wiring — by tomorrow's audit either the dashboard shows the 5 items retired (lucky case → role-doc has a minor inconsistency; pick (a) for cleanup) or the dashboard still shows them open (bad case → JSONL key format isn't what filter_open expects; either fix the role doc to match the producer, or rewrite tonight's 5 rows). Either path resolves to (a) for the smaller fix.
  - Suggested priority: P2 (Risky / doc-vs-code mismatch; doc-only fix; ~3 LOC on `roles/night-auditor.md` step 6; defer until Eric or a planner has visibility into whether tonight's 5 rows actually dropped the items from the dashboard scan).

- [ ] [proposed-by: night-auditor / 2026-05-16] Universal ~60 min early-fire skew across all 36+ scheduled tasks — likely DST-transition artifact in cron/schedule interpretation
  - Why: tonight's audit invoked `mcp__scheduled-tasks__list_scheduled_tasks` to root-cause the digest-flagged worker-12pm absence and the 5/16 22:49 + 22:57 PDT late/duplicate fires (per the digest's "tomorrow's night-auditor should observe TWO post-digest late/duplicate fires today as a possible secondary signature of the same wall-clock-skew / queueing-lag mechanism flagged across the 5/10-11 silent gap"). Discovered every Sabrina + Battle Bot + Process-Tools task — 36+ in total — fires roughly 60 minutes EARLIER than its `"At HH:MM"` schedule field. Concrete data: `sabrina-night-auditor` schedule = "At 12:05 AM" but lastRunAt = `2026-05-17T06:06:03Z` = **2026-05-16 23:06 PDT** (this very run, ~59 min early of nominal 00:05 PDT 5/17); `sabrina-worker-12pm` schedule = "At 12:02 PM" but lastRunAt = `2026-05-16T18:01:38Z` = **2026-05-16 11:01 PDT** (~61 min early of nominal 12:02 PDT); `sabrina-worker-9am` "At 09:08 AM" → 08:09 PDT (~59 min early); `sabrina-worker-11am` "At 11:08 AM" → 10:07 PDT (~61 min early); `sabrina-digest` "At 04:50 PM" → 15:49 PDT (~61 min early). Pattern is universal and consistent at ~60 min, not jitter-bounded — `jitterSeconds` values range 70–577 (~12 min max), not enough to explain a 60-min offset. The skew matches a US PST↔PDT DST offset (UTC−8 vs UTC−7); the most plausible explanation is that schedule strings are interpreted in a stored TZ (likely PST = UTC−8) while the wall-clock is currently PDT (UTC−7), so a scheduler that thinks "12:05 AM PST" really means "12:05 AM UTC−8" fires at "23:05 PDT" wall-clock. Consequence: every JOURNAL entry self-tagged with a slot label like `## [2026-05-16 12:00] worker-12pm` has been mislabeled by ~1h since the DST transition (US DST began 2026-03-08 — eight days BEFORE the 5/10-11 silent gap of PROPOSED #39, lining up suggestively). This run is self-tagging its JOURNAL header with the **actual** fire time `## [2026-05-16 23:11] night-auditor (early-fire of nominal 5/17 ~02:37 slot)` to surface the skew rather than perpetuate the mislabel.
  - What it would do: a 10-minute Eric check in the Cowork scheduled-tasks UI / settings for: (1) which timezone the scheduler interprets cron expressions in (UI string vs cron string vs system clock); (2) whether `cronExpression: "0 0 * * *"` is being read as 0h UTC, 0h PST, or 0h system-local; (3) whether task creation 2026-04-28 (Sabrina automation went live) was done before or after the 2026-03-08 DST transition (almost certainly before since 4/28 is post-DST, so tasks created in PDT should keep firing in PDT — unless the scheduler stores in PST and converts on display). If the fix is structural (scheduler bug) → file with Cowork team. If the fix is config (tasks need re-creation post-DST) → re-register all 36+ tasks from `schedule.json` with explicit TZ. If the fix is acceptance (60-min skew is OK because role agents self-correct to wall-clock) → codify in CLAUDE.md that JOURNAL slot labels mean "the slot the task was scheduled for, not the wall-clock fire time."
  - Risk / blast radius: zero by itself (proposal + observation). HIGH-VALUE because the skew silently corrupts every downstream slot-label-based audit (the 5/16 digest's "worker-12pm did not run" finding may be re-interpretable: worker-12pm DID fire per scheduler at 11:01 PDT, just an hour before its nominal slot, and produced no JOURNAL output for unrelated reasons — see companion PROPOSED #48). The skew is the most likely common-cause for: (a) 5/16 22:49 + 22:57 PDT "late/duplicate" worker-9am + worker-11am fires (could be a 2nd-fire mechanism the scheduler kicks off after a missed nominal slot); (b) the 5/10-11 silent gap of PROPOSED #39 (could be the skew interacting with a DST-edge case that left tasks unscheduled for two days). All cross-project (Battle Bot, Process-Tools) automation also affected, not just Sabrina.
  - Suggested priority: P1 (Risky — automation observability and slot-label-integrity blind spot; 36+ tasks affected; common-cause candidate for at least two prior PROPOSED entries (#39 + #48); cheap to investigate (10 min in Cowork scheduled-tasks UI), expensive to keep ignoring because every audit's slot-label assumptions are now suspect).

- [ ] [proposed-by: night-auditor / 2026-05-16] `sabrina-worker-12pm` fired 11:01 PDT 5/16 per scheduler but produced NO JOURNAL entry — third signature for PROPOSED #39's hypothesis set
  - Why: per digest 2026-05-16 16:45 "tomorrow's night-auditor 02:37 should observe ... a possible secondary signature of the same wall-clock-skew / queueing-lag mechanism flagged across the 5/10-11 silent gap and today's worker-12pm absence — same root-cause family worth folding into PROPOSED #39's surviving hypothesis-set if a third late-fire signature shows up in tomorrow's chain." Tonight's `mcp__scheduled-tasks__list_scheduled_tasks` audit shows: `sabrina-worker-12pm` lastRunAt = `2026-05-16T18:01:38.421Z` = **2026-05-16 11:01:38 PDT** (after the universal ~60-min DST skew, this is the on-time fire for the 12pm-ish slot). Per scheduler: the task fired successfully — it's enabled, dispatched, the MCP recorded the fire. Per JOURNAL: `Grep "^## \[2026-05-16 1[12]"` returns hits only for 10:00 + 11:00 + 16:45 — no 12:00 entry, no 11:01 entry, no 12:01 entry, no entry from a 12pm-shaped Worker at all on 5/16. So the failure mode is: scheduler fires → agent process starts → ??? → no JOURNAL write. This is the THIRD silent-gap signature this month, joining (1) the 5/10-11 18-run silent gap of PROPOSED #39 and (2) the 5/16 worker-12pm absence the digest flagged. The companion PROPOSED #47 (universal 60-min early-fire skew) does NOT explain this one — the scheduler shows worker-12pm fired; the failure is downstream of fire.
  - What it would do: an Eric check in the Cowork session-transcripts log (or equivalent run-log) for the `sabrina-worker-12pm` run that started ~2026-05-16T18:01:38Z. Three hypotheses to distinguish: (h1) **silent mid-run bail** — agent started the role prompt, read state files, hit some failure (FUSE lock contention deeper than the existing carve-out path, network policy block on a tool, prompt parse error, agent timeout) and exited before reaching role-doc step 8 (JOURNAL append). Evidence to look for: partial work in the session (read tool calls but no write tool calls). (h2) **agent ran to completion but JOURNAL write was lost** — agent reached step 8, made the Edit tool call, the write seemed to succeed (per Edit tool return), but the canonical NTFS file never received the write (FUSE/cache pathology, mirror of worker-9am 2026-05-07 09:00's DATA LOSS but for a Worker's own appends). Evidence to look for: session log shows successful Edit tool call to JOURNAL.md whose target line range is not present in the current canonical file. (h3) **agent process never actually started past prompt-receive** — the scheduler's lastRunAt updated on dispatch, but the agent crashed/timed-out before reading any state. Evidence to look for: no tool calls in the session log at all, only the initial prompt. The fix path depends on which: (h1) → audit role docs for silent-bail surfaces; (h2) → escalate the FUSE/cache pathology Eric is already aware of from worker-9am 2026-05-07's incident; (h3) → audit agent startup path (this would also explain the 5/10-11 silent gap if it recurred for 18 runs in a row).
  - Risk / blast radius: zero by itself (read-only diagnostic). HIGH-VALUE because the failure mode is invisible to JOURNAL-mtime audits and to the existing PROPOSED #39 framing (which assumed "tasks fired but bailed before JOURNAL append" — h1 above — without the scheduler-confirmed-fire data point). Companion-finding to PROPOSED #47 (60-min skew) but a distinct root cause: #47 affects WHEN tasks fire; this affects WHETHER fired tasks produce output. Composes with PROPOSED #39 as the third signature folded into its hypothesis-set per the digest's standing instruction. The 5/16 22:49 + 22:57 PDT late/duplicate worker-9am + worker-11am fires in JOURNAL lines 1418-1422 are a possible related signature — they exist in JOURNAL but are NOT in scheduler's lastRunAt (scheduler shows on-time-ish 08:09 + 10:07 PDT fires only). So either the scheduler tracks only the latest fire or some other mechanism (retry, manual trigger) produced those late entries.
  - Suggested priority: P1 (Risky — automation completeness; same priority as PROPOSED #39 which this folds into; cheap to investigate (one session-log query), expensive to keep ignoring because each missed Worker slot = lost dashboard answer-resolution pairing capacity at the per-marker cadence).


