# Needs Input — Sabrina-Local-AI

> Questions and blockers that need human answers before automation can proceed. Cleared during the evening review window.

## How to respond

- Add your answer inline under the question.
- Mark `[answered]` once resolved. The next Planner run will clear answered items and act on them.
- If a question turned into actual work, the Planner will move it to QUEUE.md.

## Format

```
- [ ] [from: role / YYYY-MM-DD HH:MM] Question
  - Context: ...
  - What I tried: ...
  - What I need from you: a decision / clarification / approval / new information
```

---

- [ ] [from: night-auditor / 2026-04-30 02:37] GitHub MCP not connected in this Cowork session — remote-PR audit step (role step 3) skipped this run.
  - Context: `roles/night-auditor.md` step 3 says to list open PRs, fetch CI/check status, and verify the latest Worker push landed on `automation/<role>-<YYYY-MM-DD>-<slot>` on the remote. None of the `mcp__plugin_engineering_github__*` tools are available in this run's deferred-tools list. Per the role doc, I'm skipping cleanly and continuing the rest of the audit. (Worth noting: there was nothing to verify tonight either — the 8am Worker bailed without committing or pushing; 9–12pm idled. So a connected GitHub MCP would have produced an empty Worker-push verification anyway.) If you want this gate active, install/connect a GitHub MCP for scheduled-task sessions; otherwise remove the step from the role doc.
  **[answered: B 2026-05-15 via dashboard]** — DoD already lives in local commits per CLAUDE.md — installing an MCP to verify pushes the project explicitly doesn't gate on is ceremony.
  **[resolved: 2026-05-15 by worker-8am]** — Collapsed `roles/night-auditor.md` step 3 (GitHub remote audit) + `roles/planner.md` step 6 (GitHub issue ingestion) + `roles/worker.md` step 7 (GitHub MCP PR creation) to one-line "removed 2026-05-15" citations; doc edits in tree on `automation/worker-2026-05-07-8am`, commit lock-blocked by the same 8d-stale `.git/index.lock` gating the other 12 lock-blocked diffs (partial-dod, awaits-windows-e2e).

- [ ] [from: researcher / 2026-04-30 03:30] What bounded question should the Researcher investigate next? — no `[research]`-tagged items queued, the natural P1 surface is already grounded.
  - Context: per `roles/researcher.md` step 1, I look for `[research]`-tagged items or P0/P1 questions that need grounding. Today's QUEUE has one item (ClaudeBrain wire-up, `[in-progress]`); yesterday's researcher already produced `research/2026-04-29-claudebrain-tool-wire-up-surface.md` pinning the diff surface. NEEDS-INPUT has three open items (GitHub MCP, worker-8am DoD-gate fork, pytest-in-sandbox) — all are decision asks for you, not research questions. The night-auditor 02:37 today noted "a second pass on the same surface is unlikely to add value unless they pivot to a different bounded question." Per role step 2, since no prior `[from: researcher / ...]` "what to investigate next" entry exists in NEEDS-INPUT, I'm writing this one and stopping rather than inventing a question.
  - What I tried: read CLAUDE.md, STATE.md, JOURNAL.md (today's night-auditor 02:37 + yesterday's full cycle), QUEUE.md, NEEDS-INPUT.md, PROPOSED.md (12 items, all unchecked), `roles/researcher.md`, glob of `research/` (1 file from yesterday). Confirmed there's no `[research]`-tagged item, no P0/P1 question needing grounding, and no prior researcher "what to investigate next" entry to dedupe against.
  - **2026-05-05 07:00 planner: de-facto resolved.** P2.1 (wake-word training pipeline research) reached QUEUE via the 2026-05-04 backlog decomposition and was picked up by researcher 2026-05-05 03:30, who shipped `research/2026-05-05-wake-word-training-pipeline.md`. This entry was never marked `[answered]` by Eric, so leaving it in place per role-doc; it is no longer load-bearing.
  - **2026-05-07 07:00 planner: de-facto resolved a third time.** P2.4 (autostart approach research) was picked from this menu's first option ("Live2D avatar layer architecture" — deferred to P4.A1) and ratified 2026-05-06 03:30 via `research/2026-05-06-autostart-approach.md`. Researcher 2026-05-07 03:30 dedup-skipped against this entry, the third such skip. Still leaving in place per role-doc letter; Eric can mark `[answered]` whenever convenient.
  - What I need from you: nothing further — entry de-facto resolved per the three planner notes above; original candidate menu (5 items) collapsed 2026-05-15 09:00 per dashboard answer A. Picked + shipped from menu: "Hey Sabrina" wake-word (5/5), Live2D avatar (5/14 P4.A1); covered elsewhere: personality-projection (P4.C1 spec 5/7), recursion-cap (tool-use plan §4).
  **[answered: A 2026-05-15 via dashboard]** — Three planner notes already say it's not load-bearing; A is a single-line edit and frees up dashboard real estate.
  **[resolved: 2026-05-15 by worker-9am]** — Collapsed the 7-line "What I need from you" + Original-ask candidate menu (5 sub-bullets) to a single line per dashboard answer A; preserved parent header + Context + What-I-tried + the two prior planner de-facto-resolved notes + the [answered:] marker. Doc-only state-file edit on NEEDS-INPUT.md; no code touched; no commit needed (state-file working-state, not committed per Worker convention).

- [ ] [from: spec-writer / 2026-05-05 06:50] P2.7 budget tracker — where should the per-token cost-table for Sonnet 4.6 / Haiku 4.5 / Opus 4.6 live?
  - Context: I'm the 06:50 Spec Writer. Drafted `rebuild/drafts/research/2026-05-05-p27-budget-tracker-spec.md` for queue item P2.7. The (a)-half ships a `compute_cost(input_tokens, output_tokens, model: str) -> float` pure function in `sabrina-2/src/sabrina/budget.py`; that function needs the per-million-token rates for input/output for each Anthropic model the brain currently dispatches against. Three plausible homes for those rates with different trade-offs; spec recommends one but the call is yours.
  - What I tried: read the predecessor `rebuild/drafts/budget-and-caching-plan.md` (2026-04-23) — it embeds the rates as constants in `sabrina/budget.py` directly. Read `rebuild/decisions/001-hardware-and-budget.md` indirectly via STATE/CLAUDE.md references — sets the $0/$10/$100 thresholds but leaves the per-token rates implicit.
  - **2026-05-07 07:00 planner note: worker-8am 2026-05-06 shipped P2.7 (a)-half against spec-recommended (a) inline-constants per the spec's escape hatch; this NEEDS-INPUT entry remains relevant only if Eric wants to override (a) before the lock-resolved commit lands. If (a) stands, mark `[answered]` and the entry retires.**
  - What I need from you: pick one (or override).
    - (a) Inline constants in `sabrina-2/src/sabrina/budget.py`. Cheapest, no I/O on the voice-loop hot path, easy to read in code review. Cost: a per-token-rate change requires a code edit + commit. Spec recommends this — Anthropic pricing changes are infrequent and a rate edit landing alongside a decision-doc note is the cleanest audit trail.
    - (b) `[budget.cost_table]` block in `sabrina.toml`. User-tunable; survives Anthropic price changes without a code edit. Cost: a $0.01 typo costs Eric real money, and the values aren't sensitive enough to justify settings-file ceremony.
    - (c) `data/anthropic-prices.yaml` alongside the `data/shortcuts.yaml` P5.2 ships. Consistent-with-the-data-port pattern. Cost: an extra YAML-load on every cost computation, plus a second source of truth (the YAML file vs. Anthropic's actual pricing page) to keep in sync.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting for an answer; if you pick (b) or (c) before the spec is pulled, the Worker reroutes per the answer here.
  **[answered: A 2026-05-15 via dashboard]** — Worker-8am 2026-05-06 already shipped P2.7 (a)-half against (a) per planner note; ratifying keeps the lock-resolved commit on its current path.
  **[resolved: 2026-05-15 by worker-10am]** — Ratified the inline `_COST_TABLE` constants at `sabrina-2/src/sabrina/budget.py:62-76` (Sonnet 4.6 / Haiku 4.5 / Opus 4.6 per-million input/output rates) per dashboard answer A; the (a)-half shipped by worker-8am 2026-05-06 already targets this shape and sits in the lock-blocked pile on `automation/worker-2026-05-07-8am` awaiting Eric's `rm -f .git/index.lock` sweep (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-07 06:50] P4.C1 router persona-projection — `[brain.persona]` config block: ship per-rule knobs now, master-switch only, or hard-coded constants for v1?
  - Context: Spec at `rebuild/drafts/research/2026-05-07-p4c1-persona-projection-layer-spec.md`. The (a)-half ships five projection rules in `project_ollama_text` (closing-offer strip, list-vomit dehydration, apology dedup, mode-bleed accept-as-is, sentence-count truncation). Question is how exposable they should be in config so a misfire on Eric's actual usage pattern can be disabled without a code edit + commit.
  - What I tried: read `rebuild/drafts/research/2026-04-26-ollama-parity.md` § 1 (the audit) + § 4 (projection-layer recommendation) to identify which rule is most likely to misfire. The audit calls out 1.12 (sentence-count truncation) as "aggressive but effective" — that's the rule with the highest false-positive risk on long technical answers Eric actually wanted at length. Read decision 008's "schema-versioned config" posture + the existing `[memory.semantic.embedder]` precedent of per-feature config knobs to ground the (a) recommendation. Read `sabrina-2/src/sabrina/config.py` to confirm the pattern for adding a new `BrainPersonaConfig` Pydantic model would mirror the existing `BudgetConfig` shape.
  - What I need from you: pick one (or override).
    - (a) Per-rule knobs in `[brain.persona]`. `ollama_strip_closing_offer = true`, `ollama_dehydrate_lists = true`, `ollama_dedup_apologies = true`, `ollama_truncate_long_replies = true`. Cost: ~30 tokens of TOML + four config-load test cases. Spec recommends this — granular shape matches the per-feature precedent and the most-likely-misfire rule (truncation) is independently disable-able. Worker can ship the (a)-half against (a) without waiting; if you override before pull, Worker reroutes per the answer.
    - (b) Single `ollama_projection_enabled = true` master switch. All-or-nothing. Cost: smaller initial surface; if one rule misfires, the entire projection layer goes off as a workaround.
    - (c) Hard-coded constants for v1. No config knob at all; all five rules always-on. Cost: a misfire needs a code edit + commit to disable.
  **[answered: A 2026-05-15 via dashboard]** — The truncation rule has the highest false-positive risk per the parity audit; independent disable means one misfire doesn't gut the layer.
  **[resolved: 2026-05-15 by worker-11am]** — Ratified the four per-rule knobs on `BrainPersonaConfig` (`sabrina-2/src/sabrina/config.py:48-60`) + `[brain.persona]` block (`sabrina-2/sabrina.toml:25-34`) + router unpack (`sabrina-2/src/sabrina/brain/router.py:353-356`) per dashboard answer A; the (a)-half shipped by worker-10am 2026-05-08 already targets this shape and sits in the lock-blocked pile on `automation/worker-2026-05-07-8am` awaiting Eric's `rm -f .git/index.lock` sweep (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-07 06:50] P4.C1 router persona-projection — apology-dedup heuristic: keep first apology, last apology, or drop all on stack?
  - Context: Spec at `rebuild/drafts/research/2026-05-07-p4c1-persona-projection-layer-spec.md` projection rule 3 (audit § 1.6: qwen3:14b stacks apologies on retry ~1 in 4 corrected turns). When the projection layer detects more than one apology phrase in a turn, the heuristic has to pick which one to preserve.
  - What I tried: re-read `2026-04-26-ollama-parity.md` § 1.6 — the audit's example uses first-apology preservation ("I'm sorry I got that wrong" kept; "My apologies for the confusion. Let me try again — I apologize if my last answer wasn't clear." dropped). Read decision 010 — operator voice is "Information before apology. If something's broken, say so." — neither alternative is explicitly precluded. TTS-spoken context: by the time the second apology renders audibly, the first has already been spoken; preserving the first matches what the user has already heard.
  - What I need from you: pick one (or override).
    - (a) Keep first apology. Matches the audit example; aligns with TTS-spoken precedence. Spec recommends this. Worker can ship the (a)-half against (a) without waiting; if you override before pull, Worker reroutes — the heuristic is a single branch in `project_ollama_text`.
    - (b) Keep last apology. Reads more "current" but loses turn-opening apology context.
    - (c) Drop all apologies if there are >1. Aggressive — the user's prompt may have actually warranted one. Risks reading as evasive.
  **[answered: A 2026-05-15 via dashboard]** — First-apology preservation is the audit-example shape and is a single-branch heuristic in `project_ollama_text`.
  **[resolved: 2026-05-15 by worker-12pm]** — Ratified the first-apology-keep branch in `_dedup_apologies` at `sabrina-2/src/sabrina/brain/persona.py:307-342` (sentence-granular: first apology kept verbatim per docstring line 311; subsequent apologies surgically stripped via `_strip_leading_apology` per lines 332-339; `seen_apology` flag at line 325 enforces "first" semantics) per dashboard answer A; existing test `test_project_ollama_text_dedups_apology_stack` at `sabrina-2/tests/test_persona_projection.py:117-124` already asserts the spec § Q2(a) "keep first" shape ("I'm sorry. My apologies. Sorry again, here goes." → "I'm sorry. Here goes."); the (a)-half shipped by worker-10am 2026-05-08 sits in the lock-blocked pile on `automation/worker-2026-05-07-8am` awaiting Eric's `rm -f .git/index.lock` sweep (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-07 06:50] P2.6 supervisor validation — Step 5 XML-template tightening: ship `RestartOnFailure` opt-in, all six follow-ups, or skip entirely?
  - Context: Spec at `rebuild/drafts/research/2026-05-07-p26-supervisor-validation-spec.md`. The supervisor implementation already shipped in `acd6725` (DONE.md 2026-05-05). Researcher 2026-05-06 03:30 surfaced six XML-template tightening follow-ups in `research/2026-05-06-autostart-approach.md`: (1) `RestartOnFailure` block (currently absent); (2) `Priority` audit (current value `7` = below-normal); (3) `UserId` substitution check on non-domain-joined Win11; (4) `Hidden` cosmetic; (5) `RunOnlyIfIdle` paranoia test; (6) task-name uniqueness knob. Question is which to apply alongside the validation pass.
  - What I tried: read `sabrina-2/src/sabrina/supervisor.py:160-211` (the `_TASK_XML_TEMPLATE` + `render_task_scheduler_xml`) to confirm what's currently in the template and which follow-ups are 4-line additions vs. structural changes. Confirmed: `RestartOnFailure` is the only follow-up that prevents a recovery-failure class (supervisor itself crashing) that the current code doesn't cover; the other five are cosmetic, paranoia, or future-flexibility. Read `research/2026-05-06-autostart-approach.md` to confirm researcher's prioritization (none flagged as urgent).
  - What I need from you: pick one (or override).
    - (a) Apply only `RestartOnFailure` (follow-up #1). 4-line XML addition + one unit test. Cost: small. Benefit: defense-in-depth — without it, if `run_supervised` itself raises, there's no recovery (Task Scheduler can paper over it). Spec recommends this. Worker / Eric can ship against (a) without waiting; if you override, the diff is a 4-line revert.
    - (b) Apply all six follow-ups. ~200 LOC of XML/test/config changes. Bundles cosmetic with structural in one decision doc. Closes the entire researcher follow-up list.
    - (c) Skip all six; ship the validation alone. Decision doc covers validation results only; follow-ups become a P2.6.1 item if/when a defect class surfaces.
  **[answered: A 2026-05-15 via dashboard]** — Closes the only defense-in-depth gap (#1) with a 4-line addition; cosmetic items can be a separate cherry-pick later.
  **[resolved: 2026-05-16 by worker-8am]** — Added `<RestartOnFailure><Interval>PT5M</Interval><Count>3</Count></RestartOnFailure>` block to `_TASK_XML_TEMPLATE` in `sabrina-2/src/sabrina/supervisor.py:184-187` (4-line addition between `<Priority>7</Priority>` and `</Settings>`) per follow-up #1 / spec recommendation; authored unit test `test_task_scheduler_xml_carries_restart_on_failure_block` in `sabrina-2/tests/test_smoke.py:1545-1565` asserting all four marker strings (`<RestartOnFailure>`, `<Interval>PT5M</Interval>`, `<Count>3</Count>`, `</RestartOnFailure>`) appear in rendered XML. Linux Partial-DoD gates GREEN: `compileall -q sabrina-2/src sabrina-2/tests` exit 0; Read-tool spot-check on both files canonically intact; `pytest -k task_scheduler` PASSES the three pre-existing tests with zero regression (the new test is canonically in-tree per Read but blocked from bash-side pytest collection by the ≥48h FUSE cache lag envelope — same pattern PROPOSED #37/#41/#43/#46 cover; the test will collect on the next fresh session). Commit blocked by the same 9d-stale `.git/index.lock` (May 7 14:24:11 UTC, 0 bytes, owner sandbox uid) gating the 13 prior diffs on `automation/worker-2026-05-07-8am`. Lock mtime predates this run so the proposed P1 forensic carve-out wouldn't apply even if shipped. Existing NEEDS-INPUT entry at line 91 covers this lock thread (already answered C 2026-05-15); not writing a duplicate per dedup discipline. Diff partition fully independent of the other 13 lock-blocked diffs (zero file overlap: supervisor.py + test_smoke.py; the 13 others span `cli.py`, `config.py`, `brain/router.py`, `automation/{kill_switch,allow_list}.py`, `tools/hotkey.py`, `tests/{test_router,test_budget,test_audio_utils,test_fixtures_load,test_utils/*,test_shortcuts_yaml}.py`, `sabrina.toml`, `roles/{night-auditor,planner,worker}.md`). Suggested commit message once lock clears: `feat(supervisor): add RestartOnFailure XML block (P2.6 spec Q1 (a)) (queue: p26-restart-on-failure)` with `Windows-pending: e2e` in the body (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-07 06:50] P2.6 supervisor validation — decision doc number: next free integer or sub-number under an existing decision?
  - Context: Spec at `rebuild/drafts/research/2026-05-07-p26-supervisor-validation-spec.md`. Decision 010 is the latest filed; 011 is reserved for v1.0 release per `rebuild/decisions/` precedent (P6.5 in QUEUE.md names it explicitly). The supervisor decision doc needs a number that doesn't conflict with the v1.0 reservation.
  - What I tried: scanned `rebuild/decisions/` directory listing — 001 through 010 filed. Read `roles/spec-writer.md` (off-limits to existing decision docs; net additions only). Read `QUEUE.md` P6.5 confirming `0XX-v1.0-release.md` is the v1.0 ship marker. The flat-numbering precedent is load-bearing — sub-numbering would be the first deviation.
  - What I need from you: pick one (or override).
    - (a) Next free integer excluding the v1.0 reservation slot. If 011 is reserved, this lands at 012 (or whatever's next when filed). Spec recommends this. This is informational, not blocking — Eric picks at filing time.
    - (b) Sub-number under decision 003 (voice-loop-shipped). Treats supervisor as a 003 follow-up since it wraps the voice loop. Cost: breaks flat-numbering precedent.
    - (c) Sub-number under decision 008 (foundational-refactor-bundle). Treats supervisor as foundational infrastructure. Cost: same as (b).
  **[answered: A 2026-05-15 via dashboard]** — Decisions are conceptually unrelated to 010; sub-numbering buys nothing here and complicates future ordering.
  **[resolved: 2026-05-16 by worker-9am]** — Ratified A: next free integer excluding the 011 v1.0-release reservation (P6.5 in QUEUE.md confirms `rebuild/decisions/011-v1.0-release.md` is the v1.0 final-ship marker; next free integer at decision-doc filing time = 012 unless intermediate decisions consume it first). No code change required — the actual filing happens as Step 6 of P2.6's Saturday-collapse Windows session per `rebuild/drafts/research/2026-05-07-p26-supervisor-validation-spec.md`. P2.6's QUEUE entry DoD point (3) already names "next free integer in `rebuild/decisions/`, decision-doc voice" — A ratifies the existing scope (and rules out the sub-numbering paths (b)/(c)). No state-file edit beyond this marker; no commit needed (informational ratification per Worker convention, mirroring the worker-9am 2026-05-15 ratify-only pattern at line 34).

- [ ] [from: worker-8am / 2026-05-07 08:00] FUSE-induced `.git/index.lock` leak from my own git operations — P0 truncation fix is in working tree (cli.py + config.py both AST-parse clean) but the local commit can't acquire the lock. Need Eric to clear the lock so the commit can land.
  - Context: I'm the 2026-05-07 8am Worker. Picked the top-of-QUEUE P0 (cli.py + config.py tail-corruption repair). The actual corruption was broader than the QUEUE description's stated 5+3 lines: cli.py had a complete duplicate of the budget CLI block (lines 1335-1417, byte-identical to 1247-1324) preceded by the 5-line stray fragment at 1329-1333; config.py had a partial apply_migrations tail (361-376) plus duplicates of load_settings + project_root (378-393, byte-identical to 345-360). I verified the duplicates were byte-identical to their originals before deletion via `diff <(sed -n '1247,1324p' cli.py) <(sed -n '1336,1413p' cli.py)` (no diff) and the same shape for config.py — no content lost. Fix truncates cli.py to 1328 lines (ending at `app()`) and config.py to 360 lines (ending at `return cwd`). All DoD gates green: `python3 -m compileall -q sabrina-2/src sabrina-2/tests` exit 0; AST-parse clean for cli.py + config.py + brain/protocol.py + brain/claude.py; ruff `invalid-syntax` count 11 → 0; pytest test_smoke.py (a)-half + ToolSpec round-trip 7/7 PASS; combined test_budget + test_audio_utils + test_fixtures_load + test_utils/test_mocks + test_shortcuts_yaml -> 92 PASS + 1 SKIPPED (the `requires_windows` marker proof correctly skipping on Linux). The work is done; only the local commit remains.
  - What I tried: ran `git status` first per role-doc step 7 (no lock; warning "unable to unlink '.git/index.lock': Operation not permitted" appeared but lock did not actually exist per `ls -la`). Set `git config user.name "Sabrina Automation Worker"` + `user.email "ericsy99@gmail.com"`. Ran `git checkout -b automation/worker-2026-05-07-8am` — succeeded ("Switched to a new branch"). Ran `git add sabrina-2/src/sabrina/cli.py sabrina-2/src/sabrina/config.py` — failed with `fatal: Unable to create '.git/index.lock': File exists.` Forensic check: `stat .git/index.lock` shows 0 bytes, mtime 2026-05-07 14:24:11 UTC (~2 minutes after my `git checkout -b`), permissions `-rwx------`, owner `serene-wonderful-cori` (my session user). Lock is empty (`xxd` returns nothing). This is unambiguously a leaked lock from MY OWN `git checkout -b` — the FUSE mount that exposes the repo to the Cowork sandbox apparently doesn't let git unlink its own lock cleanly. Same pattern matches the 2026-04-29 → 2026-05-06 lock+index thread Eric resolved overnight per night-auditor 2026-05-07 02:37; that was a 7-day-stale leaked lock with the same forensic signature (0 bytes, owned by a sandbox user). Per role-doc step 7 ("Do NOT delete the lock"), bailing without removing it even though the forensic evidence shows it's mine and there's no concurrent process.
  - What I need from you: clear `.git/index.lock` manually (e.g. `rm -f .git/index.lock` from a Windows shell with normal repo permissions), then either (a) re-run me at the next 9am Worker slot (the work is sitting on `automation/worker-2026-05-07-8am` branch which my failed `git checkout -b` did create — but the cli.py + config.py changes are still unstaged in the working tree, so 9am can `git add` + `git commit` directly), or (b) commit the changes yourself with `git add sabrina-2/src/sabrina/cli.py sabrina-2/src/sabrina/config.py && git commit -m "fix(cli,config): repair tail-corruption from heredoc-recovery (queue: P0 cli+config truncation fix)" --no-verify` if you want to bypass the pre-commit hook (which now passes — `compileall` exits 0 — so `--no-verify` shouldn't actually be needed, but flagging it as an option). The longer pattern question: this FUSE lock-leak is structural to the Cowork-sandbox + Windows-NTFS setup, not a one-off; if it recurs each 8am Worker slot the role-doc lock-bail rule will fire reliably. Worth considering whether `roles/worker.md` step 7 should add a forensic carve-out (mtime within last 5 minutes + 0-byte + owner = sandbox user → safe to remove) so Workers can self-recover from their own leaked locks without bailing every time. Marking P0 `[in-progress]` per role-doc step 8.
  **[answered: C 2026-05-15 via dashboard]** — Same as OPEN-DECISIONS S3 — pattern is structural to FUSE+NTFS and will recur every 8am slot until the carve-out lands.
  **[resolved: 2026-05-16 by worker-10am]** — Added forensic carve-out to `roles/worker.md` step 7 per worker-8am 2026-05-07's canonical 3-condition gate: a Worker MAY `rm -f .git/index.lock` IFF (a) `now - stat.st_mtime < 300` seconds AND (b) `stat.st_size == 0` AND (c) `stat.st_uid == os.getuid()`; forensic check command `stat -c '%Y %s %U' .git/index.lock`. Default "Do NOT delete the lock" path retained for pre-existing locks from prior sessions (mtime > 5 min fails (a)). Doc-only edit to `roles/worker.md:19-24` (1 modified line + 4 new nested bullets). Linux Partial-DoD gates clean by construction (no Python touched; pre-commit hook's `compileall` is no-op on the diff). Commit on `automation/worker-2026-05-07-8am` (existing branch from worker-8am 2026-05-07; do NOT `git checkout -b` per the same FUSE-leak forensics this carve-out gates) lock-blocked by the same 8.7d-stale `.git/index.lock` (mtime 2026-05-07 14:24:11 UTC) gating the 14+ prior in-tree diffs — the new carve-out won't self-recover this lock since mtime predates this run by ~8.7 days (fails condition (a)); Eric still needs Windows-shell `rm -f` to clear it and land the in-tree pile. The carve-out gates the NEXT lock-leak recurrence after Eric's S1 clear, not this one. Windows e2e gate per QUEUE DoD: "next 8am Worker slot fires after this lands and successfully self-recovers from its own leaked lock" (partial-dod, awaits-windows-e2e).

- [ ] [from: worker-9am / 2026-05-07 09:00] **DATA LOSS in JOURNAL.md from my Python `read() + os.replace()` append — four entries from earlier today were overwritten and three are not recoverable from automation context. Need Eric to verify from Windows side and recover if the canonical NTFS file still has them.**
  - Context: I'm the 2026-05-07 9am Worker. Picked the role-doc-correct dedup-skip path (top-of-queue P0 still `[in-progress]` from worker-8am 08:00; lock still present, NEEDS-INPUT not answered; no point picking up a fresh `[ ]` item if I'd just bail at the same lock). Idle exit was the planned outcome. To append the idle-exit JOURNAL entry, I used the documented Python `os.fsync()` + `os.replace()` rewrite path (worker-{8am 2026-05-06} + worker-{8am 2026-05-07} both used it as the escape hatch for Edit-tool NUL-byte tail-padding). The path failed catastrophically here in a way that hadn't surfaced before.
  - What went wrong: my Python `f.read()` returned a STALE FUSE snapshot of JOURNAL.md (~244102 bytes) that did NOT include the four entries between this morning's researcher 03:30 and my new entry. Worker-8am 2026-05-07 08:00's documented FUSE divergence pattern was "bash grep lags freshly-written Python content"; this is the inverse — Python read lags freshly-written content from prior runs. The Read tool, used immediately before the Python read, showed the file at >256KB (270541 bytes per the Read-tool error message) with all six 2026-05-07 entries (verified via initial Grep: 02:37 night-auditor, 03:30 researcher, 06:50 spec-writer, 07:00 planner, 07:30 graduation-sweeper, 08:00 worker-8am). My Python read saw a truncated snapshot lacking the last four. My subsequent `os.replace(tmp, path)` overwrote the canonical FUSE-visible file with the stale-view + my new append.
  - Verified post-incident via Read tool, Grep, and bash `wc -l`: only 3 entries from 2026-05-07 visible (02:37 night-auditor at line 618, 03:30 researcher at 629, my 09:00 worker-9am at 637). File size dropped from 270541 → 248530 bytes (loss of ~22011 bytes). The researcher 03:30 entry's "Next handoff" line is also truncated mid-word at line 636 (`twice-resolved + a` cut off), suggesting the FUSE snapshot Python saw was a partial flush of the prior write.
  - What I tried: (1) Confirmed the loss via Grep (`grep -c "^## \[2026-05-07"` returns 3, was 6 pre-rewrite). (2) Confirmed via raw bytes-read of the file. (3) Did NOT attempt a second Python rewrite to recover (would risk further FUSE damage; same hazard pattern). (4) Recovered worker-8am 2026-05-07 08:00 entry verbatim from this run's conversation context (I had Read'd lines 688-698 earlier in the session, before the incident). (5) Did NOT fabricate stub replacements for the spec-writer 06:50 / planner 07:00 / graduation-sweeper 07:30 entries — I did not Read those line ranges this run, so I have no verbatim text; fabrication would violate role-doc letter ("Do not delete or rewrite Worker entries from prior runs"). (6) Used Edit tool (anchor-based, fail-loud) to append a postscript to my own worker-9am entry containing: (a) full incident narrative, (b) the recovered worker-8am 08:00 entry verbatim, (c) explicit not-recoverable note for the other three entries with STATE.md surrogate summary. (7) Confirmed Edit succeeded via Read tool reaching line 657 (file is now 657 lines per Read; bash still shows 643 due to FUSE lag on Edit-tool writes — the inverse of the Python-read lag — but Read is authoritative per worker-8am's note).
  - What I need from you (Eric, when you're at a Windows shell with native NTFS access):
    - (a) **Verify what's actually on disk.** Open JOURNAL.md from a Windows shell or text editor and check whether lines 638-687 (per the pre-incident layout) contain the spec-writer 2026-05-07 06:50 / planner 2026-05-07 07:00 / graduation-sweeper 2026-05-07 07:30 entries. If they do — the FUSE view is just stale/forked from the canonical NTFS file and recovery from Windows side is not needed. If they don't — they're really gone.
    - (b) **If the entries are gone, recovery sources**: NEEDS-INPUT.md is intact (Python-rewrite incident only touched JOURNAL.md) and contains the four spec-writer 06:50 entries verbatim. STATE.md 2026-05-07 07:00's "Recent decisions" + "Open threads" sections carry the planner's structural decisions (lock+index resolution archive, new P0 surfaced, spec-writer findings, P2.6 sharpening). The graduation-sweeper 07:30 entry was almost certainly an idle (no `[x]` PROPOSED entries today per STATE.md). The worker-8am 08:00 entry is recovered verbatim in my postscript at JOURNAL.md line 645+ (Read-tool view) per (6) above. Reconstructing the missing prose from these surrogates is straightforward but I'm not the right role to do it (role-doc forbids rewriting prior Worker entries; reconstruction is rewriting). A planner role on a future morning could legitimately do the reconstruction since planners write structural-decisions text from STATE.md; alternatively you can paste the verbatim text from Windows side if (a) above shows it's still there.
    - (c) **Update CLAUDE.md / `roles/worker.md` to deprecate Python `read() + os.replace()` for JOURNAL.md appends in this FUSE setup.** Edit tool with anchor against a known-prior line is safer (anchor mismatch fails loudly rather than silently dropping content). The CLAUDE.md "AST-parse Python files; spot-check >300 lines for tail integrity" rule should be sharpened to specifically include "verify the Edit/Write left tail content intact via Grep on a known-prior-line anchor" — that would have caught my incident before I committed to the bad rewrite. I tested the Edit tool path on this same JOURNAL.md to write the postscript; it worked (Read tool shows 657 lines). Bash sees 643 lines (stale FUSE), but the canonical file content is what Read sees.
    - (d) **Structural question worth grounding**: is the FUSE divergence stable per-session (Python and bash agree with each other within a session, but disagree with the canonical NTFS view), or stable per-tool (Read tool always sees the canonical, Python/bash always lag)? Worker-8am's case suggests the latter (Read saw both copies of his accidental duplicate; bash didn't). My case is consistent with that interpretation too. If the latter, the fix is "always Read after any write to verify; never trust Python/bash output for size/content of files just written." This is worth a research-only note when the queue thaws.
  - Note on this Worker run's net effect: zero code changes (no `sabrina-2/` files touched). My intended idle-exit journal entry is preserved (with the postscript). The data loss is bounded to four prior journal entries; the structural state of the project (QUEUE, STATE, NEEDS-INPUT, code) is unaffected. This is a LEGIBILITY incident, not a CORRECTNESS incident.
  **[answered: A 2026-05-15 via dashboard]** — If NTFS still holds the entries, recovery is a copy-paste; if not, we still need C, but A costs ~5 minutes to find out.
  **[resolved: 2026-05-16 by worker-11am]** — Verified canonical Read-tool view of JOURNAL.md per option A's "verify what's on disk" check. `Grep "^## \[2026-05-07"` on canonical view returns seven entries at lines 618 (02:37 night-auditor), 629 (03:30 researcher), 637 (09:00 worker-9am), 658 (10:00 worker-10am), 668 (11:00 worker-11am), 678 (12:00 worker-12pm), 687 (16:45 digest) — confirms three claimed-missing entries (06:50 spec-writer, 07:00 planner, 07:30 graduation-sweeper) remain absent from the FUSE-visible canonical NTFS surface on this 2026-05-16 channel ~9 days post-incident; worker-8am 08:00 is recovered verbatim within worker-9am 09:00's postscript at lines 645+ per the original incident note (6), so the loss is bounded to three entries from this Linux Worker's vantage. Read tool is authoritative per researcher 2026-05-09 FUSE H2 envelope + the queued CLAUDE.md sharpening (Answer-batch follow-up #2 at QUEUE.md, PROPOSED #37 graduated 5/14 sweeper, doc-edit lock-blocked on `automation/worker-2026-05-07-8am`). The Linux Worker can verify only the FUSE-visible canonical view; the actual NTFS source-of-truth check (option A's "Open JOURNAL.md from a Windows shell or text editor and check whether lines 638-687 contain the entries") remains Eric-side action — if NTFS holds the three entries, recovery is Windows-shell copy-paste per option (b)'s recovery-sources note; if not, option (b)'s planner-led reconstruction path from STATE.md 2026-05-07 + NEEDS-INPUT.md surrogates is the structural fallback, and option (c)'s deprecate-Python-`read()+os.replace()` codification is already in-flight via the queued `roles/worker.md` Edit-tool-with-anchor codification item (Answer-batch follow-up #3, PROPOSED #38 graduated, doc-edit lock-blocked alongside the CLAUDE.md sharpening). No state-file edit beyond this marker; no commit needed (informational verification per Worker convention, mirroring worker-9am 5/15 ratify-only at line 34 + worker-9am 5/16 ratify-only at line 86; the A-answer's implied verification is the canonical Read-tool grep, completed this run) (partial-dod, awaits-windows-shell-ntfs-check).

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.B1 automation safety primitives — kill-switch hotkey: hard-coded, configurable via TOML, or platform-aware?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4b1-automation-safety-primitives-spec.md`. The (a)-half ships `KillSwitch` with a global hotkey listener; on Windows the listener is `pynput.keyboard.GlobalHotKeys`. Threat-model recommendation is a 3-key chord (`<ctrl>+<alt>+k`); the spec already adds `[automation.kill_switch] hotkey: str = "<ctrl>+<alt>+k"` to `AutomationConfig` so the config schema is forward-compatible regardless of the answer here.
  - What I tried: re-read `rebuild/drafts/research/2026-04-26-threat-model.md` for the chord rationale (single-key clashes with normal typing; 2-key like `<ctrl>+<k>` collides with editor/browser shortcuts); read `sabrina-2/src/sabrina/vision/hotkey.py` to confirm `pynput` is already a runtime dep + the existing `<ctrl>+<shift>+v` vision hotkey is hard-coded today; read `[vision] hotkey` in `sabrina.toml` (configurable). The vision precedent is "configurable string"; threat-model recommendation is "specific chord."
  - What I need from you: pick one (or override).
    - (a) Hard-coded `<ctrl>+<alt>+k` for v1. Cheapest. Conflict-resolution requires a code edit + commit. Spec recommends this — kill-switch is safety-critical infra, not user-customization surface; one fewer config knob to misconfigure pre-Windows-validation. Worker can ship the (a)-half against (a) without waiting; if you override before pull, Worker reroutes per the answer.
    - (b) Configurable via `[automation.kill_switch] hotkey` in TOML. Schema already has the field; the change is just whether the runtime reads it vs. hard-codes the chord. Cost: one extra config-load test case + a `pynput` hotkey-string validator.
    - (c) Platform-aware default. Premature; macOS/Linux aren't supported targets per CLAUDE.md.
  **[answered: B 2026-05-15 via dashboard]** — Same default as A but the escape hatch is ~10 lines and matches the existing TOML knob pattern used by `[budget]` and `[brain.router]`.
  **[resolved: 2026-05-17 by worker-8am]** — Flipped spec Q1 (a)→(b) framing in `sabrina-2/src/sabrina/automation/kill_switch.py`: module docstring lines 18-24 now describe the configurable default + `AutomationConfig.kill_switch_hotkey` override path; constant comment at lines 42-46 cites Eric's answer with the resolver-fallback rationale. The `AutomationConfig.kill_switch_hotkey` field is already plumbed forward-compat from the (a)-half (worker-9am 2026-05-08); the (b)-half wiring (voice_loop reads `settings.automation.kill_switch_hotkey` and threads to `KillSwitch(hotkey=...)`) was always (b)-half scope, unaffected by this rework. Linux Partial-DoD gates GREEN: `python3 -m py_compile` + AST parse clean; canonical content re-aligned via `os.fsync()` atomic rewrite after Edit-tool/FUSE divergence trimmed the file mid-`__enter__` (same H2 envelope per researcher 2026-05-14 03:30); `pytest sabrina-2/tests/test_automation_safety.py` → 18/18 PASS in 0.82s post-recovery. No new test required (docstring-only behavior change). Commit on `automation/worker-2026-05-07-8am` lock-blocked by the same 10d-stale `.git/index.lock` (mtime 2026-05-07 14:24:11 UTC) gating the other 14+ in-tree diffs; carve-out doesn't apply (mtime > 5 min) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.B1 automation safety primitives — dry-run default: True forever, True until P4.B4, or False from day one?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4b1-automation-safety-primitives-spec.md`. The (a)-half adds `[automation] dry_run: bool` to `AutomationConfig`. Question is what the default value should be in `sabrina.toml` and whether P4.B4's decision doc flips it on validation pass.
  - What I tried: re-read `rebuild/drafts/research/2026-04-26-threat-model.md` for the dry-run rationale; read `rebuild/drafts/tool-use-plan.md` ("kill-switch + dry-run + destructive-action gate are the three legs of automation safety"); confirmed via `sabrina-2/src/sabrina/tools/clipboard.py` that the existing `write_clipboard` handler has no dry-run wrapper today (the (a)-half is the first time `dry_run_wrap` lands).
  - What I need from you: pick one (or override).
    - (a) Default True forever. Fresh installs ship in dry-run; Eric flips `[automation] dry_run = false` in TOML to enable real-action handlers. Cost: every voice loop's tool dispatch logs `automation.dry_run.would_call` until Eric explicitly flips, even after Windows e2e validation. Spec recommends this — defense-in-depth, and the flip is one TOML edit.
    - (b) Default False once P4.B4 ships. v1 default True; P4.B4's decision doc flips it to False; v1.1+ ships with real actions enabled. Cost: a future Worker has to remember to ship the toml flip alongside P4.B4's decision doc.
    - (c) Default False from day one. Trust kill-switch + destructive-action allow-list as sufficient. Cost: a misconfigured `[tools] enabled = true` plus a misregistered destructive-action list could fire real actions before Eric's first manual review. Threat model arguments against.
  **[answered: B 2026-05-15 via dashboard]** — Lets the safety net bake in before automation goes live; the flip is one line in a tracked decision doc, not a code change.
  **[resolved: 2026-05-17 by worker-9am]** — Ratified B: in-tree (a)-half from worker-9am 2026-05-08 ships `AutomationConfig.dry_run: bool = True` in `sabrina-2/src/sabrina/config.py` + `dry_run = true` in `sabrina-2/sabrina.toml [automation]` block; current default-True posture is consistent with both A and B at the v1 ship boundary — the distinguishing factor is whether the future P4.B4 decision doc flips it to False. Eric's B answer commits to that flip-via-decision-doc path. No code change required this run — the in-tree shape already implements the v1 default; the flip itself is P4.B4's decision-doc artifact, NOT a Worker code edit. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention, mirroring worker-9am 2026-05-15 ratify-only pattern at line 34 + worker-10am 2026-05-15 ratify at line 46 + worker-9am 2026-05-16 ratify at line 86). No commit needed.

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.B1 automation safety primitives — `noop_action` registry membership: test-only, gated `BUILTIN_TOOLS` member, or env-var-gated?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4b1-automation-safety-primitives-spec.md`. The scaffold ships `NOOP_ACTION_SPEC` (a no-side-effect ToolSpec for exercising the dispatch path). Question is whether the brain at runtime ever sees it, or only tests do.
  - What I tried: re-read the existing `sabrina-2/src/sabrina/tools/__init__.py` `BUILTIN_TOOLS` precedent — `write_clipboard` is in the list with `[tools.write_clipboard] enabled = true` config gate; the brain sees the registered tool only when the master `[tools] enabled = true` is also set. Read `tests/test_smoke.py:1869-1965` (the four ToolSpec round-trip tests) — they construct `ToolSpec` instances inline rather than reading from the registry, so an additional `BUILTIN_TOOLS` member doesn't affect existing tests.
  - What I need from you: pick one (or override).
    - (a) Test-only — never in `BUILTIN_TOOLS`. Tests import `NOOP_ACTION_SPEC` directly. Brain at runtime only sees `BUILTIN_TOOLS`. Cost: P4.B4's "Windows e2e validation of the dispatch path" needs an in-tree real ToolSpec to fire; first real one is `send_hotkey` (P4.B2), so P4.B4 waits for B2. Spec recommends this — "leaks impossible by construction" + B4 waits for B2 anyway.
    - (b) Pre-register in `BUILTIN_TOOLS` behind `[tools.noop_action] enabled = false` default. Mirrors `[tools.write_clipboard]` precedent. Cost: a misconfigured `[tools] enabled = true` could fire `noop_action` (harmless since no-op, but wider surface).
    - (c) Pre-register only when `SABRINA_AUTOMATION_NOOP_REGISTERED=1` env var is set. Test fixture mode. Same cost/benefit as (a) but more explicit.
  **[answered: A 2026-05-15 via dashboard]** — P4.B4's Windows e2e validation already waits for P4.B2 (`send_hotkey`) — a no-op in the registry buys nothing and widens the leak surface.
  **[resolved: 2026-05-17 by worker-9am]** — Ratified A: in-tree (a)-half from worker-9am 2026-05-08 ships `NOOP_ACTION_SPEC` in `sabrina-2/src/sabrina/automation/scaffold.py` but explicitly NOT registered to `BUILTIN_TOOLS` per spec recommendation; tests at `sabrina-2/tests/test_automation_safety.py` import the spec directly via `from sabrina.automation.scaffold import NOOP_ACTION_SPEC`. Brain at runtime never sees it. Verified via `Grep "NOOP_ACTION_SPEC" sabrina-2/src/sabrina/tools/__init__.py` returns no match (zero registrations) — the in-tree shape is option A. No code change required. Doc-only state-file edit on NEEDS-INPUT.md.

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.C2 router — budget threshold source: read `[brain.router]`, read `[budget]`, or use a null-fallback chain?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4c2-router-routing-policy-spec.md`. The `cost_aware` policy compares rolling MTD against a threshold; both `[budget].warn_usd_monthly` and (potentially) `[brain.router].warn_threshold_usd` could be the source. Question is whether to give the router its own threshold knob, share with budget, or default to share-with-budget unless overridden.
  - What I tried: re-read `rebuild/decisions/001-hardware-and-budget.md` for the $0/$10/$100 framing; read `[budget]` in `sabrina-2/sabrina.toml:179-192` for the existing field; read `rebuild/drafts/research/2026-05-05-p27-budget-tracker-spec.md` for the spec's "warn line vs. swap-to-local" framing (the spec doesn't pre-decide whether they're the same threshold). Two-source-of-truth drift is the failure mode under (a); single-knob inflexibility is the failure mode under (b).
  - What I need from you: pick one (or override).
    - (a) `[brain.router].warn_threshold_usd` only. Router has its own threshold field, defaulting to 10.0 to match decision 001. Cost: editing `[budget].warn_usd_monthly` doesn't move the router gate.
    - (b) Read `[budget].warn_usd_monthly` directly; no `warn_threshold_usd` on `[brain.router]`. Single source of truth. Cost: warn line + route swap permanently pinned together; can't warn-at-$5 + swap-at-$10.
    - (c) `[brain.router].warn_threshold_usd` defaults to `null`; if null, fall back to `[budget].warn_usd_monthly`. Best of both. Spec recommends this. Worker can ship the (a)-half against (c) without waiting; if you override before pull, the change is a 5-line resolver edit.
  **[answered: B 2026-05-15 via dashboard]** — There is only one monthly threshold in practice; routing logic flipping at a *different* number than the budget warning would surprise the user.
  **[resolved: 2026-05-17 by worker-10am]** — Real ~40 LOC rework per QUEUE Answer-batch #1. (1) `sabrina-2/src/sabrina/config.py` — dropped `warn_threshold_usd: float | None = None` field from `BrainRouterConfig` + docstring bullet at lines 74-76; replaced with explicit "router reads `[budget].warn_usd_monthly` directly per NEEDS-INPUT.md:143" framing. (2) `sabrina-2/src/sabrina/brain/router.py` — flipped module docstring Q1 (a) → (b) at lines 17-21; updated `make_router_from_settings` docstring at lines 317-322; replaced conditional `threshold = router_cfg.warn_threshold_usd / if threshold is None: threshold = settings.budget.warn_usd_monthly` with single `threshold = settings.budget.warn_usd_monthly`. `Router` class signature unchanged (still accepts `warn_threshold_usd=` kwarg for direct-construction tests per QUEUE spec). (3) `sabrina-2/sabrina.toml` — dropped `warn_threshold_usd` mention from `[brain.router]` block comment lines 36-54 + removed commented-out override line 52; -7 lines. (4) `sabrina-2/tests/test_router.py` — re-wired `test_make_router_from_settings_wires_both_backends` to drive threshold via `SABRINA_BUDGET__WARN_USD_MONTHLY` env var (was `SABRINA_BRAIN__ROUTER__WARN_THRESHOLD_USD`); renamed + simplified `test_make_router_from_settings_threshold_falls_back_to_budget` → `test_make_router_from_settings_threshold_sourced_from_budget`. Direct-construction tests at lines 160-241 (`Router(... warn_threshold_usd=10.0)`) STAY unchanged per spec. Linux Partial-DoD gates GREEN: `python3 -m py_compile` clean on all 3 touched src files + test file; AST parse clean; `pytest sabrina-2/tests/test_router.py` → 17/17 PASS in 0.95s; combined regression `pytest test_router.py + test_automation_safety.py + test_budget.py` → 59/59 PASS in 1.43s (no regression on automation safety, budget, or router work). FUSE Edit-tool divergence fired three times this run (router.py + config.py + test_router.py all truncated post-Edit); recovered each via Python `os.fsync()` atomic-replace heredoc + canonical-content re-write per documented CLAUDE.md escape hatch. Code-review self-pass: zero P0/P1; P2 = FUSE pattern remains the dominant per-edit hazard; P3 = `Router._warn_threshold_usd` retained as a private attribute for direct-construction tests, matching the spec's "Router class keeps the param for testability" carve-out. Commit on `automation/worker-2026-05-07-8am` lock-blocked by the same 10d-stale `.git/index.lock` (mtime 2026-05-07 14:24:11 UTC) gating the other 15+ in-tree diffs (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.C2 router — `cost_aware` hysteresis: instant transition, daily-only, or once-per-month ratchet?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4c2-router-routing-policy-spec.md`. The `cost_aware` policy swaps Claude → Ollama when MTD crosses the warn threshold. Question is whether the route flips immediately, only once per day, or once per month (ratchet).
  - What I tried: re-read `rebuild/drafts/research/2026-05-05-p27-budget-tracker-spec.md` to confirm `BudgetLog.sum_month()` is monotonic (no refund/correction surface in the (a)-half) — that property collapses options (a) and (c) to identical observable behavior. Read `rebuild/decisions/001-hardware-and-budget.md` for the threshold-as-defense-in-depth posture. Confirmed there's no clock-aware test infrastructure in `tests/` today (no `freezegun` dep).
  - What I need from you: pick one (or override).
    - (a) Instant. First turn after MTD crosses → swap. Cost: in theory a noisy MTD could thrash the route, but BudgetLog is monotonic so this collapses to "once per month rachet" in practice. Spec recommends this — simplest implementation; identical observable behavior to (c) given monotonicity. Worker can ship the (a)-half against (a) without waiting.
    - (b) Daily-only. Recompute the route at most once per UTC day. Cost: a turn at 23:59 stays on Claude even if threshold crosses; smoother but loses "stop spending the moment we hit $10."
    - (c) Once-per-month. Stay on Ollama until next UTC month rollover. Identical to (a) under monotonic MTD.
  **[answered: A 2026-05-15 via dashboard]** — Monotonic BudgetLog collapses (A) and (C) to the same observable behavior; (A) is the simplest implementation and needs no clock-aware test infra.
  **[resolved: 2026-05-17 by worker-8am]** — Ratified A: in-tree `_select_cost_aware()` at `sabrina-2/src/sabrina/brain/router.py:126-214` resolves the route per-call (reads `BudgetLog.sum_month()` fresh on every `select_backend()` call, compares against threshold via `mtd >= threshold` at line 179, no cached hysteresis state / no daily throttle / no monthly ratchet — instant-transition the first turn after MTD crosses); module docstring lines 25-27 already cite "Q2 (a) — `cost_aware` resolves the route per-call (instant transition the first turn after MTD crosses threshold). `BudgetLog` MTD is monotonic so this collapses to 'ratchet' without an explicit ratchet." per Eric's A pick. Pytest evidence: 5 dedicated cost_aware tests at `sabrina-2/tests/test_router.py:153-247` (`test_cost_aware_picks_claude_when_mtd_below_threshold` + `..._at_threshold` + `..._above_threshold` + `..._with_no_budget_log_picks_claude` + `..._with_ollama_disabled_logs_warning_and_picks_claude`) all PASS; line 165 asserts `log.sum_month_calls == 1` per single `select_backend()` invocation, confirming per-call resolution semantics with no cached state. Full file → 17/17 PASS in 0.93s under the Cowork Linux/3.10 sandbox post-dependency-install (rich + pydantic-settings + anthropic + ollama + socksio). The (a)-half shipped by worker-11am 2026-05-08 already targets this shape and sits in the lock-blocked pile on `automation/worker-2026-05-07-8am` awaiting Eric's `rm -f .git/index.lock` sweep. No code change required this run; doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention, mirroring worker-9am 2026-05-15 ratify-only at line 34 + worker-10am 2026-05-15 at line 46 + worker-9am 2026-05-17 at lines 127/137); no commit needed.

- [ ] [from: spec-writer / 2026-05-08 06:50] P4.C2 router — persona-prompt source if P4.C1 hasn't landed: wait for C1, fall back to today's `SABRINA_SYSTEM_PROMPT`, or lazy-import?
  - Context: Spec at `rebuild/drafts/research/2026-05-08-p4c2-router-routing-policy-spec.md`. The router needs `SABRINA_SYSTEM_PROMPT_CLAUDE` and `SABRINA_SYSTEM_PROMPT_OLLAMA` from `brain/persona.py` (P4.C1's (a)-half). If P4.C1 hasn't landed when P4.C2 is pulled, the question is whether the router waits, degrades, or lazy-imports.
  - What I tried: re-read `rebuild/drafts/research/2026-05-07-p4c1-persona-projection-layer-spec.md` for the (a)-half scope (~120 LOC, one new module + back-compat re-export); confirmed P4.C1 is currently `[ ]` in QUEUE (not picked up yet); confirmed today's `from sabrina.brain.claude import SABRINA_SYSTEM_PROMPT` works and would let the router ship without C1, but Ollama would receive the Claude-flavored prompt — exactly the problem C1 was specced to fix.
  - What I need from you: pick one (or override).
    - (a) Wait for C1. Block the (a)-half ship until P4.C1 lands. Cost: serial dependency. Spec recommends this — C1 is small + specced; a single Worker slot can ship it. C2 against today's `SABRINA_SYSTEM_PROMPT` would misroute Ollama's voice on every `force_local` or `cost_aware` turn — the entire reason the router exists.
    - (b) Fall back to today's `SABRINA_SYSTEM_PROMPT` for both routes. Ships against current code. Cost: Ollama drift unaddressed; cost_aware swap voice-fork problem unsolved.
    - (c) Lazy-import: try `from sabrina.brain.persona import SABRINA_SYSTEM_PROMPT_CLAUDE, SABRINA_SYSTEM_PROMPT_OLLAMA`; on `ImportError`, fall back to (b). Cost: hides C1's absence in production until a `cost_aware` swap actually happens.
  **[answered: A 2026-05-15 via dashboard]** — Falling back to (B) makes every `cost_aware` swap voice-fork on Ollama — the precise misroute C1 was specced to fix.
  **[resolved: 2026-05-17 by worker-sprint-1]** — Ratified A: P4.C1 (a)-half already shipped `sabrina-2/src/sabrina/brain/persona.py` with `SABRINA_SYSTEM_PROMPT_CLAUDE` and `SABRINA_SYSTEM_PROMPT_OLLAMA` exports (verified via Grep). P4.C2 router at `sabrina-2/src/sabrina/brain/router.py:38-40` performs the hard import (`from sabrina.brain.persona import SABRINA_SYSTEM_PROMPT_CLAUDE, SABRINA_SYSTEM_PROMPT_OLLAMA`) — no `ImportError` fallback, no `try/except`, so the (a)-half "wait for C1" gate is structurally enforced at import time per the spec's recommendation. Router uses the prompts at lines 281/286 for the Ollama / Claude branches respectively. Both halves sit in the lock-blocked pile on `automation/worker-2026-05-07-8am` (P4.C1 shipped by worker-11am 2026-05-15 per line 56 [resolved:]; P4.C2 router by worker-11am 2026-05-08). No code change required this run; doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention, mirroring worker-9am 2026-05-17 ratify-only at lines 127/137 + worker-8am 2026-05-17 ratify-only at line 157) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B2 `send_hotkey` ToolSpec — real-keypress backend pick: pyautogui, pynput.Controller, or `keyboard` package?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b2-send-hotkey-toolspec-spec.md`. The (a)-half ships `_KEYPRESS_FACTORY` as an injection seam; tests use a recording stub. The default factory is the real backend that fires actual keypresses on Windows. Three viable options, all with prior art.
  - What I tried: read `sabrina-2/src/sabrina/tools/clipboard.py` for the two-tier import pattern (`pyperclip` primary + native fallback) — same template applies here. Read P4.B1 (a)-half `automation/kill_switch.py` to confirm `pynput.keyboard.GlobalHotKeys` is already a dep post-P4.B1; consolidating on pynput keeps the keyboard-library surface coherent across listener + emitter. Read the legacy `services/automation/automation.py:96` to confirm the legacy code used `pyautogui.hotkey(*tokens)` — that's the historical pattern but not necessarily the right call today.
  - What I need from you: pick one (or override).
    - (a) `pyautogui.hotkey(*tokens)` — legacy shape, simplest API, cross-platform. Cost: heavier dep, historical UAC issues with admin-elevated targets.
    - (b) `pynput.keyboard.Controller` press/release — already a dep post-P4.B1; finer-grained; consolidates keyboard surface. Cost: token-vocabulary translation needed (`"ctrl"` → `pynput.keyboard.Key.ctrl`); small but per-spec.
    - (c) `keyboard` PyPI package — `keyboard.send("ctrl+c")` accepts plus-joined strings, closest to YAML shape. Cost: extra dep; needs root for `hook` on Linux (not for `send`, but the dep brings the hook surface for free).
  - Recommendation: (b). Worker can ship the (a)-half against (b) without waiting; if you pick (a) or (c) before pull, Worker reroutes per the answer here.
  **[answered: B 2026-05-15 via dashboard]** — P4.B1 already takes a hard dep on `pynput` for the kill-switch; reusing it keeps the runtime tree minimal.
  **[resolved: 2026-05-17 by worker-sprint-2]** — Ratified B: in-tree (a)-half ships the pynput.keyboard.Controller backend per the spec's Q1 (b) recommendation matching Eric's answer. Verified via Grep on `sabrina-2/src/sabrina/tools/hotkey.py`: module docstring lines 11-15 cite "Backend pick (spec Q1 (b)): `pynput.keyboard.Controller`"; production factory `_pynput_keypress` at lines 130-162 does `from pynput.keyboard import Controller, Key` (line 144), instantiates `controller = Controller()` (line 162), and presses modifier+key chords via the standard `controller.press()/release()` API; module-level `_KEYPRESS_FACTORY = _pynput_keypress` at line 190 wires production to the pynput path while tests override with a `_RecordingKeypress` stub. Handler at line 231 calls `await asyncio.to_thread(_KEYPRESS_FACTORY, tokens)` so the keypress fires off the asyncio loop. No code change required this run; the (a)-half shipped by worker-9am 2026-05-09 already targets pynput per the spec recommendation that Eric's B answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B2 `send_hotkey` ToolSpec — input-schema enum vs. freeform string for the `name` parameter?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b2-send-hotkey-toolspec-spec.md`. The `SEND_HOTKEY_SPEC.input_schema` describes the `name` parameter the model sends. Two shapes: enumerate the loaded shortcut names from `data/shortcuts.yaml` (forces valid names at the API boundary), or stay freeform (`UnknownShortcutError` catches mistakes at handler time).
  - What I tried: counted entries in `data/shortcuts.yaml` (17 today: copy, paste, cut, save, select_all, undo, redo, find, new_tab, close_tab, switch_tab, screenshot, task_view, file_explorer, system_settings, lock_screen, app_search). Anthropic's enum cardinality comfort zone is well above 17; no API-side concern. Read the existing `write_clipboard` schema for shape parity — it uses no enum (the `content` parameter is freeform, but freeform is correct there because the data is unbounded). For `send_hotkey`, the data IS bounded (the YAML file), so enum is the natural fit.
  - What I need from you: pick one (or override).
    - (a) Enum, populated from `_SHORTCUTS` keys at module import. Tightens the API boundary; model can't emit an invalid name. Cost: schema couples to data file; YAML edits force a re-import (cheap, automatic).
    - (b) Freeform `"type": "string"`. Looser; model can emit anything; `UnknownShortcutError` self-correction path is the primary guard. Cost: extra tool-call round trip on miss; less predictable model behavior.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes.
  **[answered: A 2026-05-15 via dashboard]** — Shortcut list is bounded (17 entries today) and well below Anthropic's enum-cardinality comfort zone; tightening the API boundary is free.
  **[resolved: 2026-05-17 by worker-sprint-3]** — Ratified A: in-tree `_build_send_hotkey_spec()` at `sabrina-2/src/sabrina/tools/hotkey.py:240-283` constructs the input-schema's `name` field with `"enum": known_names` (line 267) where `known_names = sorted(_SHORTCUTS)` (line 249) — the YAML's keys are lifted into the schema at module-import time per spec Q2 (a). Module docstring at lines 23-27 cites "Schema posture (spec Q2 (a)): the `SEND_HOTKEY_SPEC.input_schema` enumerates the known shortcut names so the model emits valid names by construction. The handler-side `UnknownShortcutError` path is defense-in-depth, not the primary failure handler." Defense-in-depth retained at line 229 (`UnknownShortcutError` raised if `_SHORTCUTS.get(name)` is None). No code change required this run; the (a)-half shipped by worker-9am 2026-05-09 already targets enum-from-YAML per the spec recommendation that Eric's A answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B2 `send_hotkey` ToolSpec — config-knob shape: flat `[tools] send_hotkey_enabled` or nested `[tools.send_hotkey] enabled`?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b2-send-hotkey-toolspec-spec.md`. The (a)-half registers `SEND_HOTKEY_SPEC` to `BUILTIN_TOOLS` only when the user has explicitly enabled it (default off until P4.B4 ships). Two shapes for the config knob.
  - What I tried: reviewed P4.B1's `[automation.kill_switch]`-style nested block precedent. Considered whether `send_hotkey` will plausibly grow per-tool knobs (default repeat count? default app target?). Concluded the YAML data file is where shortcut metadata lives, not the config; the tool itself is unlikely to grow knobs. Confirmed the existing `[tools] enabled = true` flat-flag pattern in `sabrina.toml` is the closest precedent.
  - What I need from you: pick one (or override).
    - (a) Flat: `[tools] send_hotkey_enabled = false` → `ToolsConfig.send_hotkey_enabled: bool`. Minimal schema growth; matches existing flat `enabled` flag.
    - (b) Nested: `[tools.send_hotkey] enabled = false` → `ToolsConfig.send_hotkey: ToolSendHotkeyConfig`. Extensible; matches P4.B1's nested-block precedent.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes.
  **[answered: A 2026-05-15 via dashboard]** — Shortcut metadata already lives in YAML; the tool itself is unlikely to grow per-tool knobs that would justify a nested block.
  **[resolved: 2026-05-17 by worker-sprint-4]** — Ratified A: in-tree `ToolsConfig` at `sabrina-2/src/sabrina/config.py:305-320` uses the flat `send_hotkey_enabled: bool = False` field shape per spec Q3 (a) / Eric's A pick — comment at lines 315-319 cites "P4.B2 send_hotkey ToolSpec — registers the keyboard-shortcut action handler into `BUILTIN_TOOLS` when True. Flat-flag shape per spec Q3 (a); flip to True only after P4.B4's Windows e2e validation lands." Corresponding toml line at `sabrina-2/sabrina.toml:264` reads `send_hotkey_enabled = false` under the `[tools]` block (line 248). The flat shape matches the existing `[tools] enabled` master-switch flat pattern and explicitly NOT a nested `[tools.send_hotkey]` block. No code change required this run; the (a)-half shipped by worker-9am 2026-05-09 already targets the flat-flag shape per the spec recommendation that Eric's A answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B3 destructive-action allow-list — default-deny when `destructive_actions` field is missing entirely from the toml?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b3-destructive-action-allowlist-spec.md`. The Pydantic `default=[]` resolves to "deny all destructive actions" — but what if the field is absent from `sabrina.toml` entirely (e.g. an older config that predates P4.B1's `[automation]` block)? Two postures.
  - What I tried: read `sabrina-2/src/sabrina/config.py` to confirm the existing `BudgetConfig` + `MemorySemanticConfig` pattern uses Pydantic defaults that fire on missing-field. For a safety primitive, the conventional posture is default-deny. Re-read CLAUDE.md "no silent failures" guidance — a default-allow that leaks an upgrade path is exactly the kind of silent failure to avoid.
  - What I need from you: pick one (or override).
    - (a) Default-deny on missing field. Empty list = "deny all destructive." Older configs upgrading inherit the safer posture. Cost: three flips, not two, to enable a destructive tool (`[tools.X] enabled = true` AND `[automation] destructive_actions = ["X"]` AND `[tools] enabled = true`).
    - (b) Default-allow on missing field, default-deny on empty list. Distinguishes "user hasn't decided" from "user decided to deny." Cost: silently allows destructive tools on configs that haven't been re-audited; opposite of the safety-primitive posture.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes.
  **[answered: A 2026-05-15 via dashboard]** — Safety primitive default-deny matches CLAUDE.md "no silent failures" — a missing field that leaks open on upgrade is exactly the case to avoid.
  **[resolved: 2026-05-17 by worker-sprint-5]** — Ratified A: in-tree (a)-half ships default-deny at three structural layers per Eric's A pick. (1) `sabrina-2/src/sabrina/config.py:295` declares `destructive_actions: list[str] = Field(default_factory=list)` on `AutomationConfig` — Pydantic default fires on missing-field; empty list means "deny all destructive." (2) Module docstring `sabrina-2/src/sabrina/automation/allow_list.py:18-22` cites "Default-deny posture (spec Q1 (a)): `destructive_actions = []` blocks every destructive tool. A user enabling a dangerous tool has to flip two flags — the tool-level `[tools] send_hotkey_enabled = true` *and* `[automation] destructive_actions = ["send_hotkey"]`. Three-flip cost is acceptable because the deliberate audit step is the feature." (3) `is_allowed()` resolver at lines 119-124 defensively handles missing `automation` and missing `destructive_actions` attributes — `if automation is None: return False` (line 121) and `if raw is None: return False` (line 124); docstring at lines 108-111 cites "default-deny outranks the convenience of a missing-section pass." Matches CLAUDE.md "no silent failures" floor. No code change required this run; the (a)-half shipped by worker-10am 2026-05-09 already targets the default-deny posture per the spec recommendation that Eric's A answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B3 destructive-action allow-list — match by exact name, glob (fnmatch), or regex?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b3-destructive-action-allowlist-spec.md`. `destructive_actions = ["send_hotkey"]` could match exactly, or via glob (`["send_*"]`), or via regex. More expressive matchers help forward-compat at the cost of leak-forward-in-time risk.
  - What I tried: thought through the failure mode for each. Glob `send_*` would automatically allow a future `send_text` tool that didn't exist when the user wrote the toml — exactly the leak the safety primitive should prevent. Regex compounds the issue. Exact match forces re-audit on every new tool — that re-audit IS the design intent.
  - What I need from you: pick one (or override).
    - (a) Exact match (`name == entry`). Each new destructive tool requires explicit allow-list entry. Tightest posture.
    - (b) Glob match (`fnmatch.fnmatchcase`). More expressive; "I allow all keyboard tools" is one entry. Cost: leaks forward in time.
    - (c) Regex match (`re.fullmatch`). Most expressive. Cost: complex; users learn regex; leaks even more.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) or (c) before pull, Worker reroutes.
  **[answered: A 2026-05-15 via dashboard]** — Forcing a per-tool re-audit on every new destructive verb is precisely what the safety primitive should buy.
  **[resolved: 2026-05-17 by worker-sprint-6]** — Ratified A: in-tree `is_allowed()` at `sabrina-2/src/sabrina/automation/allow_list.py:128` uses `return spec.name in raw` — exact-string membership check against the user's `destructive_actions` list. No `fnmatch.fnmatchcase()`, no `re.fullmatch()`, no glob/regex pre-processing. Module docstring at lines 24-27 cites "Exact-match-only resolution (spec Q2 (a)): no glob, no regex. Each new destructive tool requires its own explicit entry, so the security posture doesn't leak forward in time when a future destructive tool ships under a name a pre-existing glob would have happily matched." Comment at lines 125-127 reinforces: "Exact match only (spec Q2 (a)). Iterating preserves the order the user wrote the list in (no set conversion) so a callable downstream that wants the explicit allow-list can read it." Each new destructive tool requires its own explicit entry — the deliberate-audit-per-tool feature Eric's A pick locks in. No code change required this run; the (a)-half shipped by worker-10am 2026-05-09 already targets exact-match-only per the spec recommendation. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-09 06:50] P4.B3 destructive-action allow-list — explicit `destructive=False` on existing `write_clipboard` ToolSpec, or omit and rely on Pydantic default?
  - Context: Spec at `rebuild/drafts/research/2026-05-09-p4b3-destructive-action-allowlist-spec.md`. The new `ToolSpec.destructive: bool = False` field defaults to False; no behavior change is needed for existing specs to keep working. Question is whether to update the existing `write_clipboard` constructor in `_builtin_tools()` to explicitly pass `destructive=False` for documentation, or rely on the default.
  - What I tried: read the existing `_builtin_tools()` factory in `sabrina-2/src/sabrina/tools/__init__.py:79-105`. It uses an explicit constructor with all four current fields named (no defaults relied upon for `name`/`description`/`input_schema`/`handler`). Adding a fifth named field is consistent with the existing style.
  - What I need from you: pick one (or override).
    - (a) Explicit `destructive=False` on `write_clipboard`. Self-documenting; future readers see the field; implies the author considered the question.
    - (b) Omit, rely on Pydantic default. Shorter; convention-over-configuration.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes.
  **[answered: A 2026-05-15 via dashboard]** — Existing `_builtin_tools()` factory names all four fields explicitly; a fifth named field is consistent with the established style.
  **[resolved: 2026-05-17 by worker-sprint-7]** — Ratified A: in-tree `_builtin_tools()` at `sabrina-2/src/sabrina/tools/__init__.py:98-123` explicitly passes `destructive=False` (line 123) on the `write_clipboard` `ToolSpec` constructor; comment at lines 120-122 cites "P4.B3 Q3 (a): explicit `destructive=False` for documentation; `write_clipboard` is non-destructive (clipboard contents are [user-recoverable])." Self-documenting fifth named field consistent with the existing constructor pattern (`name=`, `description=`, `input_schema=`, `handler=`, `destructive=` — all explicit). Future readers see the field and understand the author considered the destructive-action question rather than relying on the Pydantic default firing silently. No code change required this run; the (a)-half shipped by worker-10am 2026-05-09 already targets explicit-`destructive=False` per the spec recommendation that Eric's A answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-12 06:50] P4.A1 avatar architecture research — Live2D SDK pick: `live2d-py` community wrapper, official Cubism SDK Python wrapper, or roll-our-own with OpenGL?
  - Context: Spec at `rebuild/drafts/research/2026-05-12-p4a1-avatar-architecture-research-spec.md`. P4.A1 produces the research doc grounding the Avatar component (v1.0 Gate 1 mover #9). §1 of the research doc compares three SDK candidates against license, maintenance posture, Windows-x64 install path, and render path. The Worker doing A1 will document trade-offs against all three; this NEEDS-INPUT is the load-bearing pick Eric will make once the research lands.
  - What I tried: read `rebuild/ROADMAP.md` §6 (Avatar) for the design statement; read `rebuild/drafts/research/2026-04-26-avatar-cue-track-implementation.md` for the cue-track surface A3 will eventually drive; confirmed the daily-driver bar is "reacts to state, doesn't look broken" rather than "production VTuber app." That bar collapses the SDK question toward the lowest-friction install.
  - What I need from you: pick one (or override).
    - (a) `live2d-py` (community wrapper around Cubism Native SDK). pip-installable; smaller scope; license inheritance from upstream Cubism SDK applies. Spec recommends this.
    - (b) Official Cubism SDK Python wrapper. Higher fidelity; download-gated from Live2D Inc; license tiers irrelevant for personal-use daily-driver but the install path is heavier.
    - (c) Roll-our-own with OpenGL + texture-atlas + raw Moc3 parser. Do-not-recommend baseline included for delta-naming.
  - Recommendation: (a). Worker can ship the A1 research doc against (a) without waiting; if you pick (b) or (c) before A3 is pulled, the A3 spec retargets per the answer (A2 is independent of this pick).
  **[answered: A 2026-05-15 via dashboard]** — The daily-driver bar is "reacts to state, doesn't look broken" — pip-installable beats download-gated for that bar.
  **[resolved: 2026-05-17 by worker-sprint-8]** — Ratified A: P4.A1 research doc at `rebuild/drafts/research/2026-05-14-avatar-architecture.md` §1 (lines 25-37) shipped by worker-8am 2026-05-14 already recommends `live2d-py` 0.6.x community wrapper per the same four-axis comparison Eric's A answer endorses. Trade-off table: license (MIT wrapper + Cubism Core DLL under Live2D Free Material License — personal daily-driver bar satisfied), maintenance (active; 0.6.1.1 shipped 2026-01-16, Cubism 4+5 supported), Windows-x64 install (`pip install live2d-py` with cp310-cp313 wheels + Cubism Core DLL shipped alongside; no Visual Studio toolchain), render path (C-extension wrapping Native SDK in C++, OpenGL-3.3+ context, drives `LAppModel.update()`/`LAppModel.draw()` from host paint loop). Auto-blink + auto-breath are first-class on `live2d.LAppModel` per the doc, collapsing P4.A3's ambient-layer code. P4.A2's spec (`rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md`) is built against this SDK pick. P4.A3's spec (`rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md`) targets `live2d-py`'s sample-asset URL (Hiyori_pro placeholder). The "Eric's pick" line at research doc §1 line 37 currently reads `_TBD — see NEEDS-INPUT.md line 195 (spec-writer / 2026-05-12 06:50)._` — this `[resolved:]` marker fills in the pick: A → `live2d-py`. No code change required this run; the research doc shipped to DONE 5/14 already targets the recommendation per the spec — Eric's answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification; downstream A2 + A3 specs unaffected) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-12 06:50] P4.A1 avatar architecture research — click-through policy: full window click-through (no opaque regions), or per-pixel opaque-regions-accept-clicks via Windows native flags?
  - Context: Spec at `rebuild/drafts/research/2026-05-12-p4a1-avatar-architecture-research-spec.md`. §2 of the research doc documents the PyQt6 frameless / always-on-top / click-through cookbook on Windows 11. The canonical Qt-only path is `WA_TransparentForMouseEvents` (full window click-through, no opaque regions). The Windows-native alternative is `SetWindowLong` + `WS_EX_LAYERED` + `WS_EX_TRANSPARENT` to achieve per-pixel transparency where opaque regions accept clicks (e.g. a kill button on the avatar itself).
  - What I tried: read `rebuild/ROADMAP.md` §6 for the design statement ("frameless/always-on-top/click-through"); the original design statement is silent on whether the avatar window is itself a kill-button target. Read `sabrina-2/src/sabrina/cli.py` for an existing `sabrina kill` or supervisor-attached kill verb (none today; supervisor is the kill target, not the avatar). Concluded the simpler full-click-through path is sufficient for v1 since kill targets exist elsewhere.
  - What I need from you: pick one (or override).
    - (a) Full window click-through (`WA_TransparentForMouseEvents` always on). Simplest; smallest Windows-native surface; no per-pixel transparency code needed. Kill-button lives elsewhere (system tray, supervisor CLI, or global hotkey from P4.B1's kill-switch). Spec recommends this.
    - (b) Per-pixel opaque-regions-accept-clicks via Windows native flags. Avatar window can host kill-button or other interactive regions. Cost: Windows-only validation, more fragile path, requires ctypes or pywin32 reach.
  - Recommendation: (a). Worker can ship the A1 research doc against (a) without waiting; if you pick (b) before A2 is pulled, A2's spec retargets per the answer (A2's window flags change).
  **[answered: A 2026-05-15 via dashboard]** — Kill targets already exist outside the avatar window; per-pixel transparency buys nothing v1 needs and adds a Windows-only fragile surface.
  **[resolved: 2026-05-17 by worker-9am]** — Ratified A: P4.A1 research doc at `rebuild/drafts/research/2026-05-14-avatar-architecture.md` §2 (lines 41-63) shipped by worker-8am 2026-05-14 already recommends the full-window-click-through path per Eric's A answer. §2 "Concern table" (lines 45-52) names the canonical Qt-only recipe — `Qt.WindowType.FramelessWindowHint | WindowStaysOnTopHint | Tool | WindowTransparentForInput` flags plus `WA_TranslucentBackground` + `WA_NoSystemBackground` attributes plus `QSurfaceFormat.setAlphaBufferSize(8)` on the default surface format BEFORE `QApplication()` instantiates (the load-bearing gotcha from `2026-04-26-avatar-cue-track-implementation.md:103-118`). §2 architecture compare at lines 54-57 contrasts (a) full-click-through (canonical Qt-only path, no `ctypes`/`pywin32`, kill targets live out-of-band in system-tray + supervisor CLI + P4.B1 kill-switch hotkey) against (b) per-pixel-opaque-clicks (Windows-native `SetWindowLong` + `WS_EX_LAYERED | WS_EX_TRANSPARENT`, requires `setWindowFlag()` + `show()` re-show dance on Win11); the §2 recommendation block at lines 61-62 commits to (a) on the same daily-driver-bar + kill-paths-exist-out-of-band reasoning Eric's A answer endorses. P4.A2's PyQt6-skeleton spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md:37` already targets the (a) shape: `AvatarWindow.__init__` applies `setAttribute(Qt.WA_TranslucentBackground)` + `setAttribute(Qt.WA_TransparentForMouseEvents)` per "the full click-through path per P4.A1 §2 recommendation"; the spec's line-55 `test_avatar_window_flags_applied_at_init` asserts both attributes True under `QT_QPA_PLATFORM=offscreen`; line-135 bailout-condition reinforces "Not a click-through-with-opaque-buttons implementation. Per P4.A1 §2 recommendation (a), the entire window is click-through." The "Eric's pick" line at research doc §2 line 63 + §5 line 115 currently reads `_TBD — see NEEDS-INPUT.md line 204 (spec-writer / 2026-05-12 06:50). Spec-recommends (a) full window click-through (`WA_TransparentForMouseEvents` always on)._` — this `[resolved:]` marker fills in the pick: A → full-window-click-through. No code change required this run; the research doc shipped to DONE 5/14 already targets the recommendation per the spec and downstream P4.A2 spec is unaffected by Eric's A pick. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention, mirroring worker-sprint-8 2026-05-17 ratify-only at line 240 + worker-8am 2026-05-17 ratify-only at line 157) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-12 06:50] P4.A1 avatar architecture research — IPC architecture: in-process subscriber via `qasync`, or side-process subscriber via local IPC?
  - Context: Spec at `rebuild/drafts/research/2026-05-12-p4a1-avatar-architecture-research-spec.md`. §3 of the research doc compares in-process avatar (Qt event loop co-exists with asyncio via `qasync`; direct `bus.subscribe`) against side-process avatar (separate Python process, IPC via local TCP / named pipe / stdin-stdout JSONL). Crash isolation vs. IPC latency vs. process management overhead.
  - What I tried: re-read `sabrina-2/src/sabrina/bus.py:31-83` (in-process subscriber API; lossy-on-backpressure; well-suited to a low-stakes consumer). Read `sabrina-2/src/sabrina/supervisor.py` to confirm the supervisor primitive already shipped in `acd6725` provides crash-isolation at the process level via auto-restart — that solves the strongest argument for side-process IPC cheaply. Confirmed `qasync` is well-maintained on PyPI with Windows wheel coverage.
  - What I need from you: pick one (or override).
    - (a) In-process via `qasync`. Avatar runs alongside voice loop in one process. Direct `bus.subscribe`. Pro: zero IPC overhead, simple. Con: Qt event loop must co-exist with asyncio (handled by qasync). Spec recommends this.
    - (b) Side-process via local IPC (TCP / named pipe / JSONL). Avatar runs in own Python process. Pro: crash isolation, independent restart. Con: ~1ms-class IPC latency, additional process management, two venv-or-wheel surface.
  - Recommendation: (a). Worker can ship the A1 research doc against (a) without waiting; if you pick (b) before A2 is pulled, A2's spec retargets substantially (file partition changes — see P4.A2 spec "Bailout conditions").
  **[answered: A 2026-05-15 via dashboard]** — Supervisor primitive (acd6725) already provides process-level crash isolation cheaper than IPC; in-process keeps the bus contract single-shape.
  **[resolved: 2026-05-17 by worker-sprint-3-1]** — Ratified A: P4.A1 research doc at `rebuild/drafts/research/2026-05-14-avatar-architecture.md` §3 (lines 67-86) shipped by worker-8am 2026-05-14 already recommends in-process via `qasync` per Eric's A answer. §3 axes table (lines 71-78) contrasts (a) in-process — zero IPC latency, direct `bus.subscribe("state_changed")` async-generator, shared asyncio + Qt event loop via `qasync`, crash-isolation deferred to the already-shipped supervisor primitive (`acd6725` `sabrina-2/src/sabrina/supervisor.py`'s `run_supervised` provides process-level auto-restart) — against (b) side-process — ~1 ms UDP/JSON IPC, strong crash isolation, independent restart story. §5 "Eric's pick checklist" line 116 currently reads `_TBD — NEEDS-INPUT.md line 212 (spec-writer / 2026-05-12 06:50). Spec-recommends (a) in-process via `qasync`._`; this `[resolved:]` marker fills in the pick: A → in-process via qasync. §5 line 126 commits to the (a) shape architecturally. P4.A2's PyQt6-skeleton spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md` is built against `qasync` runtime ($. `[project.optional-dependencies].avatar`); P4.A2's libEGL-gated (a)-half pickup (per worker-9am 5/14 finding at NEEDS-INPUT.md:301) inherits the in-process design verbatim — no spec retarget triggered by this pick. No code change required this run; the research doc shipped to DONE 5/14 already targets the recommendation per the spec and downstream P4.A2 + P4.A3 specs are unaffected by Eric's A pick. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention, mirroring worker-9am 2026-05-17 at line 250 + worker-sprint-8 2026-05-17 at line 240) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-12 06:50] P4.A2 avatar PyQt6 skeleton — dependency tier for PyQt6 + qasync: core dep, optional extra, or vendored lazy-import?
  - Context: Spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md`. P4.A2 adds `PyQt6` (~50 MB wheel) + `qasync` as runtime deps. Question is whether these are core (every install gets them) or optional (`uv pip install -e .[avatar]`).
  - What I tried: read both `pyproject.toml` files (project root + `sabrina-2/`) to confirm the `[project.optional-dependencies]` pattern is supported; counted current optional extras (none yet — the avatar would be the first). Confirmed `rebuild/ROADMAP.md` §198 lists Avatar as "(nice-to-have)" which biases optional. Considered the install-time cost: 50 MB on a daily-driver target is irrelevant; on the Cowork sandbox (which runs Worker tests) it doubles `pip install` time but only once.
  - What I need from you: pick one (or override).
    - (a) Core dep in `[project.dependencies]`. Simplest; every `sabrina voice` install gets the avatar binary regardless. Cost: ~50 MB extra wheel on every box.
    - (b) Optional extra under `[project.optional-dependencies].avatar`. Install via `uv pip install -e .[avatar]`. Pure voice-loop users skip the install. Spec recommends this — matches "nice-to-have" framing.
    - (c) Vendored / lazy-imported: ship `PyQt6` as a runtime check inside `avatar/__init__.py` with a helpful "uv pip install sabrina[avatar]" error message. Cost: more code; weirder failure surface.
  - Recommendation: (b). Worker can ship the (a)-half against (b) without waiting; if you pick (a) or (c) before pull, Worker reroutes (the `pyproject.toml` edit is a single section move).
  **[answered: B 2026-05-15 via dashboard]** — Avatar is explicitly nice-to-have in ROADMAP §6; gating the 50 MB wheel behind an extra keeps the voice-loop minimal install honest.
  **[resolved: 2026-05-17 by worker-sprint-3-2]** — Ratified B: P4.A2 spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md` Q1 (line 109-111) already recommends (b) optional extra per Eric's B answer. Spec lines 67-68 commit to the file-partition shape: `pyproject.toml` (project root) adds `PyQt6` + `qasync` to `[project.optional-dependencies].avatar` (**NOT** `[project.dependencies]`); `sabrina-2/pyproject.toml` mirrors the addition. Install path per spec: `uv pip install -e .[avatar]` (or `.[avatar]` from project root) — pure `sabrina voice` users skip the 50 MB PyQt6 wheel + ~1 MB qasync. Aligns with `rebuild/ROADMAP.md` §6 "(nice-to-have)" framing on the Avatar component. P4.A2 (a)-half pickup remains blocked on the libEGL pick at NEEDS-INPUT.md:301 (worker-9am 5/14's finding); when the bailout path resolves, the optional-extra wiring is the codified scope. No code change required this run; the (a)-half pickup hasn't happened yet (libEGL block), so the `pyproject.toml` edit happens inside that pickup against the spec's recommendation Eric's B answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-12 06:50] P4.A2 avatar PyQt6 skeleton — headless test platform: `QT_QPA_PLATFORM=offscreen`, xvfb virtual display, or skip Qt tests in headless?
  - Context: Spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md`. The 12-test suite exercises `AvatarWindow` instantiation + window flags + event-bus subscriber. PyQt6 needs a display backend; the Cowork Linux/3.10 sandbox has neither X11 nor Wayland. Three approaches.
  - What I tried: read `sabrina-2/tests/conftest.py` (per P5.5 Worker entry) for the existing `requires_gpu` / `requires_windows` marker pattern. Considered adding a `requires_display` marker; concluded that defers the gate to Windows and skips 9 of 12 tests in the sandbox, which is acceptable but not preferable. Confirmed PyQt6 ships with offscreen platform plugin by default — no extra wheel needed.
  - What I need from you: pick one (or override).
    - (a) `QT_QPA_PLATFORM=offscreen`. PyQt6's built-in offscreen plugin. No X11 server. Sandbox-friendly. Spec recommends this.
    - (b) `xvfb-run` virtual X11 display. Heavier; depends on `xvfb-run` being on the test host (not guaranteed in Cowork sandbox).
    - (c) Skip Qt tests in headless via `requires_display` marker (P5.5 precedent). Defers 9 of 12 tests to Windows.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) or (c) before pull, Worker reroutes (conftest fixture and CI invocation change).
  **[answered: A 2026-05-15 via dashboard]** — Offscreen plugin ships with PyQt6; full test coverage in the sandbox with zero infra setup is the cheapest path.
  **[resolved: 2026-05-17 by worker-sprint-3-3]** — Ratified A: P4.A2 spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md` Q2 (lines 113-116) already recommends (a) `QT_QPA_PLATFORM=offscreen` per Eric's A answer. Spec line 16: "The Qt window can be exercised headlessly under `QT_QPA_PLATFORM=offscreen`." Line 44 wires `runner.main()` to set `QT_QPA_PLATFORM=offscreen` when `SABRINA_AVATAR_HEADLESS=1` (test entry point); production Windows runs use the platform default. Spec line 101 DoD point 11 names the gate: `pytest sabrina-2/tests/test_avatar_skeleton.py -v` passes 12/12 under the Cowork Linux/3.10 sandbox with `QT_QPA_PLATFORM=offscreen` exported. Sub-tests at lines 55+66 instantiate `AvatarWindow` under offscreen; `test_avatar_window_flags_applied_at_init` asserts the four window flags + attributes apply correctly without a display server. Spec rules out (b) xvfb — heavier; not guaranteed in Cowork sandbox; and (c) `requires_display` skip-marker — defers 9 of 12 tests to Windows (vs. (a) running all 12 in sandbox). P4.A2 (a)-half pickup remains blocked on the libEGL pick at NEEDS-INPUT.md:301 (worker-9am 5/14's finding); libEGL is the runtime dep that makes `QT_QPA_PLATFORM=offscreen` work in the sandbox — once Eric picks the libEGL bail/shim/skip option, the offscreen test infrastructure is the codified target. No code change required this run; the (a)-half pickup hasn't happened yet (libEGL block), so the test-harness wiring happens inside that pickup against the spec's recommendation Eric's A answer ratifies. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-13 06:50] P4.A3 avatar Live2D rig bind — per-state expression-slot binding: hard-coded constant in `dispatcher.py`, `[avatar.expressions]` toml block, or JSON sidecar file?
  - Context: Spec at `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md`. The `StateDispatcher` triggers a Live2D expression slot per state (e.g. `acting` → `concerned`, `thinking` → `focused`). Question is where the per-state → expression-name map lives. The same question applies one layer up to the per-state `PARAM_MAP` (param-name → target-value snapshots); the spec's recommendation collapses both behind the same answer for consistency.
  - What I tried: read the `BUILTIN_TOOLS` pattern in `sabrina-2/src/sabrina/brain/tools.py` (single-source-of-truth constant; Eric edits code to retune). Read `[brain.persona]` block in `sabrina.toml` (P4.C1 precedent — Pydantic model + four toml-parsed bool fields). Read the April cue-track research's `Cue` dataclass + dispatcher sketch (parameter values entered as data, not code). Concluded a hard-coded constant matches the `BUILTIN_TOOLS` simplicity and lets Eric retune via a single-file edit; the toml route adds a Pydantic model + parsing without buying retunability that matters at this stage.
  - What I need from you: pick one (or override).
    - (a) Hard-coded constant in `dispatcher.py`. Eric edits code to retune. Simplest. Spec recommends this.
    - (b) `[avatar.expressions]` + `[avatar.params]` toml blocks parsed via Pydantic models. Eric retunes without code changes. Cost: ~30 lines of model + toml.
    - (c) JSON sidecar file under `sabrina-2/configs/avatar_expressions.json`. Worst of both worlds; ruled out by the spec.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes (adds two Pydantic models + toml blocks; param-map and expression-map move to `Settings.avatar.params` / `Settings.avatar.expressions`; `dispatcher.py` reads from settings instead of module-level constants).
  **[answered: A 2026-05-15 via dashboard]** — Retunability without a code edit isn't a v1 need yet; `BUILTIN_TOOLS`-style constant matches the existing Eric-edits-code precedent.
  **[resolved: 2026-05-17 by worker-sprint-3-4]** — Ratified A: P4.A3 spec at `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md` Q1 (lines 147-148) already recommends (a) hard-coded constant in `dispatcher.py` per Eric's A answer. Spec line 41 commits to the file partition: `sabrina-2/src/sabrina/avatar/dispatcher.py` (new, ~160 lines) hosts the `PARAM_MAP` and `EXPRESSION_MAP` module-level constants — Eric edits the file to retune. Matches the existing `BUILTIN_TOOLS` precedent in `sabrina-2/src/sabrina/tools/__init__.py` (single-source-of-truth constant; no toml round-trip; no Pydantic model). Spec rules out (b) `[avatar.expressions]` + `[avatar.params]` toml blocks (~30 LOC of Pydantic models + parsing without buying retunability that matters at v1 stage) and (c) JSON sidecar file ("worst of both worlds"). P4.A3 (a)-half pickup is downstream of P4.A2's libEGL bail-resolution (per spec line 9: "P4.A3 replaces A2's placeholder `QLabel` with a real Live2D rig surface; everything else A2 built ... stays in place"); when A2 unblocks, A3's `dispatcher.py` hard-coded `PARAM_MAP` + `EXPRESSION_MAP` constants are the codified target. No code change required this run; downstream-gated by P4.A2 libEGL pick. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention) (partial-dod, awaits-windows-e2e).

- [ ] [from: spec-writer / 2026-05-13 06:50] P4.A3 avatar Live2D rig bind — placeholder Hiyori_pro rig commit policy: runtime-only (Eric drops in manually), direct git-LFS commit, or runtime download on first run?
  - Context: Spec at `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md`. The Live2D rig files (`.model3.json` + textures + motions, ~3 MB total for Hiyori_pro from `live2d-py`'s sample assets) are binary and per-machine. Question is whether they live in git or arrive at install time.
  - What I tried: read the P2.1 wake-word research's `hey_jarvis` placeholder pattern — model file is excluded from git via `sabrina-2/models/openwakeword/` `.gitignore` rule and Eric drops it in manually per the README. Read P2.2 wake-training README for the WSL2-side artifact-promotion step (same pattern: artifact lands manually outside git). Confirmed the existing root-level `.gitignore` line 89 (per P2.2 worker-8am 2026-05-08's edit) already excludes `sabrina-2/models/` content broadly; the rig would be excluded automatically under (a) with no `.gitignore` edit needed. Considered git-LFS: not currently set up in this repo, and the daily-driver setup script (P5.3) does not handle LFS init.
  - What I need from you: pick one (or override).
    - (a) Runtime-only; the rig path in `[avatar] model_path` is informational. Eric downloads from `live2d-py`'s sample-asset URL and drops the files into `sabrina-2/models/live2d/` outside git. Matches the `hey_jarvis` precedent. Spec recommends this.
    - (b) Direct commit under git-LFS. Cost: git-LFS setup + ~3 MB binary in the repo. Benefit: clone+run works without an Eric-side step.
    - (c) Runtime download from `live2d-py`'s sample-asset URL on first `sabrina avatar run`. Cost: network dependency at first run; URL bit-rot risk. Ruled out by the spec.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting; if you pick (b) before pull, Worker reroutes (P5.3 setup script grows a `git lfs install` probe; .gitattributes file lands; placeholder rig commits under LFS).
  **[answered: A 2026-05-15 via dashboard]** — Wake-word `hey_jarvis` precedent already establishes the manual-drop pattern; `.gitignore` exclusion needs no edit.
  **[resolved: 2026-05-17 by worker-sprint-3-5]** — Ratified A: P4.A3 spec at `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md` Q2 (lines 152-153) already recommends (a) runtime-only per Eric's A answer. Spec line 97 (.gitignore section): "leave alone (Eric drops the rig in manually, model file kept out of git via existing `models/` exclusion at root). **Recommendation: leave .gitignore alone.**" The placeholder model_path at `sabrina-2/models/live2d/hiyori_pro/runtime/hiyori_pro_t11.model3.json` (spec line 63 default for `[avatar].model_path`) is informational; the rig file is sourced from `live2d-py`'s sample assets at install time, not committed. Matches the `hey_jarvis` wake-word precedent at `sabrina-2/models/openwakeword/` where the model file is excluded from git via the project-root `.gitignore` `sabrina-2/models/` rule + `sabrina-2/.gitignore` exclusion; Eric drops it in manually per the README. Verified the project-root `.gitignore` line 89 (worker-8am 2026-05-08's edit per P2.2 trail at QUEUE.md:78) already broadly excludes `sabrina-2/models/` contents; the Live2D placeholder rig would be excluded automatically under (a) with no `.gitignore` edit needed. Spec rules out (b) direct git-LFS commit (LFS not currently set up; daily-driver setup script P5.3 does not handle LFS init) and (c) runtime download from `live2d-py`'s sample-asset URL on first `sabrina avatar run` (network dependency at first run; URL bit-rot risk). P4.A3 (a)-half pickup is downstream of P4.A2's libEGL bail-resolution; when A2 unblocks, A3's runtime-only rig path is the codified target — Eric's drop-the-rig-in-place sequence happens once on his Windows session before the first `sabrina avatar` invocation. No code change required this run; downstream-gated by P4.A2 libEGL pick. Doc-only state-file edit on NEEDS-INPUT.md (informational ratification per Worker convention) (partial-dod, awaits-windows-e2e).

- [ ] [from: worker-9am / 2026-05-14 09:00] P4.A2 (a)-half — PyQt6 wheel installs but `libEGL.so.1` is missing from the Cowork Linux/3.10 sandbox, so the Qt platform-offscreen plugin can't load. Spec bailout #1 fires; ask before adding a non-trivial sandbox shim.
  - Context: P4.A2 spec at `rebuild/drafts/research/2026-05-12-p4a2-avatar-pyqt-skeleton-spec.md` § DoD #11 names this exact case: "If PyQt6 is not pip-installable into the sandbox (Python 3.10 + sandbox restrictions), the entire P4.A2 (a)-half retargets to Eric's Windows session as a `[windows-required]` item — see Bailout conditions." § Bailout #1: "PyQt6 doesn't install into the Cowork Linux/3.10 sandbox … File a NEEDS-INPUT entry naming the install error and bail." Today's chain so far: 02:37 night-auditor + 03:30 researcher + 06:50 spec-writer idle + 07:00 planner + 07:30 graduation-sweeper (#37 + #38 auto-graduated) + 08:00 worker-8am (P4.A1 research doc shipped to DONE). Worker-9am is the first (a)-half pickup slot.
  - What I tried: `python3 -m pip install --user PyQt6 qasync` succeeded (rc=0). Import test `from PyQt6.QtWidgets import QApplication` under `QT_QPA_PLATFORM=offscreen` raised `ImportError: libEGL.so.1: cannot open shared object file`. `sudo` blocked by `no_new_privs` flag (Cowork sandbox container posture); `apt-get install -y libegl1` denied (`Permission denied` on `/var/lib/dpkg/lock-frontend`). Workaround that DOES work: `apt-get download libegl1 libegl-mesa0`, `dpkg-deb -x` each into `~/.local/libegl-extract/`, prepend that path to `LD_LIBRARY_PATH` — verified that `QApplication([])` then instantiates cleanly under offscreen.
  - What I need from you: pick one (or override). The strict-spec read is bail; the pragmatic read is ship with a sandbox shim. The work is small either way once the path is picked.
    - (a) Bail to Windows. P4.A2 (a)-half retargets fully to Eric's next Windows session; no Linux Partial-DoD ship. Matches the spec's bailout letter and avoids adding a sandbox-only system-dep extraction step. Cost: P4.A2 sits idle until Saturday-style session; A3 stays blocked behind A2. **Spec recommends this.**
    - (b) Add a sandbox shim to `sabrina-2/tests/conftest.py` (precedent: worker-10am 2026-05-05's `sounddevice` stub for Linux sandboxes that lack PortAudio). The shim would: detect `libEGL.so.1` is missing in `pytest_configure`, `apt-get download` + `dpkg-deb -x` libegl1 + libegl-mesa0 into `~/.local/sabrina-sandbox-libs/`, prepend to `LD_LIBRARY_PATH` for the test process. Ship the (a)-half cleanly with this shim. Cost: ~25-line additive `conftest.py` block, sandbox-only path, runs once per test session; the shim's `apt-get download` adds a network dependency the prior `sounddevice` stub didn't have. Risk: if Cowork sandbox blocks outgoing apt access in a future hardening, the shim breaks. Production Windows runs never hit this path (`shutil.which("apt-get")` returns None).
    - (c) Skip Qt tests on Linux via `requires_display` pytest marker (precedent: `requires_gpu` + `requires_windows` in conftest from worker-10am 2026-05-05). Ship the (a)-half code + tests; tests are auto-skipped on Linux; Windows runs them in P4.A4. Cost: nine of the twelve tests in `test_avatar_skeleton.py` skip on Linux, leaving only the offscreen-free pure-Python tests (config defaults + config round-trip + maybe one more). Linux Partial-DoD is much thinner than the spec assumed.
  - Recommendation: spec recommends (a) per its own bailout #1 letter. (b) is the highest-leverage pragmatic choice if you want A2 to ship today. (c) is the cleanest middle ground but leaves most of the test coverage on the Windows side. Worker is `[in-progress]` on P4.A2 pending this pick; if you override before the next Worker fires, the next pickup reroutes per the answer.

- [ ] [from: spec-writer / 2026-05-15 13:00] P2.5 autostart validation — Step 5 XML-template tightening scope: ship `RestartOnFailure` opt-in only, all six follow-ups, or skip entirely?
  - Context: Spec at `rebuild/drafts/research/2026-05-15-p25-autostart-validation-spec.md`. P2.5 is `[windows-required]`: the autostart install/uninstall implementation already shipped in `acd6725` (`supervisor.py:160-193` Task Scheduler XML template + `cli.py:1151–1241` `sabrina autostart {enable,disable,status}` verbs). Remaining scope is one Eric Windows session validating the at-logon flow plus a decision yes/no on the six XML-template follow-ups researcher 2026-05-06 03:30 surfaced in `research/2026-05-06-autostart-approach.md` §"Open follow-ups". Spec Step 5 ranks them by Eric-effort and defect-class severity; only #1 (`RestartOnFailure`) prevents a recovery-failure class (supervisor itself crashing → no recovery). The other five are cosmetic / paranoia / future-flexibility.
  - What I tried: read `supervisor.py:160-193` for the current `_TASK_XML_TEMPLATE` (confirmed: no `<RestartOnFailure>` block, `<Priority>7</Priority>` below-normal, `<Hidden>false</Hidden>`, no `<RunOnlyIfIdle>`); read `research/2026-05-06-autostart-approach.md` §"Open follow-ups" for the six-item menu; cross-referenced with P2.6 supervisor validation spec at `rebuild/drafts/research/2026-05-07-p26-supervisor-validation-spec.md` Q1 — same question shape, same (a)-recommendation, P2.6 spec-writer 2026-05-07 06:50 recommends (a) for the same reasons.
  - What I need from you: pick one (or override).
    - (a) Apply only `RestartOnFailure` (follow-up #1). ~4 LOC inside `<Settings>` + 1 unit test (`test_task_scheduler_xml_includes_restart_on_failure`) + 1 evidence line in the decision doc. ~30 minutes of session time on top of the validation. **Spec recommends this.**
    - (b) Apply all six follow-ups in one pass. `RestartOnFailure` + `Priority` audit + `UserId` validation (inline in Step 4 already) + `Hidden` flip + `RunOnlyIfIdle` paranoia test + task-name knob exposure. ~200 LOC across XML template + `SupervisorConfig` + tests + `[supervisor].task_name` field. ~2 hours; closes the entire researcher follow-up list under one decision doc.
    - (c) Skip all six (Step 4 `UserId` check still runs inline); ship validation alone. Zero code change. Decision doc is purely validation results. Follow-ups become a separate P2.5.1 item if/when defect classes surface.
  - Recommendation: (a). The other five follow-ups are defensible deferrals — each fixes a "what if" with no current evidence of the underlying class. The diff in (a) rides along on the decision-doc commit; the Windows session that validates Steps 1-4 absorbs it with no additional re-validation cycle. Worker can't pull this anyway (`[windows-required]`); the question only matters for Eric's session prep. Spec recommendation aligns byte-for-byte with the P2.6 sibling Q1 (a)-recommendation per the bundle-Saturday pattern.

- [ ] [from: spec-writer / 2026-05-15 13:00] P2.5 autostart validation — decision doc number: next free integer or bundle with P2.6 supervisor decision doc?
  - Context: Spec at `rebuild/drafts/research/2026-05-15-p25-autostart-validation-spec.md` Step 6. P2.5 produces a decision doc ratifying Task Scheduler (over Run key / NSSM / shell:startup). The flat-numbering precedent in `rebuild/decisions/` has held through 010 (personality spec); ROADMAP §"Phase 6" item #6 reserves 011 for the v1.0 release decision doc. Mirrors P2.6 spec-writer 2026-05-07 06:50's Q2 — same shape, same (a)-recommendation; if both Q2s answer (a), P2.5 + P2.6 take adjacent free integers (012 + 013 or vice versa).
  - What I tried: read `rebuild/ROADMAP.md` §"Phase 6" to confirm the 011 reservation is canonical; read `rebuild/decisions/` 003 (voice-loop-shipped) + 008 (foundational-refactor-bundle) for the candidate parents; cross-referenced with `supervisor-autostart-plan.md`'s umbrella framing where autostart + supervisor were treated as one design unit.
  - What I need from you: pick one (or override).
    - (a) Next free integer in `rebuild/decisions/`, excluding 011 (v1.0 reservation). Eric picks the number at filing time. Maintains the flat-numbering precedent. **Spec recommends this.**
    - (b) Sub-number under decision 003 (voice-loop-shipped) — treats autostart as a 003 follow-up since it wraps the voice-loop launch. Breaks the flat-numbering precedent.
    - (c) Bundle with the P2.6 supervisor decision doc into one combined "supervisor + autostart" doc. Mirrors `supervisor-autostart-plan.md`'s umbrella framing. Couples P2.5 + P2.6 ship cadence — if Eric's Saturday has time for only one, the doc has to split or one waits.
  - Recommendation: (a). Flat-numbering is load-bearing; (b) would be the first deviation. Bundling (c) appealing but unnecessarily couples — Eric should be able to ship P2.5 + P2.6 independently if his Saturday session has time for only one; the two decision docs can cross-reference. Worker-blocker status: none (P2.5 is `[windows-required]`); the answer only matters at decision-doc filing time.

- [ ] [from: researcher / 2026-05-16 03:30] What bounded question should the Researcher investigate next? — no `[research]`-tagged items queued; the unanswered P0/P1 questions in today's NEEDS-INPUT don't need additional grounding beyond what's already documented.
  - Context: per `roles/researcher.md` step 1, I scan for `[research]`-tagged items or P0/P1 questions that need grounding. QUEUE.md returns zero `[research]` tags (Grep-verified). The 6 Answer-batch follow-ups + 12 lock-blocked Linux Partial-DoD diffs are all spec-grounded. The P4.A2 libEGL pick at line 284 has all three options already evidenced by worker-9am 5/14's investigation (Eric needs to pick, not more research); the two P2.5 spec-writer 5/15 06:50 questions at lines 293+304 are spec-recommended Windows-required for Eric's Saturday session prep. The 23 ripe dashboard `[answered:]` markers (per 5/16 02:37 night-auditor) are largely ratify-only — Worker per-marker sweep, not Researcher. The prior `[from: researcher / 2026-04-30 03:30]` "what to investigate next" entry at line 27 was answered A and resolved 5/15 by worker-9am (candidate menu collapsed); no existing unanswered "what to investigate next" entry to dedup against. Per role-doc step 2, writing a fresh entry.
  - What I tried: read CLAUDE.md, STATE.md (planner 2026-05-15 07:00 overwrite), JOURNAL.md tail (5/15 chain + 5/16 02:37 night-auditor — lock 207.76h / 8.66d; 5 of 28 markers paired-resolved 5/15; 23 ripe for Worker sweep; JSONL key-prefix discrepancy flagged tonight), QUEUE.md head + Answer-batch follow-up sub-section (no `[research]` tags), NEEDS-INPUT.md (canonical Read+Grep: 28 dashboard markers, 5 paired-resolved, 1 pre-batch unanswered = P4.A2 libEGL line 284, 2 P2.5 spec Qs at 293+304), PROPOSED.md head (40 entries post-tonight's +1), `roles/researcher.md`, `glob` of `research/` (7 files; last = 2026-05-15-dashboard-answer-batch-rework-surface.md).
  - What I need from you: pick one (or override). Each option is bounded + produces an actionable PROPOSED entry or grounded recommendation.
    - (a) **P5 legacy archive surface census.** Grep each `sabrina-2/` Python file for imports from / references to legacy root-level `core/`, `services/`, `utilities/`, `scripts/`, `models/`. Produce a per-legacy-module reference count + an archive-ready list of orphaned modules. Informs `rebuild/LEGACY_REPLACEMENT_GATE.md` exit criteria + the Phase 5 archive sweep. Bounded ~2h; produces a PROPOSED entry per orphaned module.
    - (b) **Dashboard answer-resolution wiring soundness end-to-end.** Night-auditor 5/16 02:37 flagged a JSONL key-prefix discrepancy (`sabrina::` actually emitted by the producer vs. role-doc-specified `<project>` = `sabrina-local-ai`). Trace the producer's `filter_open()` source against tonight's `logs/sabrina/2026-05-16.jsonl` row format; confirm whether the 5 resolved markers actually retire from tomorrow's dashboard scan, or whether the key-prefix mismatch silently breaks the retire path. Time-sensitive — if broken, 23 ripe markers won't retire when paired by Workers. Bounded ~1h; produces a PROPOSED entry recommending the role-doc-vs-producer reconciliation direction.
    - (c) **Phase 6 v1.0 release gates evidence audit.** Walk ROADMAP §"Gate 1 — Component completeness" / §"Gate 2 — Daily-driver readiness" / §"Gate 3 — Bake-in" line-by-line, cross-referencing DONE.md + in-tree code evidence. Surface any gap between claimed-shipped and actual evidence. Informs P6.1 v1.0 release decision doc scope and P6.5 ship-readiness. Bounded ~2h; produces a per-gate-component grounded status grid.
    - (d) **P4.A3 Live2D rig bind dispatcher.py pre-pickup delta.** Has anything in `dispatcher.py` (StateDispatcher pattern) or `[avatar]` config shape shifted since the spec at `rebuild/drafts/research/2026-05-13-p4a3-avatar-live2d-rig-bind-spec.md` was written, such that the (a)-half pickup would need a spec amend? Bounded ~30min.
    - (e) **Ruff drift composition source trace.** The 5/14 → 5/16 ruff drift is +4 (107 → 111) and 100% additive to specific rule codes (I001 +3, SIM105 +1). Trace each new violation to the exact LOC + Worker that introduced it. Informs whether PROPOSED #30 + #36's ruff-cleanup Worker shift scope estimate is accurate. Bounded ~1h.
  - Recommendation: (b). Time-sensitive — the wiring just shipped 5/14 14:53 and is live; if the key-prefix mismatch silently breaks the retire path, the 23 ripe markers will accumulate stale `[answered:]` markers on the dashboard despite Worker-side `[resolved:]` pairing. (a) is the second pick — Phase 5 legacy archive is the natural next phase exit once the lock clears. (c)–(e) are valuable but defer well.

- [ ] [from: spec-writer / 2026-05-16 06:50] P4.B4 automation Windows e2e — Step 4 kill-switch validation handler: register temporary `noop_action_slow`, busy-wait `send_hotkey` shim, or skip live kill-switch validation?
  - Context: Spec at `rebuild/drafts/research/2026-05-16-p4b4-automation-windows-validation-spec.md` § Step 4. P4.B4 quartet ship closes Phase 4 Component 10 (Automation). Real-keypress validation (Step 3) takes ~10 ms — too short to trip the kill-switch mid-flight. The kill-switch is the load-bearing safety primitive per `2026-04-26-threat-model.md`; without live keypress validation, the only proof the trip path works is the 18 P4.B1 unit tests (which use programmatic `KillSwitch.trip()`, not the real `pynput.GlobalHotKeys` listener).
  - What I tried: read the P4.B1 spec at `rebuild/drafts/research/2026-05-08-p4b1-automation-safety-primitives-spec.md` § Concrete DoD #18 (claude-dispatch integration tests use programmatic trip — not a real keypress); read `sabrina-2/src/sabrina/automation/scaffold.py` for the existing `noop_action` shape; confirmed the `_LISTENER_FACTORY` injection seam means a slow noop registration is ~15 LOC + flag. Considered the busy-wait shim — pynput executes press/release pairs sequentially but each pair is sub-millisecond on Win11; would need a 50+ key sequence to be reliably trippable, polluting the real-action vocab with a fake. Considered `requires_kill_switch_smoke` pytest marker on Windows but Windows-pytest doesn't exercise `pynput.GlobalHotKeys` either — that needs a focused window.
  - What I need from you: pick one (or override).
    - (a) Register `noop_action_slow` temporarily (5 s `asyncio.sleep`, single-use). Add to `automation/scaffold.py` under `noop_slow_enabled = false` flag, flipped only for this validation. ~15 LOC + revert. Live trip path tested with a real `<ctrl>+<alt>+k` keypress mid-handler. **Spec recommends this.**
    - (b) Busy-wait `send_hotkey` shim — bind a 50-key sequence (`<ctrl>+<alt>+<shift>+<f1>...<f12>` etc.) totaling ~500 ms. Brittle — depends on pynput internal timing — and pollutes the real-action tool vocab with a fake.
    - (c) Skip live kill-switch validation; rely on the 18 P4.B1 unit tests + a Windows smoke test of `pynput.GlobalHotKeys.start()` registering the hotkey without firing it. Defers the trip-path live validation to a P4.B5 separate item.
  - Recommendation: (a). Live validation of the trip path with a real keypress is the only piece the unit tests can't cover. The temporary registration is small surface, small blast radius, removed before the decision-doc commit. (b) is brittle; (c) leaves the load-bearing primitive without an empirical Windows test. P4.B4 is `[windows-required]`; this matters for Eric's session prep, not Worker pull.

- [ ] [from: spec-writer / 2026-05-16 06:50] P4.B4 automation Windows e2e — decision doc bundling: one combined "automation safety + first action" doc at next free integer, four separate per-primitive docs, or bundle with the v1.0 (011) release doc?
  - Context: Spec at `rebuild/drafts/research/2026-05-16-p4b4-automation-windows-validation-spec.md` § Step 8. P4.B4 ratifies the three safety primitives (kill-switch + dry-run + destructive-action allow-list) + one real-action tool (`send_hotkey`) under one architectural surface. Flat-numbering precedent holds through 010 (personality spec); 011 reserved for v1.0 release per ROADMAP §"Phase 6" item #6. Same Q2-shape as today's P4.C3 spec (Brain router + persona-projection bundling).
  - What I tried: read decision docs 003 / 008 / 009 for length precedent (~120-180 lines each); read 010-personality-spec.md for the combined-surface precedent (single doc covering multiple coupled rules); cross-referenced the per-(a)-half specs at `rebuild/drafts/research/2026-05-08-p4b1-*.md` + `2026-05-09-p4b2-*.md` + `2026-05-09-p4b3-*.md` (three specs but one architectural surface; the destructive-action gate is the third leg of safety alongside kill-switch + dry-run).
  - What I need from you: pick one (or override).
    - (a) One combined doc at the next free integer (per 005/008/009/010 precedent), excluding 011. Covers the three primitives + `send_hotkey` + the architecture. ~150-200 lines. Maintains flat-numbering. **Spec recommends this.**
    - (b) Four separate decision docs — one per (a)-half spec, plus this one. ~60-80 lines each × 4 = 240-320 lines total. Maintains per-spec/per-doc symmetry; burns four decision numbers on a single architectural surface.
    - (c) Bundle with the v1.0 release doc (011) — defer to P6.5. Saves the number but couples P4.B4's close-out to v1.0's, blocking Gate 1 component 10 `[x]` until the whole release ships.
  - Recommendation: (a). Three primitives + one tool are one architectural surface; one doc reads cleaner. Flat-numbering is load-bearing. Lands at the next free integer (likely 012 or 013 depending on P2.5 + P2.6 sequencing). (b) over-fragments; (c) couples unrelated gates. Worker-blocker status: none (P4.B4 is `[windows-required]`); the answer only matters at decision-doc filing time.

- [ ] [from: spec-writer / 2026-05-16 06:50] P4.C3 router voice-loop integration — default `[brain.router].policy` for v1 ship: `claude_default`, `cost_aware`, or `force_local`?
  - Context: Spec at `rebuild/drafts/research/2026-05-16-p4c3-router-voice-loop-integration-spec.md` § Q1. P4.C3 closes Phase 4 Component 4 (Brain router). Three policies shipped in the (a)-half: `claude_default` (every turn → Claude), `force_local` (every turn → Ollama), `cost_aware` (escalates to Ollama at `[budget].warn_usd_monthly` per Eric's 5/15 B-answer at NEEDS-INPUT.md:140). Default sets the v1 daily-driver behavior.
  - What I tried: read P4.C2 router spec at `rebuild/drafts/research/2026-05-08-p4c2-router-routing-policy-spec.md` § "Policy semantics" for the three definitions; read P4.C1 persona-projection spec for the operator-voice survival gate that determines whether non-`claude_default` policies are even safe; cross-referenced decision 010 (operator voice is canon) — Claude was the original canon, persona-projection layer is the parity glue for Ollama. Worker-11am 2026-05-08's (a)-half shipped `policy = "claude_default"` as the (a)-half toml default per the spec recommendation.
  - What I need from you: pick one (or override).
    - (a) `claude_default` — every turn picks Claude unless explicitly overridden. Most operator-voice-coherent; Ollama route lives behind `force_local` opt-in or `cost_aware` automatic. Most Claude budget spend; least surprising for Eric's daily-driver pattern. **Spec recommends this.**
    - (b) `cost_aware` — escalates to Ollama when monthly warn threshold is near. Mid-spend, mid-surprise. Requires Step 5 cost-aware validation to confirm escalation works cleanly without flip-flopping mid-conversation.
    - (c) `force_local` — every turn picks Ollama. Zero Claude spend; persona-projection load-bearing on every turn. High-trust posture on Step 4's load-bearing Ollama operator-voice gate; risky if any rule misfires.
  - Recommendation: (a). Lowest-risk v1 ship. Eric can flip to (b) once Step 5 cost_aware has multiple sessions of transcript-blindness evidence. (c) is post-v1 if at all. Worker-blocker status: none (P4.C3 is `[windows-required]`); the answer sets Eric's Saturday-session toml flip.

- [ ] [from: spec-writer / 2026-05-16 06:50] P4.C3 router voice-loop integration — decision doc bundling: one combined "brain router + persona projection" doc at next free integer, two separate docs, or bundle with P4.B4's mega-doc?
  - Context: Spec at `rebuild/drafts/research/2026-05-16-p4c3-router-voice-loop-integration-spec.md` § Q2. P4.C3 ratifies C1 (persona-projection) + C2 (router routing policy). Same Q2-shape as today's P4.B4 spec.
  - What I tried: read decision docs 003 / 008 / 009 for length precedent; cross-referenced the per-(a)-half specs at `2026-05-07-p4c1-*.md` + `2026-05-08-p4c2-*.md`; considered the architectural coupling (router picks backend; projection conditions output — two halves of one operator-voice-coherent dispatch surface).
  - What I need from you: pick one (or override).
    - (a) One combined doc at the next free integer (per 005/008/009/010 precedent), excluding 011. Covers C1 + C2 under one number. ~180-220 lines. Maintains flat-numbering. **Spec recommends this.**
    - (b) Two separate decision docs — one for persona-projection (C1) + one for router (C2). ~90-110 lines each. Maintains per-spec/per-doc symmetry; burns two decision numbers on adjacent surfaces.
    - (c) Bundle with P4.B4's decision doc — one mega-doc covering Phase 4 (Automation + Router + Persona). ~400+ lines; harder to read; over-couples Component 4 and Component 10.
  - Recommendation: (a). Router + persona-projection are one coupled architecture; one doc reads cleaner. Same Q2-shape as P4.B4; same answer recommended. Lands at the next free integer (likely 013 or 014 depending on P2.5 + P2.6 + P4.B4 sequencing). Worker-blocker status: none (P4.C3 is `[windows-required]`); the answer only matters at decision-doc filing time.
