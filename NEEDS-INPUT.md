# Needs Input — Sabrina-Local-AI

> Questions and blockers that need human answers before automation can proceed. Cleared during the evening review window.

## How to respond

- Add your answer inline under the question.
- Mark `[answered]` once resolved. The next Planner run will clear answered items and act on them.
- If a question turned into actual work, the Planner will move it to QUEUE.md.

## Format

```
- [ ] [from: role / YYYY-MM-DD HH:MM] Question
  - Context: ...
  - What I tried: ...
  - What I need from you: a decision / clarification / approval / new information
```

---

- [ ] [from: night-auditor / 2026-04-30 02:37] GitHub MCP not connected in this Cowork session — remote-PR audit step (role step 3) skipped this run.
  - Context: `roles/night-auditor.md` step 3 says to list open PRs, fetch CI/check status, and verify the latest Worker push landed on `automation/<role>-<YYYY-MM-DD>-<slot>` on the remote. None of the `mcp__plugin_engineering_github__*` tools are available in this run's deferred-tools list. Per the role doc, I'm skipping cleanly and continuing the rest of the audit. (Worth noting: there was nothing to verify tonight either — the 8am Worker bailed without committing or pushing; 9–12pm idled. So a connected GitHub MCP would have produced an empty Worker-push verification anyway.) If you want this gate active, install/connect a GitHub MCP for scheduled-task sessions; otherwise remove the step from the role doc.

- [ ] [from: researcher / 2026-04-30 03:30] What bounded question should the Researcher investigate next? — no `[research]`-tagged items queued, the natural P1 surface is already grounded.
  - Context: per `roles/researcher.md` step 1, I look for `[research]`-tagged items or P0/P1 questions that need grounding. Today's QUEUE has one item (ClaudeBrain wire-up, `[in-progress]`); yesterday's researcher already produced `research/2026-04-29-claudebrain-tool-wire-up-surface.md` pinning the diff surface. NEEDS-INPUT has three open items (GitHub MCP, worker-8am DoD-gate fork, pytest-in-sandbox) — all are decision asks for you, not research questions. The night-auditor 02:37 today noted "a second pass on the same surface is unlikely to add value unless they pivot to a different bounded question." Per role step 2, since no prior `[from: researcher / ...]` "what to investigate next" entry exists in NEEDS-INPUT, I'm writing this one and stopping rather than inventing a question.
  - What I tried: read CLAUDE.md, STATE.md, JOURNAL.md (today's night-auditor 02:37 + yesterday's full cycle), QUEUE.md, NEEDS-INPUT.md, PROPOSED.md (12 items, all unchecked), `roles/researcher.md`, glob of `research/` (1 file from yesterday). Confirmed there's no `[research]`-tagged item, no P0/P1 question needing grounding, and no prior researcher "what to investigate next" entry to dedupe against.
  - **2026-05-05 07:00 planner: de-facto resolved.** P2.1 (wake-word training pipeline research) reached QUEUE via the 2026-05-04 backlog decomposition and was picked up by researcher 2026-05-05 03:30, who shipped `research/2026-05-05-wake-word-training-pipeline.md`. This entry was never marked `[answered]` by Eric, so leaving it in place per role-doc; it is no longer load-bearing.
  - What I need from you: nothing further; this can be marked `[answered]` and removed in any future planner run, or left to age out. Original question text below for context:
  - Original ask: name a bounded question (or check a PROPOSED item with `[research]` so a future Researcher slot picks it up). Candidate areas surfaced from PROPOSED / STATE / CLAUDE.md if useful as a menu — pick at most one and sharpen it:
    - Personality-projection layer for Ollama parity (CLAUDE.md flags this as "planned" alongside the 010 personality spec; bounded question would be "what's the minimal projection-layer surface that lets Ollama emit operator-voice without re-implementing the personality blocks in `claude.py`?").
    - "Hey Sabrina" custom wake-word training pipeline (STATE.md known one-day task; bounded question would be "what's the openWakeWord training-data + tooling shape, given the current `hey_jarvis` placeholder?"). **← Picked + shipped 2026-05-05 03:30.**
    - Live2D avatar layer architecture (CLAUDE.md "planned"; bounded question would be "what does the avatar process look like as a sibling to the voice loop — IPC shape, frame budget, integration point?").
    - Recursive tool loop hardening for the wire-up's recursion cap = 5 (research doc lands the cap but doesn't ground the observability/error-budget shape — bounded question: "how should tool-use loops surface 'cap exceeded' to the operator voice without leaking internals?").
    - None of the above — write your own bounded question.

- [ ] [from: worker-9am / 2026-05-04 09:00] Stale `.git/index.lock` (5 days old, 0 bytes) blocks `git add` — wire-up landed clean in working tree but unable to commit.
  - Context: I'm the 9am Worker. Picked up the new `[linux-runnable] [partial-dod-eligible] [P1]` (a)-half item Eric staged via the 2026-05-04 unblock run. Did the full wire-up (protocol.py + claude.py + ollama.py + 6 unit tests per research/2026-04-29-claudebrain-tool-wire-up-surface.md). All gates GREEN under Linux/3.10 sandbox: `python -m compileall -q sabrina-2/src sabrina-2/tests` clean; AST-parses clean; 6 new wire-up tests PASS; 4 existing ToolSpec round-trip tests at `tests/test_smoke.py:1869-1965` continue to PASS; pre-existing `brain/claude.py +134` personality blocks restored intact in working tree (Edit-tool truncation hit twice mid-run per CLAUDE.md hazard — recovered via bash heredoc, then verified AST + line counts post-recovery). Code-review self-pass: no P0/P1; P2/P3 noted in run summary.
  - What I tried: ran `git status` per role doc step 7. Status itself succeeded (read-only). Then `git add --dry-run sabrina-2/src/sabrina/brain/protocol.py` returned exit 128 with `fatal: Unable to create '.git/index.lock': File exists.` Lock metadata: `.git/index.lock`, size 0 bytes, mtime 2026-04-29 14:15:52 UTC (5 days old). Almost certainly a zombie from some 2026-04-29 git operation that crashed — worker-8am 2026-04-29 bailed without committing per its NEEDS-INPUT entry above, so the lock isn't from this run or any subsequent worker. Per role doc: "Do NOT delete the lock. Append a NEEDS-INPUT entry... and stop." Following the letter of the rule even though pragmatically the lock is stale, because the role doc explicitly carves zero exceptions for staleness ("Sabrina's repo has been clean of this pattern; the safeguard stays anyway").
  - What I need from you: clear the stale lock and decide whether tomorrow's worker can pick up the commit step or if you'd rather do it yourself. Options:
    - (a) `rm .git/index.lock` then on your next session run the commit yourself — `git checkout -b automation/worker-2026-05-04-9am`, then selectively stage just the wire-up hunks (NOT the personality blocks in claude.py per QUEUE caveat / PROPOSED #5; NOT the unrelated dirty test additions in test_smoke.py beyond ToolSpec round-trip + my 6 new tests). Suggested staging: `git add sabrina-2/src/sabrina/brain/protocol.py sabrina-2/src/sabrina/brain/ollama.py`, then `git add -p sabrina-2/src/sabrina/brain/claude.py sabrina-2/tests/test_smoke.py` and pick the wire-up + 6-new-tests + 4-ToolSpec-round-trip-tests hunks only. Commit message suggestion: `feat(brain): wire ClaudeBrain.chat tool-use dispatch (a-half) (queue: claudebrain-wire-up-a-half)` with `Windows-pending: e2e` in the body and the Windows DoD checklist below.
    - (b) `rm .git/index.lock` then leave the work in the tree for tomorrow's 8am Worker to commit. The (a)-half QUEUE item is now `[in-progress]` with a pointer to this entry; tomorrow's Worker reads STATE/JOURNAL/NEEDS-INPUT, sees this entry as `[answered]` (after you mark it), runs `git status` (no lock), and proceeds to the commit step using the same staging approach above. Tests will still pass tomorrow because the wire-up sits unchanged in the working tree.
    - (c) `rm .git/index.lock` then revert the working tree (`git checkout -- sabrina-2/src/sabrina/brain/protocol.py sabrina-2/src/sabrina/brain/claude.py sabrina-2/src/sabrina/brain/ollama.py sabrina-2/tests/test_smoke.py`), have tomorrow's Worker re-do the wire-up cleanly. This is the most-conservative no-op of my run; only pick if you don't trust the in-tree state.
  - Windows DoD checklist for the (a)-half (per Partial-DoD tier in CLAUDE.md, regardless of which option above): `pytest sabrina-2/tests/test_smoke.py -k "tool or claude_executes or claude_recursion or claude_tool_handler or claude_unknown_tool or ollama_raises or brain_chat_tools_none"` passes on Windows / Python 3.12 with full deps (pywin32, pyperclip, pynput, mss, sounddevice). The (b)-half (`events.py` + `voice_loop.py` + `sabrina.toml` flip + voice-turn e2e validation) remains gated on your Windows session per the existing `[windows-required]` queue item.
  - Recommendation: (a) if you're at the keyboard and want the commit to land tonight; (b) if you want tomorrow's Worker to break the 6-day idle pattern with a clean pickup. (c) only if you specifically want to inspect/redo the wire-up — but it tested green and matches the research doc literally, so there's no quality reason to redo.
  - **2026-05-05 07:00 planner: lock still in place per night-auditor 02:37. Code in working tree intact (verified by night-auditor's tail-integrity spot-check on the four touched files). Today's 8am Worker should attempt the commit if you've cleared the lock overnight; if still locked, fall through to one of P5.2 / P5.4 / P5.5 per the (a)-half QUEUE note.**

- [ ] [from: spec-writer / 2026-05-05 06:50] P2.7 budget tracker — where should the per-token cost-table for Sonnet 4.6 / Haiku 4.5 / Opus 4.6 live?
  - Context: I'm the 06:50 Spec Writer. Drafted `rebuild/drafts/research/2026-05-05-p27-budget-tracker-spec.md` for queue item P2.7. The (a)-half ships a `compute_cost(input_tokens, output_tokens, model: str) -> float` pure function in `sabrina-2/src/sabrina/budget.py`; that function needs the per-million-token rates for input/output for each Anthropic model the brain currently dispatches against. Three plausible homes for those rates with different trade-offs; spec recommends one but the call is yours.
  - What I tried: read the predecessor `rebuild/drafts/budget-and-caching-plan.md` (2026-04-23) — it embeds the rates as constants in `sabrina/budget.py` directly. Read `rebuild/decisions/001-hardware-and-budget.md` indirectly via STATE/CLAUDE.md references — sets the $0/$10/$100 thresholds but leaves the per-token rates implicit.
  - What I need from you: pick one (or override).
    - (a) Inline constants in `sabrina-2/src/sabrina/budget.py`. Cheapest, no I/O on the voice-loop hot path, easy to read in code review. Cost: a per-token-rate change requires a code edit + commit. Spec recommends this — Anthropic pricing changes are infrequent and a rate edit landing alongside a decision-doc note is the cleanest audit trail.
    - (b) `[budget.cost_table]` block in `sabrina.toml`. User-tunable; survives Anthropic price changes without a code edit. Cost: a $0.01 typo costs Eric real money, and the values aren't sensitive enough to justify settings-file ceremony.
    - (c) `data/anthropic-prices.yaml` alongside the `data/shortcuts.yaml` P5.2 ships. Consistent-with-the-data-port pattern. Cost: an extra YAML-load on every cost computation, plus a second source of truth (the YAML file vs. Anthropic's actual pricing page) to keep in sync.
  - Recommendation: (a). Worker can ship the (a)-half against (a) without waiting for an answer; if you pick (b) or (c) before the spec is pulled, the Worker reroutes per the answer here.

- [ ] [from: worker-8am / 2026-05-05 08:00] Stale `.git/index.lock` still blocks commit — P5.2 (shortcut-table data port) work landed clean in working tree but unable to commit. Same blocker as the worker-9am 2026-05-04 entry above; this entry exists per role doc step 7 ("Append a NEEDS-INPUT entry describing the situation (path, mtime, your run id)").
  - Context: I'm the 8am Worker on 2026-05-05. Falling through to P5.2 per planner 07:00 handoff because the (a)-half wire-up commit is still lock-blocked. Authored `sabrina-2/data/shortcuts.yaml` (17-entry port verbatim from legacy `services/automation/automation.py:47-65`), `sabrina-2/tests/test_shortcuts_yaml.py` (20 parametrized tests, all PASS under Linux/3.10 sandbox), and a targeted `sabrina-2/.gitignore` tweak (`/data/*` + `!/data/*.yaml` negation so YAML data assets are tracked while runtime `sabrina.db` stays ignored — verified via `git check-ignore`). All Linux Partial-DoD gates GREEN: `python -m compileall -q sabrina-2/src sabrina-2/tests` clean; AST parses clean; no production-code changes (only data + test + gitignore). Edit-tool truncation hit once on the .gitignore mid-edit — recovered via bash heredoc per CLAUDE.md hazard.
  - What I tried: ran `git status` per role doc step 7. Status itself succeeded (read-only). `git add --dry-run sabrina-2/data/shortcuts.yaml sabrina-2/tests/test_shortcuts_yaml.py sabrina-2/.gitignore` returned exit 128 with `fatal: Unable to create '.git/index.lock': File exists.` Lock metadata: `.git/index.lock`, size 0 bytes, mtime 2026-04-29 14:15:52 UTC (now 6 days old). Same zombie lock the worker-9am 2026-05-04 entry diagnoses; not duplicating that diagnosis.
  - What I need from you: clear the stale lock per the worker-9am entry's options (a)/(b)/(c). Once cleared, suggested staging for P5.2: `git add sabrina-2/data/shortcuts.yaml sabrina-2/tests/test_shortcuts_yaml.py sabrina-2/.gitignore`, branch `automation/worker-2026-05-05-08am`, commit message `port: extract shortcut table from legacy services/automation/ to sabrina-2/data/shortcuts.yaml (queue: P5.2)` with `Windows-pending: e2e` in the body. (For P5.2 specifically the e2e gate is a no-op in practice — the file is data, not runtime — but the marker keeps the partial-DoD audit trail consistent.) Files in tree are byte-identical to what the next Worker will see; can be picked up cleanly by tomorrow's 8am Worker if you don't want to commit yourself tonight.
  - Recommendation: piggyback on whichever option you pick for the worker-9am wire-up. Same `rm .git/index.lock` solves both; both items are then commit-ready in independent diff partitions (the (a)-half touches `sabrina-2/src/sabrina/brain/`; P5.2 touches `sabrina-2/data/`, `sabrina-2/tests/test_shortcuts_yaml.py`, `sabrina-2/.gitignore` — no overlap).
