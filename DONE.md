# Done — Sabrina-Local-AI

> Completed items. Workers move things here when finished. Lets future runs see what's been addressed so they don't re-propose it.

## Format

```
## YYYY-MM-DD
- Title — done by role at HH:MM. Outcome: ...
  - Commit: <sha or "no commit">
  - Tests: pass | fail | n/a
```

---

## 2026-05-17
- **P4.C2 router warn_threshold field-removal rework — align in-tree (a)-half with Eric's B answer to spec Q1** — DoD satisfied by worker-10am 2026-05-17 11:00 (interactive rapid-sprint slot 3); moved QUEUE→DONE by planner 2026-05-17 07:00 same morning. Outcome: real ~40 LOC rework per QUEUE Answer-batch #1 / NEEDS-INPUT.md:143. Touched (1) `sabrina-2/src/sabrina/config.py` — dropped `warn_threshold_usd: float | None = None` field from `BrainRouterConfig` + docstring bullet; replaced with explicit "router reads `[budget].warn_usd_monthly` directly" framing. (2) `sabrina-2/src/sabrina/brain/router.py` — flipped module docstring + `make_router_from_settings` docstring Q1 (a)→(b); replaced 3-line conditional resolver with `threshold = settings.budget.warn_usd_monthly`. `Router` class signature unchanged (still accepts `warn_threshold_usd=` kwarg for direct-construction tests per spec carve-out). (3) `sabrina-2/sabrina.toml` — dropped `warn_threshold_usd` mention from `[brain.router]` block comment + removed commented-out override line; -7 lines. (4) `sabrina-2/tests/test_router.py` — re-wired `test_make_router_from_settings_wires_both_backends` to drive threshold via `SABRINA_BUDGET__WARN_USD_MONTHLY` env var; renamed `test_make_router_from_settings_threshold_falls_back_to_budget` → `_sourced_from_budget`. Direct-construction tests at lines 160-241 (`Router(... warn_threshold_usd=10.0)`) STAY unchanged per spec. Linux Partial-DoD gates GREEN: `python3 -m py_compile` clean on all 3 src files + test file; `pytest sabrina-2/tests/test_router.py` → 17/17 PASS in 0.95s; combined regression `pytest test_router.py + test_automation_safety.py + test_budget.py` → 59/59 PASS in 1.43s. FUSE Edit-tool divergence fired three times this run (router.py + config.py + test_router.py); recovered each via Python `os.fsync()` atomic-replace heredoc. Resolved marker at NEEDS-INPUT.md:144 with `(partial-dod, awaits-windows-e2e)` trailer.
  - Commit: no commit landed this run (`automation/worker-2026-05-07-8am` lock-blocked by the same 9.67d-stale `.git/index.lock` from 2026-05-07; this is one of 16 lock-blocked diffs in the pile). Suggested commit once lock clears: `git add sabrina-2/src/sabrina/config.py sabrina-2/src/sabrina/brain/router.py sabrina-2/sabrina.toml sabrina-2/tests/test_router.py`, message `feat(brain.router): drop warn_threshold_usd field; read [budget].warn_usd_monthly directly (queue: Answer-batch-#1)` with `Windows-pending: e2e` in the body.
  - Tests: pass (Linux/3.10 sandbox: 17/17 PASS on test_router.py post-rework; 59/59 PASS on combined regression). Windows e2e pending Saturday-collapse bundle alongside the rest of the (b)-half cluster.

- **CLAUDE.md — sharpen "verify post-edit" rule to name Read tool as authoritative** — DoD satisfied by worker-11am 2026-05-17 12:00 (interactive rapid-sprint slot 4); moved QUEUE→DONE by planner 2026-05-17 07:00 same morning. Outcome: appended one sentence to CLAUDE.md line 30 (the "Edit-tool truncation is a recurring hazard" rule) naming the Read tool as the only authoritative post-write verifier in this sandbox, with explicit rationale that bash `wc -l`/`cat` and Python `open().read()` go through the FUSE cache and can lag tool-channel writes by hours within a single session. Pure additive guidance; no Worker behavior change beyond "use Read tool instead of bash for the existing verification step." Graduated PROPOSED #37 (researcher 2026-05-09) via 2026-05-14 graduation-sweeper after 5-day eligibility window. Linux Partial-DoD gates GREEN by construction (zero Python touched; pre-commit hook's `compileall` no-op on the diff). Read-tool post-write verification confirmed edit landed at canonical NTFS layer; bash `wc -l` showed FUSE-stale count (108 vs Read's 110) — empirical re-confirmation of the H2 envelope this edit codifies.
  - Commit: no commit (lock-blocked alongside the other 15 in-tree diffs on `automation/worker-2026-05-07-8am`). Suggested commit once lock clears: `git add CLAUDE.md`, batched with the roles/worker.md edit below into `docs(automation): sharpen Edit-tool verify rule + codify state-file append discipline (queue: Answer-batch-#2+#3)` with `Windows-pending: e2e` in the body.
  - Tests: n/a (doc-only edit; `python3 -m compileall sabrina-2/src` no-op on the diff).

- **roles/worker.md — codify state-file append discipline (Edit-tool-with-anchor primary; deprecate Python `read() + os.replace()`)** — DoD satisfied by worker-11am 2026-05-17 12:00 (interactive rapid-sprint slot 4); moved QUEUE→DONE by planner 2026-05-17 07:00 same morning. Outcome: added a new "## State-file append discipline (FUSE-safety)" section to `roles/worker.md` between step 9 and "What you DO NOT do," codifying Edit-tool-with-anchor as the primary safe path for state-file appends, with the open-mode `"a"` + `f.flush() + os.fsync()` fallback for huge appends. Cites worker-9am 2026-05-07 09:00's data-loss incident verbatim. Graduated PROPOSED #38 (researcher 2026-05-09) via 2026-05-14 graduation-sweeper. Linux Partial-DoD gates GREEN by construction (no Python touched). Read-tool post-write verification confirmed edit landed at canonical NTFS layer; bash showed FUSE-stale count (49 vs Read's 62) — empirical re-confirmation of the H2 envelope.
  - Commit: no commit (lock-blocked; batches with the CLAUDE.md edit above into one commit when lock clears).
  - Tests: n/a (doc-only edit).

- **P4.B1 kill-switch hotkey docstring flip — spec Q1 (a)→(b) framing in `kill_switch.py`** — DoD satisfied by worker-8am 2026-05-17 09:00 (interactive rapid-sprint slot 1); moved QUEUE→DONE by planner 2026-05-17 07:00 same morning. Outcome: `sabrina-2/src/sabrina/automation/kill_switch.py` module docstring lines 18-24 flipped to "defaults to `<ctrl>+<alt>+k` and can be overridden via `[automation] kill_switch_hotkey` (spec Q1 (b))" with citation to NEEDS-INPUT.md:116; constant comment lines 42-46 reframed around the (b) answer + resolver-fallback rationale. The `AutomationConfig.kill_switch_hotkey` field was already plumbed forward-compat from the (a)-half (worker-9am 2026-05-08); the (b)-half wiring (voice_loop reads `settings.automation.kill_switch_hotkey` and threads to `KillSwitch(hotkey=...)`) was always (b)-half scope, unaffected by this rework. Hit Edit-tool/FUSE H2 divergence — file truncated mid-`__enter__` in FUSE view (bash 169 / Read 181); recovered via Python `os.fsync()` atomic-replace heredoc + `__pycache__` clear + clean re-import. Linux Partial-DoD gates GREEN: `python3 -m py_compile` exit 0; AST parse clean; `pytest sabrina-2/tests/test_automation_safety.py -q` → 18/18 PASS in 0.82s. No new test required (docstring-only behavior change). Resolved marker at NEEDS-INPUT.md:117 with `(partial-dod, awaits-windows-e2e)` trailer.
  - Commit: no commit (lock-blocked on the same 9.67d-stale `.git/index.lock`; this is one of 16 lock-blocked diffs in the pile). Suggested commit once lock clears: `git add sabrina-2/src/sabrina/automation/kill_switch.py`, message `docs(automation): flip kill_switch hotkey docstring (a)→(b) per NEEDS-INPUT.md:116 (queue: P4.B1-docstring-followup)` with `Windows-pending: e2e` in the body.
  - Tests: pass (Linux/3.10 sandbox: 18/18 PASS on test_automation_safety.py; no regression on kill_switch / dry_run / scaffold / claude-dispatch integration tests). Windows e2e pending Saturday-collapse bundle.

## 2026-05-16
- **Worker.md FUSE lock-leak forensic carve-out — `roles/worker.md` step 7 narrow exception to "Do NOT delete the lock"** — DoD satisfied by worker-10am 2026-05-16 10:00; pulled via dashboard answer-resolution sweep (worker.md step 2) — topmost answered-and-unresolved was NEEDS-INPUT.md:91 (worker-8am 2026-05-07 lock-bail, **[answered: C 2026-05-15 via dashboard]**), which mapped directly to this queued item per the Answer-batch follow-up sub-section + planner.md step 4 ("act on the answer"). Outcome: `roles/worker.md:19-24` edited — original line-19 bullet retains the "Do NOT delete the lock" default plus a "with one narrow forensic carve-out" pointer; new nested forensic-carve-out bullet codifies worker-8am 2026-05-07's canonical 3-condition gate verbatim — (a) `now - stat.st_mtime < 300` seconds, (b) `stat.st_size == 0`, (c) `stat.st_uid == os.getuid()` — with `stat -c '%Y %s %U' .git/index.lock` as the forensic check command, and an explicit "carve-out does NOT apply to a pre-existing lock from a prior session — mtime older than 5 minutes fails (a)" clause guarding against any temptation to misapply to the current 8.7d-stale lock. Net delta: 1 modified line + 4 new nested bullets (~12 LOC of doc). Linux Partial-DoD gates clean by construction (no Python touched; pre-commit hook's `compileall` is no-op on the diff). The carve-out gates the NEXT lock-leak recurrence after Eric's S1 (`rm -f .git/index.lock` from Windows shell) clears the current one; this run's lock is itself outside the carve-out window since its mtime is from 2026-05-07. Resolved NEEDS-INPUT.md:91 marker per worker.md step 6 with `(partial-dod, awaits-windows-e2e)` trailer at line 93.
  - Commit: no commit landed this run (`automation/worker-2026-05-07-8am` lock-blocked by the same 8.7d-stale `.git/index.lock` (mtime 2026-05-07 14:24:11 UTC) gating the 14+ prior in-tree diffs; this is the 15th lock-blocked diff in the pile). Commit pending Eric's Windows-shell `rm -f .git/index.lock` per OPEN-DECISIONS S1. Suggested commit message once lock clears: `docs(roles): add FUSE-leak forensic carve-out to worker.md step 7 (queue: worker-md-fuse-carve-out)` with `Windows-pending: e2e` in the body. Files in tree are byte-identical to what the next Worker / Eric's lock-clear sweep will see; pickup is clean.
  - Tests: n/a (doc-only edit; `python3 -m compileall sabrina-2/src` no-op on the diff). Windows e2e gate per the QUEUE entry: "next 8am Worker slot fires after this lands and successfully self-recovers from its own leaked lock" — that empirical validation rides along with whatever lock-leak the next 8am Worker generates on a future fresh-session-after-S1 run.

## 2026-05-14
- **P4.A1 Avatar — architecture research + decision draft** — DoD satisfied by worker-8am 2026-05-14 08:00; moved from QUEUE to DONE same run. Outcome: `rebuild/drafts/research/2026-05-14-avatar-architecture.md` shipped (~270 lines, 5 sections per spec DoD: §1 SDK pick / §2 PyQt6 cookbook / §3 IPC architecture / §4 Frame budget / §5 Eric's pick checklist). Recommendations: (a) `live2d-py` 0.6.x for SDK, (a) full window click-through via `WA_TransparentForMouseEvents`, (a) in-process via `qasync`. Eric's three picks pending via existing NEEDS-INPUT.md entries from spec-writer 2026-05-12 06:50 (lines 195/204/212); no new NEEDS-INPUT filed this run per dedup discipline. Unblocks P4.A2 (PyQt6 skeleton, spec'd 2026-05-12) + P4.A3 (Live2D rig bind, spec'd 2026-05-13) + P4.A4 (Windows e2e + decision doc). Spec parameterizations remain accurate against (a)/(a)/(a); Eric override before A2/A3 pull retargets the matching downstream spec per the "Note for A2's spec" callout in §3.
  - Commit: no commit (research artifact in working tree at `rebuild/drafts/research/2026-05-14-avatar-architecture.md`; per role-doc step 7 "skip for doc-only or no-op runs," no commit step fired this run — follows the P2.4 precedent in this file).
  - Tests: n/a (research-only; no code touched, `python -m compileall sabrina-2/src` clean from before equals after).

## 2026-05-06
- **P2.4 Autostart — research approach + draft (Task Scheduler vs. registry vs. NSSM vs. shell:startup)** — DoD satisfied by researcher 2026-05-06 03:30; moved from QUEUE to DONE by planner 2026-05-06 07:00. Outcome: `research/2026-05-06-autostart-approach.md` shipped (~270 lines, 4 sections + open follow-ups). Recommendation = ratify Task Scheduler (already in `acd6725` via `sabrina autostart enable/disable` + `supervisor.py`'s `render_task_scheduler_xml` / `install_task_scheduler_task`). Six XML-template tightening follow-ups identified (RestartOnFailure block missing, Priority audit, UserId substitution on non-domain-joined Win11, Hidden cosmetic, RunOnlyIfIdle paranoia test, task-name uniqueness). Major surprise: P2.4's research is post-hoc relative to code — implementation already shipped in HEAD on 2026-05-05. PROPOSED #35 (P2) sharpens P2.5 description from "implement" to "validate + tighten"; planner absorbed the sharpening directly into the P2.5 entry this run. Unblocks P2.5 (Windows e2e + XML polish).
  - Commit: no commit (research artifact in working tree as `research/2026-05-06-autostart-approach.md`; not yet in `main` due to lock+index recovery still pending).
  - Tests: n/a (research-only).

## 2026-05-05
- **(a)-half ClaudeBrain wire-up — protocol.py + claude.py + ollama.py + 6 unit tests** — code authored by worker-9am 2026-05-04 09:00, Linux gates green, then bundled into Eric's `acd6725` "Add rebuild research, digests & sabrina-2 updates" commit (2026-05-05 08:43:46 -0600). Promoted from QUEUE `[in-progress]` to DONE by planner 2026-05-06 07:00 after night-auditor 2026-05-06 02:37 confirmed HEAD advanced. The (b)-half remains in QUEUE as `[ ] [windows-required]` for Eric's next Windows session — `events.py` + `voice_loop.py` + `[tools] enabled = true` flip + Windows e2e voice-turn validation. Per the Partial-DoD tier in CLAUDE.md, the (a)-half hits `[done]` in main on its own; the original parent `[in-progress]` P1 line was removed from QUEUE this morning as redundant.
  - Commit: `acd6725`.
  - Tests: pass (Linux/3.10 sandbox: 6 new wire-up tests + 4 ToolSpec round-trip tests at `tests/test_smoke.py:1872-1965`). Windows pytest pending — gates with the (b)-half.

- **P5.2 Port — keyboard shortcut table → `sabrina-2/data/shortcuts.yaml`** — code authored by worker-8am 2026-05-05 08:00, Linux gates green, bundled into Eric's `acd6725` commit alongside the (a)-half. Promoted from QUEUE `[in-progress]` to DONE by planner 2026-05-06 07:00. Includes `sabrina-2/data/shortcuts.yaml` (17 entries, verbatim port from `services/automation/automation.py:47-65`), `sabrina-2/tests/test_shortcuts_yaml.py` (20 parametrized tests), and the targeted `sabrina-2/.gitignore` `/data/*` + `!/data/*.yaml` negation. Composes with Phase 4 P4.B2 (`send_hotkey` ToolSpec) which consumes this data.
  - Commit: `acd6725`.
  - Tests: pass (20/20 under Linux/3.10).

- **P2.1 Wake-word — custom "Hey Sabrina" training pipeline research draft** — DoD satisfied by researcher 2026-05-05 03:30; moved from QUEUE to DONE by worker-12pm 2026-05-05 12:00. Outcome: `research/2026-05-05-wake-word-training-pipeline.md` shipped (~270 lines, recommendation = Go). Findings: (1) train inside WSL2 (Eric has Ubuntu + Ubuntu-22.04 installed) — predecessor plan's Windows-CPU assumption wrong against current upstream tooling; (2) replace hand-rolled 200-speaker × prosody loop with a ~30-line wrapper around `piper-sample-generator`; (3) drop `voices/wake/hey_sabrina.onnx` for QUEUE-P2.2's `sabrina-2/models/openwakeword/hey_sabrina.onnx` path; (4) verifier-models on Eric's voice are documented out-of-MVP follow-up. Three new PROPOSED items appended by researcher 03:30 (#32 wake-word-plan amendment, #33 sandbox-host probe before P2.2, #34 ship-criterion citation). Unblocks P2.2 (build + package) pending PROPOSED #33 sandbox-host probe.
  - Commit: `acd6725` (research artifact landed in Eric's bundled commit on 2026-05-05 08:43, which absorbed the rebuild research drafts).
  - Tests: n/a (research-only).
