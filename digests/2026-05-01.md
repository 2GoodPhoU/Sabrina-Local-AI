# Digest — 2026-05-01

## TL;DR

Day-3 of identical block-state. Nothing shipped; all five Worker slots idled again. Same one-`[x]`-unblocks-tomorrow shape as the last two evenings.

## Done today

Nothing. `DONE.md` is still empty.

## Waiting on you (5)

1. **[worker-8am / 2026-04-29]** QUEUE P1 DoD-gate fork — pick (a) split per PROPOSED #4/#9, (b) accept multi-slot, (c) move Workers to a Windows runner, or (d) triage the working tree first. (a) is still the smallest unblock.
2. **[planner / 2026-04-30]** Day-2 legibility marker — auto-resolves the moment you answer #1 above or check `[x]` on PROPOSED #4 or #9. Carrying into day-3 unchanged; tomorrow's planner will dedup-skip again.
3. **[researcher / 2026-04-30]** Pick a bounded question for the Researcher slot (menu: personality-projection parity, "Hey Sabrina" wake-word training, Live2D avatar architecture, recursive tool-loop hardening, or write your own). Until answered, today's and tomorrow's Researcher dedup-skips.
4. **[night-auditor / 2026-04-29]** Pytest-gate policy: accept static-only audits, move auditor to Windows runner, or keep escalating nightly.
5. **[night-auditor / 2026-04-30]** GitHub MCP not connected in scheduled-task sessions; remote-PR audit step skipped. Either install the MCP or remove the step from the role doc.

## For your approval

14 PROPOSED items, all unchecked. Highest-leverage / highest-risk:

- **PROPOSED #4 + #9** (P1) — split QUEUE P1 into the (a)/(b) halves the research doc pins. Single `[x]` on either tomorrow gives the 8am Worker a productive slot.
- **PROPOSED #5** (P1, **high blast radius**) — human-led working-tree triage. 28 modified + 30 untracked + 1 deleted-not-staged. Gating prerequisite for any code-touching Worker run that doesn't entangle with the existing dirty diff. Two new subordinate items (#11 `.pre-commit-config.yaml`, #14 `write_test.txt`) fold into this.
- **PROPOSED #1, #2, #3, #6, #7, #8, #10, #12** — commit ToolSpec migration, ONNX embedder swap, in-tree round-trip tests, untracked-research-drafts policy, stage-pass2 scripts fate, ToolSpec-location DECISIONS doc. All subordinate to or shaped by #5.
- **PROPOSED #13** (P2) — ruff cleanup (82 errors, 50 auto-fixable; 20 format-drift files). Run after #5, not before.

## Queued for tomorrow

QUEUE has one item, unchanged for three days:

- `[in-progress] [P1]` ClaudeBrain wire-up. Blocked on the worker-8am DoD-gate fork above. Tomorrow's 8am Worker will skip and idle unless something gets answered or `[x]`'d before 08:00.

## Notable / surprises

- Three identical days running is itself the surprise — JOURNAL has 5 idle-exit Worker entries today, same as 2026-04-30. Nothing new. No tests ran (no tests can run in this sandbox). No commits. Zero lint/format drift since 2026-04-29 02:37 (ruff baseline holds exactly: 82 errors, 20 format files).
- Two trivial working-tree census deltas surfaced overnight by the night-auditor: `.pre-commit-config.yaml` confirmed dirty (the load-bearing `compileall-sabrina2` hook lives only in your working tree — a stash/branch-switch eats it), and `write_test.txt` is `D`eleted-not-staged (scratch file from commit `8dfb1ac`; staging the deletion is the expected resolution). Both fold into PROPOSED #5.
- PROPOSED count: 12 → 14 (the two census-correction items above).

## Shortest path to a non-idle 2026-05-02

Same as the last two digests. Any one of:
- `[x]` on PROPOSED #4 or #9 (split the P1).
- One-line `[answered]` on the worker-8am DoD-gate fork.
- `[x]` on PROPOSED #5 (human-led triage; doesn't unblock the P1 alone but is a prerequisite for any clean code-touching Worker run).
