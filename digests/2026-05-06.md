# Digest — 2026-05-06

## TL;DR

The 7-day commit drought broke overnight: HEAD advanced to `acd6725` (your manual commit, 2026-05-05 08:43, +16,072/-1,191 across 78 files), absorbing the (a)-half wire-up + P5.2 + the personality test suite + the pre-commit hook + ROADMAP + all rebuild research drafts. Today's research + spec runs landed clean; today's 8am Worker shipped P2.7 budget-tracker (a)-half code green-in-tree but lock-blocked; 9am missing, 10/11/12pm all idle-exited rather than compound the pile-up.

## Done today

- **P2.4 autostart research → DONE.** Researcher 03:30 shipped `research/2026-05-06-autostart-approach.md` (~270 lines). Recommendation = ratify Task Scheduler — which `acd6725` already shipped via `sabrina autostart enable/disable` + `supervisor.py`. Six XML-template polish follow-ups identified (RestartOnFailure, Priority audit, UserId substitution, Hidden cosmetic, RunOnlyIfIdle, task-name uniqueness).
- **Two new spec drafts.** Spec-writer 06:50 produced `2026-05-06-p53-system-deps-setup-script-spec.md` (P5.3 setup-deps) and `2026-05-06-p22-wake-word-build-spec.md` (P2.2 wake-word build, supersedes the 2026-04-23 plan with WSL2 + piper-sample-generator routing). Both name (a)/(b) seam, both ship-against-(a)-default with escape hatches.
- **Retroactive DONE promotions.** Planner 07:00 moved (a)-half wire-up + P5.2 to 2026-05-05 DONE (both landed in `acd6725`); P2.1 wake-word research was already moved by worker-12pm yesterday.
- **P2.7 budget tracker (a)-half — code green, commit blocked.** Worker-8am 06:50 authored `budget.py` (302L) + `test_budget.py` (24/24 PASS) + protocol/claude/cli/config/sabrina.toml wiring. 92 PASS + 1 SKIPPED combined regression; all 13 wire-up + ToolSpec round-trip tests still PASS. Marked `[in-progress]` on the same lock thread.
- **STATE.md wholesale rewrite + QUEUE reconciled** to HEAD=acd6725 reality. Removed redundant parent `[in-progress]` P1 entry; folded P2.5 description sharpening (PROPOSED #35) inline.

## Waiting on you (4 distinct decisions across 8 NEEDS-INPUT lines)

1. **`rm .git/index.lock && git read-tree HEAD`** — the one-minute action that unblocks 6 accumulated Worker diffs at once (P5.4 / P5.5 / P5.1 + worker-12pm queue admin from 2026-05-05 + P2.7 (a)-half from today). Lock now 7 days stale; index still 24020-byte truncated artifact.
2. **Spec-writer's 4 new picks (all spec-recommends-(a), Worker can ship without waiting):** P5.3 bootstrap-uv-when-missing (a/b/c); P5.3 ship `apply` or `check`-only; P2.2 sandbox-vs-WSL2 training host (a/b); P2.2 model-commit policy (a/b/c).
3. **Spec-writer P2.7 cost-table location** — open since 2026-05-05; 8am Worker shipped against recommended (a) inline-constants without waiting per spec escape hatch.
4. **GitHub MCP** — still not connected in scheduled-task sessions; install or remove the audit step.

(Plus 04-30 bounded-question, de-facto-resolved twice over — researcher-suggested archive candidate.)

## For your approval

- **PROPOSED #30 (P2) — ruff drift cleanup.** `ruff check` 81 → 87 errors overnight (+6 from new test files; +4 I001 + 2 likely F401); `ruff format --check` 20 → 25 files. All auto-fixable; one ~10-min Worker shift, partial-DoD-eligible (test-tree only).
- **PROPOSED #31 (P3) — state-file NUL-byte tail-padding.** `QUEUE.md` (1971 NULs) and `STATE.md` (1175 NULs) regrew tail-pad after worker-8am 2026-05-05's strip. Pure tail-pad, content intact; 3-line Python pass to clean.
- (PROPOSED #29 informational HEAD-advanced + #35 P2.5 sharpening already absorbed by today's planner — no action needed.)

## Queued for tomorrow

- **Recommended pick: P2.7 budget-tracker (a)-half commit** — if you clear the lock overnight, tomorrow's 8am Worker stages the green-in-tree diff and ships. Otherwise idle-exit pattern continues.
- **Fall-throughs once lock clears:** P5.3 setup-deps (a)-half [L, has spec], P2.2 wake-word build (a)-half [L, has spec], or PROPOSED #30 ruff cleanup if approved.
- **(b)-half wire-up** + P2.3 / P2.5 / P2.6 / P2.8 all `[windows-required]`, parked for your Windows session.

## Notable / surprises

- **Edit-tool truncation hit hard on worker-8am** — protocol.py / claude.py / cli.py / config.py all truncated mid-line; recovered all four via bash heredoc + `os.fsync`. Pattern is reproducible enough now that it's worth investigating the Edit-tool / FS-sync interaction.
- **9am Worker JOURNAL entry missing** — either non-execution or truncated append. Working tree at 10:00 byte-identical to 8am's leftover, so no in-tree work lost.
- **P2.4 research was post-hoc relative to code.** Implementation already shipped in `acd6725`; remaining P2.5 scope is one Windows e2e + optional XML polish, not "implement install/uninstall scripts."
- **Worker idle-exit cluster (10/11/12pm)** — all picked correctly per CLAUDE.md anti-sprawl and worker-8am's explicit handoff. Adding a 7th lock-blocked diff would compound your recovery cost rather than reduce it.
- **Lock+index unchanged across 7 days** despite Eric's `acd6725` commit landing via a different working-tree / git client. Recovery still pending; PROPOSED #5 working-tree triage still load-bearing.
