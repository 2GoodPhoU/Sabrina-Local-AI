# Digest — 2026-04-29

## TL;DR

Quiet day. The single P1 went `[in-progress]` at 08:00 because the Linux sandbox can't satisfy the DoD (pytest + Windows e2e), the four subsequent Worker slots idled, and nothing shipped. Two questions wait on you; both gate the same root cause (sandbox ≠ Sabrina runtime).

## Done today

Nothing. `DONE.md` is empty.

## Waiting on you (2)

1. **worker-8am 08:00** — QUEUE P1 wire-up, pick a path: (a) approve the (a)/(b) split per PROPOSED #4+#9 [worker recommends this], (b) accept multi-slot effort, (c) move Workers to a Windows runner, (d) approve PROPOSED #5 (working-tree triage) first.
2. **night-auditor 02:37** — pytest cannot run in the sandbox; pick a gate: (a) accept static-only audits, (b) move auditor to Windows-side runner, (c) keep escalating nightly. Same root cause as #1.

## For your approval (11 items in PROPOSED, all unchecked)

- **P1 #1, #2** — commit ToolSpec migration / commit ONNX embedder swap (state-vs-reality cleanup; #2 is medium blast — semantic memory critical-path).
- **P1 #4, #9** — split the QUEUE P1 into (a) protocol+claude+ollama+tests / (b) events+voice_loop+toml flip+Windows e2e. #9 pins the boundary using the new research doc. **Highest leverage: unblocks today's idle Workers tomorrow.**
- **P1 #5** — human-led working-tree triage (27 modified + 27 untracked). High-risk: every Worker run building on the dirty tree is at silent-invalidation risk until this is done.
- **P2 #3 / #8** — #3 (round-trip tests) is in-tree-done per #8; if you check #3 read it as "commit existing tests," not "write new ones."
- **P2 #6, #7** — decide policy on 22 untracked research drafts and the two repo-root `stage-pass2-commits*.ps1` scripts.
- **P2 #10** — document ToolSpec-location divergence (plan says `brain/protocol.py`, code shipped to `tools/__init__.py`).
- **P2 #11** — ruff cleanup pass (82 errors, 50 auto-fixable, 20 format-drift files). Defer until after #5.

## Queued for tomorrow

Same single P1 — ClaudeBrain wire-up — currently `[in-progress]`, blocked on the worker-8am NEEDS-INPUT entry. No other items. Until you pick (a)/(b)/(c)/(d), tomorrow's Workers will idle the same way.

## Notable

- **State-vs-reality gap surfaced concretely**: ToolSpec round-trip tests already live at `tests/test_smoke.py:1869-1965`. PROPOSED #3 was authored not knowing this. First instance of the broader pattern PROPOSED #5 calls out.
- **Research doc landed**: `research/2026-04-29-claudebrain-tool-wire-up-surface.md` pins the wire-up diff (~80 LOC vs ~150, keeps `stream.text_stream` fast path, recursion cap = 5, Ollama `NotImplementedError`). The design isn't gating; the dirty tree + sandbox limits are.
- **Four Worker slots idled** (09:00–12:00). Expected given the 8am bail; not a failure mode beyond the unanswered NEEDS-INPUT.
- **No Worker touched code today.** Working tree is byte-identical to where it stood at 07:00 planner.
