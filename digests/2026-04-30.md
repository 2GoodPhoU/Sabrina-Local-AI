# Digest — 2026-04-30

## TL;DR

Day-2 of the same block-state as yesterday. Nothing shipped; QUEUE P1 stayed `[in-progress]`; all five Worker slots idled. The fork from worker-8am 2026-04-29 is still the gate.

## Done today

- Nothing. `DONE.md` empty.

## Waiting on you (5)

1. **worker-8am 2026-04-29 08:00 — DoD-gate fork (a)/(b)/(c)/(d).** The QUEUE P1's DoD has two gates a Linux sandbox can't satisfy (pytest, Windows e2e voice turn). Highest-leverage unblock; equivalent to checking PROPOSED #4 or #9.
2. **planner 2026-04-30 07:00 — legibility marker.** New today, no new info. Single line saying day-2 = day-1. Delete after answering one of the others.
3. **researcher 2026-04-30 03:30 — what bounded question next?** No `[research]`-tagged item, P1 surface already grounded. Pick from menu (personality-projection / wake-word / Live2D / tool-loop hardening) or write your own. Recommendation: personality-projection.
4. **night-auditor 2026-04-30 02:37 — GitHub MCP not connected.** Remote-PR audit step skips cleanly each night. Either install a GitHub MCP for scheduled-task sessions or remove the step from the role doc.
5. **night-auditor 2026-04-29 02:37 — pytest-in-sandbox gate.** Same Linux/Python-3.10 vs Windows/3.12 wall. Decide: accept static-only, move to Windows runner, or keep escalating nightly.

## For your approval (12, all unchecked)

Highest-leverage:
- **#4 + #9 — split QUEUE P1 into (a)/(b).** Pinned boundary: (a) protocol+claude+ollama+tests / (b) events+voice_loop+toml+Windows e2e. One `[x]` here makes tomorrow's 8am Worker productive.
- **#5 — working-tree triage** (28 modified + 31 untracked, human-led). Gates any clean code-touching Worker run. **High blast-radius** if skipped — every Worker is at risk of building on disposable working-tree state.

Subordinate / informational: #1 ToolSpec migration commit, #2 ONNX embedder commit, #3 round-trip tests (reinterpret as in-tree-done per #8), #6 untracked research drafts policy, #7 stage-pass2 scripts fate, #8 #3-reinterpretation, #10 ToolSpec-location doc divergence, #11 fold `.pre-commit-config.yaml` into #5 census, #12 ruff cleanup (after triage).

## Queued for tomorrow

- `[in-progress] [P1]` ClaudeBrain wire-up — same item, same `[in-progress]` status, same blocker. Without one `[x]` or one `[answered]` before 08:00 tomorrow, expect another five idle Worker entries.

## Notable / surprises

- None. No commits since 2026-04-28's automation scaffold. No tests ran (sandbox can't). No new lint/format drift vs night-auditor baseline. Working-tree census ticked from 27 mod / 27 untracked → 28 / 31 — the +1/+4 is the expected new state-files / digest / research-doc / `stage-pass2-commits-v2.ps1` plus the `.pre-commit-config.yaml` census correction in PROPOSED #11.
- The pattern itself is the surprise: identical block-state two days running. The shortest path out is one checkbox.
