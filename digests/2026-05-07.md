# Digest — 2026-05-07

## TL;DR

The 7-day lock+index thread RESOLVED overnight (you cleared it between 12:00 yesterday and 02:37 tonight) — but a new FUSE-induced `.git/index.lock` leak from worker-8am's own `git checkout -b` re-blocked the day. P0 truncation fix is green-in-tree (89 lines off cli.py, 33 off config.py, all gates pass) but uncommitted. Plus a JOURNAL.md data-loss incident at 09:00: Python `read() + os.replace()` served a stale FUSE snapshot and overwrote three 2026-05-07 entries (spec-writer 06:50, planner 07:00, graduation-sweeper 07:30).

## Done today

- **Lock+index thread RESOLVED.** Night-auditor 02:37 confirmed `.git/index.lock` ENOENT, `.git/index` rebuilt to 29046 bytes, `git status` returns sane output. The 6-entry NEEDS-INPUT thread is de-facto resolved; planner 07:00 archived all 6 to JOURNAL. HEAD = `acd6725` unchanged.
- **New P0 surfaced + repaired in working tree.** Night-auditor 02:37 found tail-corruption on cli.py + config.py from worker-8am 2026-05-06's heredoc-recovery. Worker-8am 08:00 found the corruption was broader than reported (89L from cli.py + 33L from config.py, not 5+3 — duplicate budget-CLI block + duplicate load_settings/project_root, all byte-identical to originals so no content lost). All DoD gates green: `compileall` exit 0, AST clean on 4 brain files, ruff invalid-syntax 11 → 0, 7/7 ToolSpec round-trip + (a)-half wire-up tests PASS, 92 PASS + 1 SKIPPED combined regression. Branch `automation/worker-2026-05-07-8am` exists from the failed `git checkout -b`.
- **Two new spec drafts.** Spec-writer 06:50 (entry text lost, recovered from STATE.md) produced `2026-05-07-p4c1-persona-projection-layer-spec.md` (P4.C1 router persona-projection (a)/(b) split) and `2026-05-07-p26-supervisor-validation-spec.md` (P2.6 description-correction — implementation already in `acd6725`, scope is validate + decision doc).
- **P2.6 description sharpened inline** (description-correction pattern, same as P2.5 yesterday).

## Waiting on you (3 actionable + 2 carry-overs)

1. **`rm -f .git/index.lock`** from a Windows shell — the one-minute action that unblocks tomorrow's 8am to commit the P0 directly on the existing `automation/worker-2026-05-07-8am` branch (no fresh `git checkout -b` needed, which avoids re-triggering the FUSE leak). Once landed, P2.7 (a)-half + the five 2026-05-05 lock-blocked diffs all become committable.
2. **Verify JOURNAL.md NTFS-side** (worker-9am 09:00 NEEDS-INPUT entry). Three 2026-05-07 entries (spec-writer 06:50, planner 07:00, graduation-sweeper 07:30) were dropped from the FUSE view by his Python rewrite. NEEDS-INPUT.md is intact and contains the four spec-writer entries verbatim; STATE.md 07:00 carries the planner's structural decisions; if Windows-side JOURNAL still has the prose, paste it back. Worker-8am 08:00 entry is recovered verbatim in worker-9am's postscript.
3. **Spec-writer's 4 new picks (all spec-recommends-(a), Worker can ship without waiting):** P4.C1 `[brain.persona]` config knobs (a per-rule / b master switch / c hard-coded); P4.C1 apology-dedup heuristic (a keep first / b keep last / c drop all); P2.6 Step 5 XML-tightening (a `RestartOnFailure` only / b all six / c skip); P2.6 decision doc number (a next free integer / b/c sub-numbers).
4. **P2.7 cost-table location** (open since 2026-05-05) — worker-8am 2026-05-06 already shipped against recommended (a) inline constants per spec escape hatch.
5. **GitHub MCP** — still not connected in scheduled-task sessions.

## For your approval

No new PROPOSED items today. The 32-item backlog stands as last reviewed; PROPOSED #30 (ruff drift) and #31 (NUL-byte hygiene) remain queued from 2026-05-06.

## Queued for tomorrow

- **Recommended pick if lock cleared:** 8am stages the P0 commit directly on the existing branch via `git add sabrina-2/src/sabrina/cli.py sabrina-2/src/sabrina/config.py && git commit -m "fix(cli,config): repair tail-corruption from heredoc-recovery (queue: P0 cli+config truncation fix)"` with `Windows-pending: e2e`. No fresh `git checkout -b` — that's what keeps leaking the lock.
- **Fall-throughs once P0 lands:** P2.7 (a)-half commit chain reopens; the five 2026-05-05 lock-blocked diffs (P5.4 / P5.5 / P5.1 / worker-12pm) are partition-clean and committable in any order.
- **`[windows-required]` parked for your Windows session:** (b)-half wire-up, P2.3, P2.5, P2.6, P2.8.

## Notable / surprises

- **FUSE-induced lock leak is now demonstrably reproducible per-Worker-session.** Worker-8am's own `git checkout -b` leaked a 0-byte lock owned by his sandbox user; Cowork sandbox can't unlink its own lock cleanly. 9 consecutive Workers across 3 days have hit it. Worker-8am proposed a `roles/worker.md` step 7 forensic carve-out (mtime within last 5 min + 0-byte + sandbox-user-owner → safe to remove); structural fix worth your call.
- **JOURNAL.md data-loss incident at 09:00.** Python `read() + os.replace()` served a stale FUSE snapshot. ~22 KB / 4 entries lost from FUSE view (one recovered verbatim from session context; three not). Lesson: deprecate Python rewrite for JOURNAL appends in this FUSE setup. Edit tool with anchor against a known-prior line is fail-loud — worker-10am, worker-11am, worker-12pm all switched to it without incident.
- **4 consecutive Worker idle-exits (9/10/11/12)** stacked on a single one-minute Eric action — correctly anti-sprawl per CLAUDE.md and worker-8am's explicit handoff. Adding more lock-blocked diffs would compound recovery cost.
- **Today's net Worker output:** zero commits across four scheduled slots; one P0 fix sitting green-in-tree on a pre-created branch waiting on the lock clear.
