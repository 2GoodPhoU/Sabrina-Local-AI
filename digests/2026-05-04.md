# Digest — 2026-05-04

## TL;DR

Day-6 idle pattern broken — sort of. Out-of-band workflow-efficiency-analysis + unblock run reshaped the harness (Partial-DoD tier, planner split-authority, QUEUE split), 9am Worker actually did the (a)-half wire-up with all Linux gates GREEN, but commit blocked on a stale 5-day-old `.git/index.lock`. ROADMAP.md filed; 31 decomposed QUEUE items added. `DONE.md` still empty — no commit landed.

## Done today

Nothing committed (DONE.md empty), but four meaningful in-tree changes:

- **Harness rewrite** (15:00 unblock run) — CLAUDE.md gained Partial-DoD tier ("Linux-validated, Windows-pending"); `roles/planner.md` step 3 gained standing authority to split mixed-surface QUEUE items without `[x]`; `roles/night-auditor.md` step 2 dropped pytest from required checks. PROPOSED #4 + #9 + #19 + #20 marked `[x]`.
- **(a)-half wire-up code-complete** (9am Worker) — `protocol.py` +50, `claude.py` +154 wire-up on top of personality, `ollama.py` +15 (raises `NotImplementedError`), `test_smoke.py` +396 with 6 new tests. Linux gates GREEN: compileall clean, AST clean, all 6 new tests PASS, 4 existing ToolSpec round-trip tests PASS. Commit bailed on stale `.git/index.lock` per role-doc letter.
- **ROADMAP.md filed** (17:00) — release-targeted master plan to v1.0; phase ladder + gates + ~62–70% current-state marker. Pointer added to CLAUDE.md.
- **Backlog decomposed** (18:00) — 31 new worker-pickable QUEUE items across Phase 2/4/5/6 under `## Decomposed (next-pull-ready)` + `## Decomposed by phase`. 22 Linux-runnable / 9 Windows-required.

## Waiting on you (5)

1. **[worker-9am / 2026-05-04]** Stale `.git/index.lock` (5d old, 0 bytes, mtime 2026-04-29 14:15) blocks the (a)-half commit. Pick (a) `rm` it and commit yourself tonight, (b) `rm` it and let tomorrow's 8am Worker commit from the in-tree state, or (c) `rm` + revert + re-do. Recommendation: (a) or (b). **This entry duplicated itself in NEEDS-INPUT — same content twice.** *(highest leverage tonight)*
2. **[researcher / 2026-04-30]** Pick a bounded question (menu: personality-projection parity, "Hey Sabrina" wake-word training, Live2D architecture, recursive tool-loop hardening, or write your own). 4th consecutive Researcher dedup-skip.
3. **[night-auditor / 2026-04-30]** GitHub MCP not connected in scheduled-task sessions. Install the MCP or remove the step from the role doc.

(Three previously-open items now `[answered]` via the 15:00 unblock: worker-8am DoD fork, night-auditor pytest-gate, planner day-2 marker.)

## For your approval

PROPOSED grew 14 → 25 overnight; 4 now `[x]` + 1 `[informational]`, leaving **20 unchecked**. Highest leverage:

- **#5** (P1, high blast radius) — working-tree triage. 28 modified + 33 untracked + 1 deleted-not-staged. Subordinates #11 / #13 / #14 fold in. Gating for any code-touching Worker run that doesn't entangle with the dirty diff. **Now extra-relevant** because the (a)-half code sits in the dirty tree awaiting commit.
- **#21** (P1) — tag every QUEUE item `[linux-runnable]` / `[windows-required]`. The new decomposed items already do this; this proposal codifies it as planner contract.
- **#22** (P1) — auto-promote doc-only census-correction items without `[x]`. Would clear #8, #11, #13, #14 mechanically.
- **#23** (P1) — convert idle Worker slots into a bounded sidework ladder (code-review / testing-strategy / ruff --fix on clean files / census-sync).
- **#24** (P1) — move planner/researcher dedup-marker entries out of NEEDS-INPUT into a `STATUS-MARKERS.md`.
- **#1, #2, #3, #6, #7, #10, #12, #15** — commits / docs / cleanups subordinate to or shaped by #5.

## Queued for tomorrow

- `[in-progress] [linux-runnable] [P1]` ClaudeBrain wire-up (a)-half — code-complete, blocked on stale lock. Tomorrow's 8am Worker picks this up first if you clear the lock.
- `[ ] [windows-required] [P1]` ClaudeBrain wire-up (b)-half — events.py + voice_loop.py + sabrina.toml flip + Windows e2e. Waiting for your Windows session.
- **Next-pull-ready Linux-runnable backups** if (a)-half stays locked: P5.2 (shortcut table → YAML, ~0.5h), P5.4 (test fixtures port, ~1h), P5.5 (pytest scaffolding, ~2h), P2.1 (wake-word training research draft).
- 27 more decomposed items across Phase 2/4/5/6.

## Notable / surprises

- **Edit-tool truncation hit repeatedly mid-run** in worker-9am (ollama.py, protocol.py, claude.py, test_smoke.py, NEEDS-INPUT.md, QUEUE.md, JOURNAL.md all needed restoration). Recovery via `cat > path <<'EOF'` heredoc + AST + `wc -l`. Hit again in the 18:00 backlog-decomposition run (QUEUE.md heredoc append truncated mid-word at byte 9296; STATE.md Edit ate the bottom 11 lines). Canonical write path under Cowork sandbox should be bash heredoc with post-write byte-count + tail verification — but heredoc *appends* also truncate occasionally. Worth a PROPOSED entry; not yet filed.
- **JOURNAL.md has duplicated entries** (worker-9am appears 3 times in slightly different forms; 07:00 planner appears twice; 08:00 worker-8am appears twice; 15:00 unblock entry appears twice). Caused by the same Edit-truncation issue. Functional but noisy; tomorrow's planner / night-auditor should expect to dedup mentally.
- **NEEDS-INPUT.md has the worker-9am entry duplicated** as well — same content twice, not two distinct entries.
- 6-day idle pattern technically still unbroken on the strict commit-landed metric — but the wire-up itself is complete and verified, which is the first productive Worker run since 2026-04-29.

## Shortest path to a productive 2026-05-05

`rm .git/index.lock`. Then either commit the (a)-half yourself with the staging plan in the worker-9am NEEDS-INPUT entry, or leave the in-tree state for tomorrow's 8am Worker. Everything else (researcher question, GitHub MCP, working-tree triage) is lower-leverage today.
