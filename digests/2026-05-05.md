# Digest — 2026-05-05

## TL;DR

First plausibly-productive Worker streak in 7 days — five Linux-runnable diffs landed in-tree all green, plus DONE.md got its first-ever content entry (P2.1 wake-word research). All five commits still blocked on the same stale `.git/index.lock`, and the index itself is now confirmed corrupted. One-line Eric action (`rm .git/index.lock && git read-tree HEAD`) unblocks five independent commits at once.

## Done today

- **P2.1 wake-word research → DONE.md** (worker-12pm) — first content entry in DONE.md since the file was created 2026-04-28. `research/2026-05-05-wake-word-training-pipeline.md` shipped (~270 lines, Recommendation = Go). Predecessor plan's Windows-CPU assumption is wrong against current upstream — train inside WSL2, swap hand-rolled loop for `piper-sample-generator` wrapper, adopt P2.2's `models/openwakeword/hey_sabrina.onnx` path. Three new PROPOSED items (#32–#34).
- **Five in-tree-green-but-commit-blocked diffs** — (a)-half wire-up (worker-9am 2026-05-04, still in tree), P5.2 shortcut table (worker-8am, 17 entries + 20 tests + .gitignore tweak), P5.4 test fixtures (worker-9am, byte-identical port + 11 tests), P5.5 pytest scaffolding (worker-10am, conftest + test_utils + 14+1 tests), P5.1 audio device fallback (worker-11am, `select_input_device` + 23 tests). All Linux Partial-DoD gates GREEN per slot.
- **Major positive externality on `test_smoke.py`** — worker-10am's P5.5 conftest took the suite from 0-collected (`ModuleNotFoundError: No module named 'sabrina'`) to 180/195 passing in the Linux sandbox. All 6 (a)-half wire-up tests + 4 ToolSpec round-trip tests now PASS in-sandbox.
- **Two new spec drafts** (spec-writer 06:50) — `2026-05-05-p55-pytest-scaffolding-port-spec.md` (used immediately by worker-10am) and `2026-05-05-p27-budget-tracker-spec.md` (names the (a)/(b) split; recommends inline cost-table + JSONL persistence; one open NEEDS-INPUT).
- **Two new automation roles** (06:50 + 07:30 slots) — spec-writer and graduation-sweeper registered to `schedule.json` (now 11 scheduled tasks). Graduation-sweeper's first run cleanly idle-exited (no eligible items).

## Waiting on you (4 distinct decisions)

1. **Stale `.git/index.lock` + corrupted `.git/index`** — *highest leverage*. Lock is now 6 days old (mtime 2026-04-29 14:15:52 UTC). Worker-9am 2026-05-05 found `.git/index` itself returns `fatal: unknown index entry format 0x74000000` — clearing the lock alone may not be enough. Recovery: `rm .git/index.lock && git read-tree HEAD` (or `git reset --mixed`). One minute of work unblocks five independent commits. *(Reflected as 5 separate NEEDS-INPUT entries — all the same thread.)*
2. **[spec-writer / 2026-05-05]** P2.7 budget tracker — pick (a) inline constants in `budget.py` *(spec recommends; Worker can ship against this without an answer)*, (b) `[budget.cost_table]` in `sabrina.toml`, or (c) `data/anthropic-prices.yaml`.
3. **[night-auditor / 2026-04-30]** GitHub MCP not connected in scheduled-task sessions. Install or remove the step from role doc.
4. **[researcher / 2026-04-30]** Bounded-question entry — de-facto resolved (P2.1 was the picked question; shipped today). Can be marked `[answered]` and removed in any planner run.

## For your approval

PROPOSED grew 25 → 28 overnight; 4 `[x]` + 1 informational + **23 unchecked**. New today (all P1 or P2):

- **#32** (P1) — amend `rebuild/drafts/wake-word-plan.md` to recommend WSL2 + adopt `piper-sample-generator` + drop `voices/wake/` for `sabrina-2/models/openwakeword/`. Locks in the path before P2.2's first Worker pull.
- **#33** (P2) — probe whether the Cowork Linux sandbox can actually host openWakeWord training before P2.2 is scheduled. If it can't, P2.2 flips from `[linux-runnable]` to `[windows-required]`.
- **#34** (P2) — cite upstream's published acceptance criteria in the wake-word ship criterion.
- **#29 / #30 / #31** (P2 each, night-auditor 2026-05-05) — JOURNAL dedup, 16:45-digest tail-truncation reconstruction, STATE.md "Last updated" staleness (last one auto-resolves on next planner run).

Highest pending leverage unchanged from yesterday: **#5** (working-tree triage — now extra-relevant because five Worker diffs sit in the dirty tree), **#22** (auto-promote doc-only census corrections), **#23** (idle Worker slots → bounded sidework ladder). The 5 in-tree-green items + P2.7 spec-writer's NEEDS-INPUT mean Workers don't need #23 today, but the proposal still has structural value.

## Queued for tomorrow

- **Five `[in-progress]` lock-blocked**: (a)-half wire-up, P5.2, P5.4, P5.5, P5.1 — all green, all independent, all wait on the same one-minute Eric unblock.
- **`[ ] [windows-required] [P1]`** ClaudeBrain wire-up (b)-half — events.py + voice_loop.py + sabrina.toml flip + Windows e2e. Waiting for your Windows session.
- **Lock-immune Linux-runnable pulls** if lock still in place at 8am: **P2.7 budget tracker (a)-half** (M, spec exists, ship against recommended (a) inline constants) or **P2.4 autostart research draft** (M, doc-only, no deps).
- 24 more decomposed items across Phase 2/4/5/6.

## Notable / surprises

- **DONE.md broke its 8-day vacancy.** First-ever content entry landed today.
- **`.git/index` is corrupted, not just locked.** New finding from worker-9am 2026-05-05; recovery now needs two steps, not one.
- **`services/hearing/hearing.py` doesn't contain the loop QUEUE P5.1 references.** Worker-11am found the "loop with name matching" lives only in `rebuild/drafts/old-repo-migration-audit.md` line 45 as an aspirational utility. Implementation honors the *intent* (substring match + graceful fallback) on the rebuild's `sounddevice` backend, not a literal port.
- **Edit-tool truncation continued hammering state files** — hit QUEUE.md (worker-9am, worker-11am), conftest.py (worker-10am), .gitignore (worker-8am), plus NULL-padding pattern on QUEUE.md / NEEDS-INPUT.md (worker-8am). All recovered via Python rewrite + `os.fsync`. Worker-9am + worker-11am both recommend Workers prefer Python rewrite over Edit-tool for QUEUE.md-sized files going forward.
- **NEEDS-INPUT.md headline count is 8 entries but only 4 distinct decisions** — five entries are the same lock thread written by five different Workers per role-doc-required protocol.

## Shortest path to a productive 2026-05-06

`rm .git/index.lock && git read-tree HEAD`. Five commits land independently in any order. Everything else is lower-leverage today.
