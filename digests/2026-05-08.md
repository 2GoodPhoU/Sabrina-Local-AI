# Digest — 2026-05-08

## TL;DR

Most productive in-tree day on record — 5 fresh (a)-halves landed across 5 consecutive Worker slots (P2.2, P4.B1, P4.C1, P4.C2, P5.3). All Linux Partial-DoD gates green. Zero commits — same FUSE-leaked `.git/index.lock` from yesterday is now ~22h old and still untouched. **One `rm -f .git/index.lock` from a Windows shell unblocks 10 committable diffs in any order.**

## Done today (in-tree, all lock-blocked from commit)

- **P2.2 (a)-half — wake-word training tooling** (worker-8am). Authored 5 files under `sabrina-2/tools/wake-training/` + `test_wake_training_tooling.py` (12 tests PASS). Flipped `[wake_word].model` to `models/openwakeword/hey_sabrina.onnx` (`enabled = false` stays — P2.3 flips it). Touched both project-root + `sabrina-2/.gitignore` (necessary for the spec's path choice).
- **P4.B1 (a)-half — automation safety primitives** (worker-9am). Authored `automation/{kill_switch,dry_run,scaffold}.py` + `AutomationConfig` + `[automation]` block + claude.py dispatch-loop poll-points + `test_automation_safety.py` (18 tests PASS). Kill-switch + dry-run + ToolSpec scaffold; `noop_action` not in `BUILTIN_TOOLS` per spec.
- **P4.C1 (a)-half — persona-projection layer** (worker-10am). Lifted persona blocks from claude.py into new `brain/persona.py` (-103 lines from claude.py, back-compat re-export). Added `_VOICE_RULES_BLOCK_OLLAMA` for parity-audit fixes + `project_ollama_text` (4 active rules) + `OllamaBrain` buffered-then-projected chat path + `BrainPersonaConfig` + `test_persona_projection.py` (14 tests PASS).
- **P4.C2 (a)-half — router with Claude/Ollama dispatch + budget-gate** (worker-11am). Authored `brain/router.py` implementing the `Brain` protocol; three policies (`force_local` / `claude_default` / `cost_aware`); per-backend system-prompt swap; tools-drop on Ollama; `make_router_from_settings` factory; `test_router.py` (17 tests PASS).
- **P5.3 (a)-half — system-deps setup script** (worker-12pm). The production `sabrina-2/scripts/setup.py` (547 LOC) was already in tree from a missing-journal session on 2026-05-06; worker-12pm authored the missing `test_setup_script.py` (30 tests PASS) and verified the in-tree implementation against the spec verbatim.
- **2 new spec drafts landed overnight** (spec-writer 06:50): `2026-05-08-p4b1-automation-safety-primitives-spec.md` + `2026-05-08-p4c2-router-routing-policy-spec.md`. Both consumed same-day by Workers.

Combined regression at end of day: **183 PASS + 1 SKIPPED in 1.90s** (+5 (a)-halves of new test surface, zero pre-existing failures introduced).

## Waiting on you (3 actionable + carry-overs)

1. **`rm -f .git/index.lock`** from a Windows shell — same lock from worker-8am 2026-05-07's `git checkout -b` leak, 0 bytes, mtime 2026-05-07 14:24:11 UTC, untouched ~22h. Unblocks 10 committable diffs (P0 cli.py+config.py + P5.4 + P5.5 + P5.1 + P2.7 + P2.2 + P4.B1 + P4.C1 + P4.C2 + P5.3). Stay on existing `automation/worker-2026-05-07-8am` branch — do NOT `git checkout -b` (that's what keeps leaking the lock; tomorrow's 8am has explicit pickup recipes per Worker entry).
2. **Verify JOURNAL.md NTFS-side** (worker-9am 2026-05-07 09:00 NEEDS-INPUT) — three 2026-05-07 entries dropped from FUSE view by his Python rewrite. Still open; no progress today.
3. **NEEDS-INPUT count 8 → 14** today. Six new spec-writer 06:50 picks (3 for P4.B1, 3 for P4.C2) — all spec-recommend an answer; Workers already shipped (a)-halves against the recommendations without waiting. Carry-overs unchanged: P2.7 cost-table (worker-8am 2026-05-06 already shipped against (a)), 4 spec-writer 2026-05-07 picks (P4.C1 + P2.6), GitHub MCP, 2026-04-30 bounded-question (now 4× dedup-skipped).

## For your approval

No new PROPOSED items today. Backlog stands at 32 items (4 `[x]` + 2 informational + 26 unchecked). Graduation-sweeper exited 0 — all eligible-by-age items still subordinate to the unchecked PROPOSED #5/#6 working-tree triage; same calibration story as 2026-05-06 / 2026-05-07.

## Queued for tomorrow

- **If lock cleared:** ten in-order commits on existing branch — partition-clean, any order. Recommended P0 first (smallest, most independent).
- **If lock not cleared:** role-doc step 2 skip recurs; 8am idles. Pull-ready candidates if a Worker can branch: P5.3 already shipped, no other `[ ]` Linux-runnable Phase 4 items remain (B2/B3 dep on B1, C3 windows). Phase 5 P5.6/P5.7 are dep-blocked. Day idles.
- **`[windows-required]` parked for your Windows session:** (b)-half wire-up, P2.3, P2.5, P2.6, P2.8, P4.B4, P4.C3.

## Notable / surprises

- **5 fresh (a)-halves in 5 consecutive Worker slots** — first time the partial-DoD tier and the recommended-(a) escape-hatch pattern have produced uninterrupted Linux-runnable throughput across an entire morning chain. Two of those (P4.B1, P4.C2) shipped same-day as their specs.
- **FUSE lock leak now 11 consecutive Worker bails** across 3 days. Worker-8am 2026-05-07's proposed `roles/worker.md` step 7 forensic carve-out (mtime within last 5 min + 0-byte + sandbox-user-owner → safe to remove) would have unblocked all 5 of today's Workers cleanly. Structural-fix call still yours.
- **Edit-tool truncation hazard fired ~20+ times across the 5 Worker runs**, all recovered (variously via `git show HEAD:` tail-graft, Python heredoc rewrites, AST/TOML re-verification). Bash/FUSE view of JOURNAL.md was confirmed stale at 692 lines vs Read tool's canonical 800-line view throughout — exactly the divergence pattern worker-9am 2026-05-07 documented.
- **P5.3 attribution oddity**: production code was already in tree from a 2026-05-06 session whose JOURNAL entry never landed. Worker-12pm verified-against-spec and added the missing test file; if you prefer cleaner attribution (revert + re-author), that's a separate state-history correction.
- **Today's net commit count: 0.** Today's net in-tree progress: ~3000 LOC of new code + tests across 5 (a)-halves + 2 specs + 6 NEEDS-INPUT picks documented.
